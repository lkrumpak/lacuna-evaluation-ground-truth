/** (github.com/dynamic-deadfunction-detector)
* Instrumentation log function used by the instrumenter
* note that the data object is already stringified.
*/
var logHistory = [];
function instrumentation_log(data) {
    
                function exists(e) { return e.file == jData.file && e.range[0] == jData.range[0] && e.range[1] == jData.range[1]; }
                var jData = JSON.parse(data);
                if (logHistory.some(exists)){ return; }
                logHistory.push(jData);
    fetch("http://127.0.0.1:8004/alivefunction", {
                method: "POST",
                headers: { "Accept": "application/json", "Content-Type": "application/json" },
                body: data
            }).then((response) => { });
}


//     Underscore.js 1.8.3
//     http://underscorejs.org
//     (c) 2009-2015 Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
//     Underscore may be freely distributed under the MIT license.

(function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[228,52901],"range":[217,52901],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":166,"label":"backbone"}');

  // Baseline setup
  // --------------

  // Establish the root object, `window` in the browser, or `exports` on the server.
  var root = this;

  // Save the previous value of the `_` variable.
  var previousUnderscore = root._;

  // Save bytes in the minified (but not gzipped) version:
  var ArrayProto = Array.prototype, ObjProto = Object.prototype, FuncProto = Function.prototype;

  // Create quick reference variables for speed access to core prototypes.
  var
    push             = ArrayProto.push,
    slice            = ArrayProto.slice,
    toString         = ObjProto.toString,
    hasOwnProperty   = ObjProto.hasOwnProperty;

  // All **ECMAScript 5** native function implementations that we hope to use
  // are declared here.
  var
    nativeIsArray      = Array.isArray,
    nativeKeys         = Object.keys,
    nativeBind         = FuncProto.bind,
    nativeCreate       = Object.create;

  // Naked function reference for surrogate-prototype-swapping.
  var Ctor = function(){instrumentation_log('{"type":"FunctionExpression","bodyRange":[1228,1230],"range":[1218,1230],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":0,"label":"backbone"}');};

  // Create a safe reference to the Underscore object for use below.
  var _ = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[1326,1443],"range":[1312,1443],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":1,"label":"backbone"}');
    if (obj instanceof _) return obj;
    if (!(this instanceof _)) return new _(obj);
    this._wrapped = obj;
  };

  // Export the Underscore object for **Node.js**, with
  // backwards-compatibility for the old `require()` API. If we're in
  // the browser, add `_` as a global object.
  if (typeof exports !== 'undefined') {
    if (typeof module !== 'undefined' && module.exports) {
      exports = module.exports = _;
    }
    exports._ = _;
  } else {
    root._ = _;
  }

  // Current version.
  _.VERSION = '1.8.3';

  // Internal function that returns an efficient (for current engines) version
  // of the passed-in callback, to be repeatedly applied in other Underscore
  // functions.
  var optimizeCb = function(func, context, argCount) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2081,2732],"range":[2047,2732],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":7,"label":"backbone"}');
    if (context === void 0) return func;
    switch (argCount == null ? 3 : argCount) {
      case 1: return function(value) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2208,2259],"range":[2192,2259],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":2,"label":"backbone"}');
        return func.call(context, value);
      };
      case 2: return function(value, other) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2305,2363],"range":[2282,2363],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":3,"label":"backbone"}');
        return func.call(context, value, other);
      };
      case 3: return function(value, index, collection) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2421,2491],"range":[2386,2491],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":4,"label":"backbone"}');
        return func.call(context, value, index, collection);
      };
      case 4: return function(accumulator, value, index, collection) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2562,2645],"range":[2514,2645],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":5,"label":"backbone"}');
        return func.call(context, accumulator, value, index, collection);
      };
    }
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[2675,2727],"range":[2664,2727],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":6,"label":"backbone"}');
      return func.apply(context, arguments);
    };
  };

  // A mostly-internal function to generate callbacks that can be applied
  // to each element in a collection, returning the desired result — either
  // identity, an arbitrary callback, a property matcher, or a property accessor.
  var cb = function(value, context, argCount) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[3013,3216],"range":[2978,3216],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":8,"label":"backbone"}');
    if (value == null) return _.identity;
    if (_.isFunction(value)) return optimizeCb(value, context, argCount);
    if (_.isObject(value)) return _.matcher(value);
    return _.property(value);
  };
  _.iteratee = function(value, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[3258,3304],"range":[3233,3304],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":9,"label":"backbone"}');
    return cb(value, context, Infinity);
  };

  // An internal function for creating assigner functions.
  var createAssigner = function(keysFunc, undefinedOnly) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[3423,3886],"range":[3389,3886],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":11,"label":"backbone"}');
    return function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[3450,3881],"range":[3436,3881],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":10,"label":"backbone"}');
      var length = arguments.length;
      if (length < 2 || obj == null) return obj;
      for (var index = 1; index < length; index++) {
        var source = arguments[index],
            keys = keysFunc(source),
            l = keys.length;
        for (var i = 0; i < l; i++) {
          var key = keys[i];
          if (!undefinedOnly || obj[key] === void 0) obj[key] = source[key];
        }
      }
      return obj;
    };
  };

  // An internal function for creating a new object that inherits from another.
  var baseCreate = function(prototype) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[4008,4215],"range":[3988,4215],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":12,"label":"backbone"}');
    if (!_.isObject(prototype)) return {};
    if (nativeCreate) return nativeCreate(prototype);
    Ctor.prototype = prototype;
    var result = new Ctor;
    Ctor.prototype = null;
    return result;
  };

  var property = function(key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[4249,4334],"range":[4235,4334],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":14,"label":"backbone"}');
    return function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[4276,4329],"range":[4262,4329],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":13,"label":"backbone"}');
      return obj == null ? void 0 : obj[key];
    };
  };

  // Helper for collection methods to determine whether a collection
  // should be iterated as an array or as an object
  // Related: http://people.mozilla.org/~jorendorff/es6-draft.html#sec-tolength
  // Avoids a very nasty iOS 8 JIT bug on ARM-64. #2094
  var MAX_ARRAY_INDEX = Math.pow(2, 53) - 1;
  var getLength = property('length');
  var isArrayLike = function(collection) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[4718,4845],"range":[4697,4845],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":15,"label":"backbone"}');
    var length = getLength(collection);
    return typeof length == 'number' && length >= 0 && length <= MAX_ARRAY_INDEX;
  };

  // Collection Functions
  // --------------------

  // The cornerstone, an `each` implementation, aka `forEach`.
  // Handles raw objects in addition to array-likes. Treats all
  // sparse array-likes as if they were dense.
  _.each = _.forEach = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[5131,5507],"range":[5098,5507],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":16,"label":"backbone"}');
    iteratee = optimizeCb(iteratee, context);
    var i, length;
    if (isArrayLike(obj)) {
      for (i = 0, length = obj.length; i < length; i++) {
        iteratee(obj[i], i, obj);
      }
    } else {
      var keys = _.keys(obj);
      for (i = 0, length = keys.length; i < length; i++) {
        iteratee(obj[keys[i]], keys[i], obj);
      }
    }
    return obj;
  };

  // Return the results of applying the iteratee to each element.
  _.map = _.collect = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[5631,5990],"range":[5598,5990],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":17,"label":"backbone"}');
    iteratee = cb(iteratee, context);
    var keys = !isArrayLike(obj) && _.keys(obj),
        length = (keys || obj).length,
        results = Array(length);
    for (var index = 0; index < length; index++) {
      var currentKey = keys ? keys[index] : index;
      results[index] = iteratee(obj[currentKey], currentKey, obj);
    }
    return results;
  };

  // Create a reducing function iterating left or right.
  function createReduce(dir) {instrumentation_log('{"type":"FunctionDeclaration","bodyRange":[6079,6963],"range":[6052,6963],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":20,"label":"backbone"}');
    // Optimized iterator function as using arguments.length
    // in the main function will deoptimize the, see #1991.
    function iterator(obj, iteratee, memo, keys, index, length) {instrumentation_log('{"type":"FunctionDeclaration","bodyRange":[6266,6477],"range":[6206,6477],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":18,"label":"backbone"}');
      for (; index >= 0 && index < length; index += dir) {
        var currentKey = keys ? keys[index] : index;
        memo = iteratee(memo, obj[currentKey], currentKey, obj);
      }
      return memo;
    }

    return function(obj, iteratee, memo, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[6529,6958],"range":[6490,6958],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":19,"label":"backbone"}');
      iteratee = optimizeCb(iteratee, context, 4);
      var keys = !isArrayLike(obj) && _.keys(obj),
          length = (keys || obj).length,
          index = dir > 0 ? 0 : length - 1;
      // Determine the initial value if none is provided.
      if (arguments.length < 3) {
        memo = obj[keys ? keys[index] : index];
        index += dir;
      }
      return iterator(obj, iteratee, memo, keys, index, length);
    };
  }

  // **Reduce** builds up a single result from a list of values, aka `inject`,
  // or `foldl`.
  _.reduce = _.foldl = _.inject = createReduce(1);

  // The right-associative version of reduce, also known as `foldr`.
  _.reduceRight = _.foldr = createReduce(-1);

  // Return the first value which passes a truth test. Aliased as `detect`.
  _.find = _.detect = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[7361,7579],"range":[7327,7579],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":21,"label":"backbone"}');
    var key;
    if (isArrayLike(obj)) {
      key = _.findIndex(obj, predicate, context);
    } else {
      key = _.findKey(obj, predicate, context);
    }
    if (key !== void 0 && key !== -1) return obj[key];
  };

  // Return all the elements that pass a truth test.
  // Aliased as `select`.
  _.filter = _.select = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[7719,7923],"range":[7685,7923],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":23,"label":"backbone"}');
    var results = [];
    predicate = cb(predicate, context);
    _.each(obj, function(value, index, list) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[7828,7897],"range":[7799,7897],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":22,"label":"backbone"}');
      if (predicate(value, index, list)) results.push(value);
    });
    return results;
  };

  // Return all the elements for which a truth test fails.
  _.reject = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[8032,8097],"range":[7998,8097],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":24,"label":"backbone"}');
    return _.filter(obj, _.negate(cb(predicate)), context);
  };

  // Determine whether all of the elements match a truth test.
  // Aliased as `all`.
  _.every = _.all = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[8240,8568],"range":[8206,8568],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":25,"label":"backbone"}');
    predicate = cb(predicate, context);
    var keys = !isArrayLike(obj) && _.keys(obj),
        length = (keys || obj).length;
    for (var index = 0; index < length; index++) {
      var currentKey = keys ? keys[index] : index;
      if (!predicate(obj[currentKey], currentKey, obj)) return false;
    }
    return true;
  };

  // Determine if at least one element in the object matches a truth test.
  // Aliased as `any`.
  _.some = _.any = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[8722,9049],"range":[8688,9049],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":26,"label":"backbone"}');
    predicate = cb(predicate, context);
    var keys = !isArrayLike(obj) && _.keys(obj),
        length = (keys || obj).length;
    for (var index = 0; index < length; index++) {
      var currentKey = keys ? keys[index] : index;
      if (predicate(obj[currentKey], currentKey, obj)) return true;
    }
    return false;
  };

  // Determine if the array or object contains a given item (using `===`).
  // Aliased as `includes` and `include`.
  _.contains = _.includes = _.include = function(obj, item, fromIndex, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[9247,9411],"range":[9209,9411],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":27,"label":"backbone"}');
    if (!isArrayLike(obj)) obj = _.values(obj);
    if (typeof fromIndex != 'number' || guard) fromIndex = 0;
    return _.indexOf(obj, item, fromIndex) >= 0;
  };

  // Invoke a method (with arguments) on every item in a collection.
  _.invoke = function(obj, method) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[9518,9761],"range":[9496,9761],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":29,"label":"backbone"}');
    var args = slice.call(arguments, 2);
    var isFunc = _.isFunction(method);
    return _.map(obj, function(value) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[9638,9755],"range":[9622,9755],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":28,"label":"backbone"}');
      var func = isFunc ? method : value[method];
      return func == null ? func : func.apply(value, args);
    });
  };

  // Convenience version of a common use case of `map`: fetching a property.
  _.pluck = function(obj, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[9872,9917],"range":[9853,9917],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":30,"label":"backbone"}');
    return _.map(obj, _.property(key));
  };

  // Convenience version of a common use case of `filter`: selecting only objects
  // containing specific `key:value` pairs.
  _.where = function(obj, attrs) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[10079,10128],"range":[10058,10128],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":31,"label":"backbone"}');
    return _.filter(obj, _.matcher(attrs));
  };

  // Convenience version of a common use case of `find`: getting the first object
  // containing specific `key:value` pairs.
  _.findWhere = function(obj, attrs) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[10294,10341],"range":[10273,10341],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":32,"label":"backbone"}');
    return _.find(obj, _.matcher(attrs));
  };

  // Return the maximum element (or element-based computation).
  _.max = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[10451,11136],"range":[10418,11136],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":34,"label":"backbone"}');
    var result = -Infinity, lastComputed = -Infinity,
        value, computed;
    if (iteratee == null && obj != null) {
      obj = isArrayLike(obj) ? obj : _.values(obj);
      for (var i = 0, length = obj.length; i < length; i++) {
        value = obj[i];
        if (value > result) {
          result = value;
        }
      }
    } else {
      iteratee = cb(iteratee, context);
      _.each(obj, function(value, index, list) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[10887,11105],"range":[10858,11105],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":33,"label":"backbone"}');
        computed = iteratee(value, index, list);
        if (computed > lastComputed || computed === -Infinity && result === -Infinity) {
          result = value;
          lastComputed = computed;
        }
      });
    }
    return result;
  };

  // Return the minimum element (or element-based computation).
  _.min = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[11246,11927],"range":[11213,11927],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":36,"label":"backbone"}');
    var result = Infinity, lastComputed = Infinity,
        value, computed;
    if (iteratee == null && obj != null) {
      obj = isArrayLike(obj) ? obj : _.values(obj);
      for (var i = 0, length = obj.length; i < length; i++) {
        value = obj[i];
        if (value < result) {
          result = value;
        }
      }
    } else {
      iteratee = cb(iteratee, context);
      _.each(obj, function(value, index, list) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[11680,11896],"range":[11651,11896],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":35,"label":"backbone"}');
        computed = iteratee(value, index, list);
        if (computed < lastComputed || computed === Infinity && result === Infinity) {
          result = value;
          lastComputed = computed;
        }
      });
    }
    return result;
  };

  // Shuffle a collection, using the modern version of the
  // [Fisher-Yates shuffle](http://en.wikipedia.org/wiki/Fisher–Yates_shuffle).
  _.shuffle = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[12097,12431],"range":[12083,12431],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":37,"label":"backbone"}');
    var set = isArrayLike(obj) ? obj : _.values(obj);
    var length = set.length;
    var shuffled = Array(length);
    for (var index = 0, rand; index < length; index++) {
      rand = _.random(0, index);
      if (rand !== index) shuffled[index] = shuffled[rand];
      shuffled[rand] = set[index];
    }
    return shuffled;
  };

  // Sample **n** random values from a collection.
  // If **n** is not specified, returns a single random element.
  // The internal `guard` argument allows it to work with `map`.
  _.sample = function(obj, n, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[12652,12839],"range":[12628,12839],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":38,"label":"backbone"}');
    if (n == null || guard) {
      if (!isArrayLike(obj)) obj = _.values(obj);
      return obj[_.random(obj.length - 1)];
    }
    return _.shuffle(obj).slice(0, Math.max(0, n));
  };

  // Sort the object's values by a criterion produced by an iteratee.
  _.sortBy = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[12958,13449],"range":[12925,13449],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":41,"label":"backbone"}');
    iteratee = cb(iteratee, context);
    return _.pluck(_.map(obj, function(value, index, list) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[13057,13179],"range":[13028,13179],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":39,"label":"backbone"}');
      return {
        value: value,
        index: index,
        criteria: iteratee(value, index, list)
      };
    }).sort(function(left, right) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[13208,13433],"range":[13186,13433],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":40,"label":"backbone"}');
      var a = left.criteria;
      var b = right.criteria;
      if (a !== b) {
        if (a > b || a === void 0) return 1;
        if (a < b || b === void 0) return -1;
      }
      return left.index - right.index;
    }), 'value');
  };

  // An internal function used for aggregate "group by" operations.
  var group = function(behavior) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[13553,13833],"range":[13534,13833],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":44,"label":"backbone"}');
    return function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[13599,13828],"range":[13566,13828],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":43,"label":"backbone"}');
      var result = {};
      iteratee = cb(iteratee, context);
      _.each(obj, function(value, index) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[13705,13799],"range":[13682,13799],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":42,"label":"backbone"}');
        var key = iteratee(value, index, obj);
        behavior(result, value, key);
      });
      return result;
    };
  };

  // Groups the object's values by a criterion. Pass either a string attribute
  // to group by, or a function that returns the criterion.
  _.groupBy = group(function(result, value, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[14024,14110],"range":[13995,14110],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":45,"label":"backbone"}');
    if (_.has(result, key)) result[key].push(value); else result[key] = [value];
  });

  // Indexes the object's values by a criterion, similar to `groupBy`, but for
  // when you know that your index values will be unique.
  _.indexBy = group(function(result, value, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[14300,14330],"range":[14271,14330],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":46,"label":"backbone"}');
    result[key] = value;
  });

  // Counts instances of an object that group by a certain criterion. Pass
  // either a string attribute to count by, or a function that returns the
  // criterion.
  _.countBy = group(function(result, value, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[14549,14619],"range":[14520,14619],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":47,"label":"backbone"}');
    if (_.has(result, key)) result[key]++; else result[key] = 1;
  });

  // Safely create a real, live array from anything iterable.
  _.toArray = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[14713,14874],"range":[14699,14874],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":48,"label":"backbone"}');
    if (!obj) return [];
    if (_.isArray(obj)) return slice.call(obj);
    if (isArrayLike(obj)) return _.map(obj, _.identity);
    return _.values(obj);
  };

  // Return the number of elements in an object.
  _.size = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[14951,15050],"range":[14937,15050],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":49,"label":"backbone"}');
    if (obj == null) return 0;
    return isArrayLike(obj) ? obj.length : _.keys(obj).length;
  };

  // Split a collection into two arrays: one whose elements all satisfy the given
  // predicate, and one whose elements all do not satisfy the predicate.
  _.partition = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[15258,15472],"range":[15224,15472],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":51,"label":"backbone"}');
    predicate = cb(predicate, context);
    var pass = [], fail = [];
    _.each(obj, function(value, key, obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[15372,15441],"range":[15346,15441],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":50,"label":"backbone"}');
      (predicate(value, key, obj) ? pass : fail).push(value);
    });
    return [pass, fail];
  };

  // Array Functions
  // ---------------

  // Get the first element of an array. Passing **n** will return the first N
  // values in the array. Aliased as `head` and `take`. The **guard** check
  // allows it to work with `_.map`.
  _.first = _.head = _.take = function(array, n, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[15765,15900],"range":[15739,15900],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":52,"label":"backbone"}');
    if (array == null) return void 0;
    if (n == null || guard) return array[0];
    return _.initial(array, array.length - n);
  };

  // Returns everything but the last entry of the array. Especially useful on
  // the arguments object. Passing **n** will return all the values in
  // the array, excluding the last N.
  _.initial = function(array, n, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[16130,16226],"range":[16104,16226],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":53,"label":"backbone"}');
    return slice.call(array, 0, Math.max(0, array.length - (n == null || guard ? 1 : n)));
  };

  // Get the last element of an array. Passing **n** will return the last N
  // values in the array.
  _.last = function(array, n, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[16368,16528],"range":[16342,16528],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":54,"label":"backbone"}');
    if (array == null) return void 0;
    if (n == null || guard) return array[array.length - 1];
    return _.rest(array, Math.max(0, array.length - n));
  };

  // Returns everything but the first entry of the array. Aliased as `tail` and `drop`.
  // Especially useful on the arguments object. Passing an **n** will return
  // the rest N values in the array.
  _.rest = _.tail = _.drop = function(array, n, guard) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[16788,16851],"range":[16762,16851],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":55,"label":"backbone"}');
    return slice.call(array, n == null || guard ? 1 : n);
  };

  // Trim out all falsy values from an array.
  _.compact = function(array) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[16930,16975],"range":[16914,16975],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":56,"label":"backbone"}');
    return _.filter(array, _.identity);
  };

  // Internal implementation of a recursive `flatten` function.
  var flatten = function(input, shallow, strict, startIndex) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[17103,17683],"range":[17058,17683],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":57,"label":"backbone"}');
    var output = [], idx = 0;
    for (var i = startIndex || 0, length = getLength(input); i < length; i++) {
      var value = input[i];
      if (isArrayLike(value) && (_.isArray(value) || _.isArguments(value))) {
        //flatten current level of array or arguments object
        if (!shallow) value = flatten(value, shallow, strict);
        var j = 0, len = value.length;
        output.length += len;
        while (j < len) {
          output[idx++] = value[j++];
        }
      } else if (!strict) {
        output[idx++] = value;
      }
    }
    return output;
  };

  // Flatten out an array, either recursively (by default), or just one level.
  _.flatten = function(array, shallow) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[17804,17852],"range":[17779,17852],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":58,"label":"backbone"}');
    return flatten(array, shallow, false);
  };

  // Return a version of the array that does not contain the specified value(s).
  _.without = function(array) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[17966,18029],"range":[17950,18029],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":59,"label":"backbone"}');
    return _.difference(array, slice.call(arguments, 1));
  };

  // Produce a duplicate-free version of the array. If the array has already
  // been sorted, you have the option of using a faster algorithm.
  // Aliased as `unique`.
  _.uniq = _.unique = function(array, isSorted, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[18269,19007],"range":[18224,19007],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":60,"label":"backbone"}');
    if (!_.isBoolean(isSorted)) {
      context = iteratee;
      iteratee = isSorted;
      isSorted = false;
    }
    if (iteratee != null) iteratee = cb(iteratee, context);
    var result = [];
    var seen = [];
    for (var i = 0, length = getLength(array); i < length; i++) {
      var value = array[i],
          computed = iteratee ? iteratee(value, i, array) : value;
      if (isSorted) {
        if (!i || seen !== computed) result.push(value);
        seen = computed;
      } else if (iteratee) {
        if (!_.contains(seen, computed)) {
          seen.push(computed);
          result.push(value);
        }
      } else if (!_.contains(result, value)) {
        result.push(value);
      }
    }
    return result;
  };

  // Produce an array that contains the union: each distinct element from all of
  // the passed-in arrays.
  _.union = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[19141,19197],"range":[19130,19197],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":61,"label":"backbone"}');
    return _.uniq(flatten(arguments, true, true));
  };

  // Produce an array that contains every item shared between all the
  // passed-in arrays.
  _.intersection = function(array) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[19328,19709],"range":[19312,19709],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":62,"label":"backbone"}');
    var result = [];
    var argsLength = arguments.length;
    for (var i = 0, length = getLength(array); i < length; i++) {
      var item = array[i];
      if (_.contains(result, item)) continue;
      for (var j = 1; j < argsLength; j++) {
        if (!_.contains(arguments[j], item)) break;
      }
      if (j === argsLength) result.push(item);
    }
    return result;
  };

  // Take the difference between one array and a number of other arrays.
  // Only the elements present in just the first array will remain.
  _.difference = function(array) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[19886,20032],"range":[19870,20032],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":64,"label":"backbone"}');
    var rest = flatten(arguments, true, true, 1);
    return _.filter(array, function(value){instrumentation_log('{"type":"FunctionExpression","bodyRange":[19980,20026],"range":[19965,20026],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":63,"label":"backbone"}');
      return !_.contains(rest, value);
    });
  };

  // Zip together multiple lists into a single array -- elements that share
  // an index go together.
  _.zip = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[20159,20195],"range":[20148,20195],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":65,"label":"backbone"}');
    return _.unzip(arguments);
  };

  // Complement of _.zip. Unzip accepts an array of arrays and groups
  // each array's elements on shared indices
  _.unzip = function(array) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[20341,20563],"range":[20325,20563],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":66,"label":"backbone"}');
    var length = array && _.max(array, getLength).length || 0;
    var result = Array(length);

    for (var index = 0; index < length; index++) {
      result[index] = _.pluck(array, index);
    }
    return result;
  };

  // Converts lists into objects. Pass either a single array of `[key, value]`
  // pairs, or two parallel arrays of the same length -- one of keys, and one of
  // the corresponding values.
  _.object = function(list, values) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[20793,21030],"range":[20770,21030],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":67,"label":"backbone"}');
    var result = {};
    for (var i = 0, length = getLength(list); i < length; i++) {
      if (values) {
        result[list[i]] = values[i];
      } else {
        result[list[i][0]] = list[i][1];
      }
    }
    return result;
  };

  // Generator function to create the findIndex and findLastIndex functions
  function createPredicateIndexFinder(dir) {instrumentation_log('{"type":"FunctionDeclaration","bodyRange":[21152,21485],"range":[21111,21485],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":69,"label":"backbone"}');
    return function(array, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[21201,21480],"range":[21165,21480],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":68,"label":"backbone"}');
      predicate = cb(predicate, context);
      var length = getLength(array);
      var index = dir > 0 ? 0 : length - 1;
      for (; index >= 0 && index < length; index += dir) {
        if (predicate(array[index], index, array)) return index;
      }
      return -1;
    };
  }

  // Returns the first index on an array-like that passes a predicate test
  _.findIndex = createPredicateIndexFinder(1);
  _.findLastIndex = createPredicateIndexFinder(-1);

  // Use a comparator function to figure out the smallest index at which
  // an object should be inserted so as to maintain order. Uses binary search.
  _.sortedIndex = function(array, obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[21872,22156],"range":[21832,22156],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":70,"label":"backbone"}');
    iteratee = cb(iteratee, context, 1);
    var value = iteratee(obj);
    var low = 0, high = getLength(array);
    while (low < high) {
      var mid = Math.floor((low + high) / 2);
      if (iteratee(array[mid]) < value) low = mid + 1; else high = mid;
    }
    return low;
  };

  // Generator function to create the indexOf and lastIndexOf functions
  function createIndexFinder(dir, predicateFind, sortedIndex) {instrumentation_log('{"type":"FunctionDeclaration","bodyRange":[22293,23053],"range":[22233,23053],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":72,"label":"backbone"}');
    return function(array, item, idx) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[22333,23048],"range":[22306,23048],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":71,"label":"backbone"}');
      var i = 0, length = getLength(array);
      if (typeof idx == 'number') {
        if (dir > 0) {
            i = idx >= 0 ? idx : Math.max(idx + length, i);
        } else {
            length = idx >= 0 ? Math.min(idx + 1, length) : idx + length + 1;
        }
      } else if (sortedIndex && idx && length) {
        idx = sortedIndex(array, item);
        return array[idx] === item ? idx : -1;
      }
      if (item !== item) {
        idx = predicateFind(slice.call(array, i, length), _.isNaN);
        return idx >= 0 ? idx + i : -1;
      }
      for (idx = dir > 0 ? i : length - 1; idx >= 0 && idx < length; idx += dir) {
        if (array[idx] === item) return idx;
      }
      return -1;
    };
  }

  // Return the position of the first occurrence of an item in an array,
  // or -1 if the item is not included in the array.
  // If the array is large and already in sort order, pass `true`
  // for **isSorted** to use binary search.
  _.indexOf = createIndexFinder(1, _.findIndex, _.sortedIndex);
  _.lastIndexOf = createIndexFinder(-1, _.findLastIndex);

  // Generate an integer Array containing an arithmetic progression. A port of
  // the native Python `range()` function. See
  // [the Python documentation](http://docs.python.org/library/functions.html#range).
  _.range = function(start, stop, step) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[23666,23973],"range":[23638,23973],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":73,"label":"backbone"}');
    if (stop == null) {
      stop = start || 0;
      start = 0;
    }
    step = step || 1;

    var length = Math.max(Math.ceil((stop - start) / step), 0);
    var range = Array(length);

    for (var idx = 0; idx < length; idx++, start += step) {
      range[idx] = start;
    }

    return range;
  };

  // Function (ahem) Functions
  // ------------------

  // Determines whether to execute a function as a constructor
  // or a normal function with the provided arguments
  var executeBound = function(sourceFunc, boundFunc, context, callingContext, args) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[24233,24482],"range":[24170,24482],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":74,"label":"backbone"}');
    if (!(callingContext instanceof boundFunc)) return sourceFunc.apply(context, args);
    var self = baseCreate(sourceFunc.prototype);
    var result = sourceFunc.apply(self, args);
    if (_.isObject(result)) return result;
    return self;
  };

  // Create a function bound to a given object (assigning `this`, and arguments,
  // optionally). Delegates to **ECMAScript 5**'s native `Function.bind` if
  // available.
  _.bind = function(func, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[24693,25076],"range":[24669,25076],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":76,"label":"backbone"}');
    if (nativeBind && func.bind === nativeBind) return nativeBind.apply(func, slice.call(arguments, 1));
    if (!_.isFunction(func)) throw new TypeError('Bind must be called on a function');
    var args = slice.call(arguments, 2);
    var bound = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[24955,25053],"range":[24944,25053],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":75,"label":"backbone"}');
      return executeBound(func, bound, context, this, args.concat(slice.call(arguments)));
    };
    return bound;
  };

  // Partially apply a function by creating a version that has had some of its
  // arguments pre-filled, without changing its dynamic `this` context. _ acts
  // as a placeholder, allowing any combination of arguments to be pre-filled.
  _.partial = function(func) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[25345,25793],"range":[25330,25793],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":78,"label":"backbone"}');
    var boundArgs = slice.call(arguments, 1);
    var bound = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[25420,25770],"range":[25409,25770],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":77,"label":"backbone"}');
      var position = 0, length = boundArgs.length;
      var args = Array(length);
      for (var i = 0; i < length; i++) {
        args[i] = boundArgs[i] === _ ? arguments[position++] : boundArgs[i];
      }
      while (position < arguments.length) args.push(arguments[position++]);
      return executeBound(func, bound, this, this, args);
    };
    return bound;
  };

  // Bind a number of an object's methods to that object. Remaining arguments
  // are the method names to be bound. Useful for ensuring that all callbacks
  // defined on an object belong to it.
  _.bindAll = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[26020,26270],"range":[26006,26270],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":79,"label":"backbone"}');
    var i, length = arguments.length, key;
    if (length <= 1) throw new Error('bindAll must be passed function names');
    for (i = 1; i < length; i++) {
      key = arguments[i];
      obj[key] = _.bind(obj[key], obj);
    }
    return obj;
  };

  // Memoize an expensive function by storing its results.
  _.memoize = function(func, hasher) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[26369,26674],"range":[26346,26674],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":81,"label":"backbone"}');
    var memoize = function(key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[26403,26625],"range":[26389,26625],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":80,"label":"backbone"}');
      var cache = memoize.cache;
      var address = '' + (hasher ? hasher.apply(this, arguments) : key);
      if (!_.has(cache, address)) cache[address] = func.apply(this, arguments);
      return cache[address];
    };
    memoize.cache = {};
    return memoize;
  };

  // Delays a function for the given number of milliseconds, and then calls
  // it with the arguments supplied.
  _.delay = function(func, wait) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[26823,26954],"range":[26802,26954],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":83,"label":"backbone"}');
    var args = slice.call(arguments, 2);
    return setTimeout(function(){instrumentation_log('{"type":"FunctionExpression","bodyRange":[26898,26942],"range":[26888,26942],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":82,"label":"backbone"}');
      return func.apply(null, args);
    }, wait);
  };

  // Defers a function, scheduling it to run after the current call stack has
  // cleared.
  _.defer = _.partial(_.delay, _, 1);

  // Returns a function, that, when invoked, will only be triggered at most once
  // during a given window of time. Normally, the throttled function will run
  // as much as it can, without ever going more than once per `wait` duration;
  // but if you'd like to disable the execution on the leading edge, pass
  // `{leading: false}`. To disable execution on the trailing edge, ditto.
  _.throttle = function(func, wait, options) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[27520,28439],"range":[27490,28439],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":86,"label":"backbone"}');
    var context, args, result;
    var timeout = null;
    var previous = 0;
    if (!options) options = {};
    var later = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[27658,27830],"range":[27647,27830],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":84,"label":"backbone"}');
      previous = options.leading === false ? 0 : _.now();
      timeout = null;
      result = func.apply(context, args);
      if (!timeout) context = args = null;
    };
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[27854,28434],"range":[27843,28434],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":85,"label":"backbone"}');
      var now = _.now();
      if (!previous && options.leading === false) previous = now;
      var remaining = wait - (now - previous);
      context = this;
      args = arguments;
      if (remaining <= 0 || remaining > wait) {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
        previous = now;
        result = func.apply(context, args);
        if (!timeout) context = args = null;
      } else if (!timeout && options.trailing !== false) {
        timeout = setTimeout(later, remaining);
      }
      return result;
    };
  };

  // Returns a function, that, as long as it continues to be invoked, will not
  // be triggered. The function will be called after it stops being called for
  // N milliseconds. If `immediate` is passed, trigger the function on the
  // leading edge, instead of the trailing.
  _.debounce = function(func, wait, immediate) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[28766,29491],"range":[28734,29491],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":89,"label":"backbone"}');
    var timeout, args, context, timestamp, result;

    var later = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[28847,29157],"range":[28836,29157],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":87,"label":"backbone"}');
      var last = _.now() - timestamp;

      if (last < wait && last >= 0) {
        timeout = setTimeout(later, wait - last);
      } else {
        timeout = null;
        if (!immediate) {
          result = func.apply(context, args);
          if (!timeout) context = args = null;
        }
      }
    };

    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[29182,29486],"range":[29171,29486],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":88,"label":"backbone"}');
      context = this;
      args = arguments;
      timestamp = _.now();
      var callNow = immediate && !timeout;
      if (!timeout) timeout = setTimeout(later, wait);
      if (callNow) {
        result = func.apply(context, args);
        context = args = null;
      }

      return result;
    };
  };

  // Returns the first function passed as an argument to the second,
  // allowing you to adjust arguments, run code before and after, and
  // conditionally execute the original function.
  _.wrap = function(func, wrapper) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[29718,29760],"range":[29694,29760],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":90,"label":"backbone"}');
    return _.partial(wrapper, func);
  };

  // Returns a negated version of the passed-in predicate.
  _.negate = function(predicate) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[29855,29939],"range":[29835,29939],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":92,"label":"backbone"}');
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[29879,29934],"range":[29868,29934],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":91,"label":"backbone"}');
      return !predicate.apply(this, arguments);
    };
  };

  // Returns a function that is the composition of a list of functions, each
  // consuming the return value of the function that follows.
  _.compose = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30106,30353],"range":[30095,30353],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":94,"label":"backbone"}');
    var args = arguments;
    var start = args.length - 1;
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30189,30348],"range":[30178,30348],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":93,"label":"backbone"}');
      var i = start;
      var result = args[start].apply(this, arguments);
      while (i--) result = args[i].call(this, result);
      return result;
    };
  };

  // Returns a function that will only be executed on and after the Nth call.
  _.after = function(times, func) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30468,30581],"range":[30446,30581],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":96,"label":"backbone"}');
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30492,30576],"range":[30481,30576],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":95,"label":"backbone"}');
      if (--times < 1) {
        return func.apply(this, arguments);
      }
    };
  };

  // Returns a function that will only be executed up to (but not including) the Nth call.
  _.before = function(times, func) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30710,30891],"range":[30688,30891],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":98,"label":"backbone"}');
    var memo;
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[30748,30886],"range":[30737,30886],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":97,"label":"backbone"}');
      if (--times > 0) {
        memo = func.apply(this, arguments);
      }
      if (times <= 1) func = null;
      return memo;
    };
  };

  // Returns a function that will be executed at most one time, no matter how
  // often you call it. Useful for lazy initialization.
  _.once = _.partial(_.before, 2);

  // Object Functions
  // ----------------

  // Keys in IE < 9 that won't be iterated by `for key in ...` and thus missed.
  var hasEnumBug = !{toString: null}.propertyIsEnumerable('toString');
  var nonEnumerableProps = ['valueOf', 'isPrototypeOf', 'toString',
                      'propertyIsEnumerable', 'hasOwnProperty', 'toLocaleString'];

  function collectNonEnumProps(obj, keys) {instrumentation_log('{"type":"FunctionDeclaration","bodyRange":[31454,31960],"range":[31414,31960],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":99,"label":"backbone"}');
    var nonEnumIdx = nonEnumerableProps.length;
    var constructor = obj.constructor;
    var proto = (_.isFunction(constructor) && constructor.prototype) || ObjProto;

    // Constructor is a special case.
    var prop = 'constructor';
    if (_.has(obj, prop) && !_.contains(keys, prop)) keys.push(prop);

    while (nonEnumIdx--) {
      prop = nonEnumerableProps[nonEnumIdx];
      if (prop in obj && obj[prop] !== proto[prop] && !_.contains(keys, prop)) {
        keys.push(prop);
      }
    }
  }

  // Retrieve the names of an object's own properties.
  // Delegates to **ECMAScript 5**'s native `Object.keys`
  _.keys = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[32100,32357],"range":[32086,32357],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":100,"label":"backbone"}');
    if (!_.isObject(obj)) return [];
    if (nativeKeys) return nativeKeys(obj);
    var keys = [];
    for (var key in obj) if (_.has(obj, key)) keys.push(key);
    // Ahem, IE < 9.
    if (hasEnumBug) collectNonEnumProps(obj, keys);
    return keys;
  };

  // Retrieve all the property names of an object.
  _.allKeys = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[32439,32631],"range":[32425,32631],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":101,"label":"backbone"}');
    if (!_.isObject(obj)) return [];
    var keys = [];
    for (var key in obj) keys.push(key);
    // Ahem, IE < 9.
    if (hasEnumBug) collectNonEnumProps(obj, keys);
    return keys;
  };

  // Retrieve the values of an object's properties.
  _.values = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[32713,32904],"range":[32699,32904],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":102,"label":"backbone"}');
    var keys = _.keys(obj);
    var length = keys.length;
    var values = Array(length);
    for (var i = 0; i < length; i++) {
      values[i] = obj[keys[i]];
    }
    return values;
  };

  // Returns the results of applying the iteratee to each element of the object
  // In contrast to _.map it returns an object
  _.mapObject = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[33083,33424],"range":[33050,33424],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":103,"label":"backbone"}');
    iteratee = cb(iteratee, context);
    var keys =  _.keys(obj),
          length = keys.length,
          results = {},
          currentKey;
      for (var index = 0; index < length; index++) {
        currentKey = keys[index];
        results[currentKey] = iteratee(obj[currentKey], currentKey, obj);
      }
      return results;
  };

  // Convert an object into a list of `[key, value]` pairs.
  _.pairs = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[33513,33712],"range":[33499,33712],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":104,"label":"backbone"}');
    var keys = _.keys(obj);
    var length = keys.length;
    var pairs = Array(length);
    for (var i = 0; i < length; i++) {
      pairs[i] = [keys[i], obj[keys[i]]];
    }
    return pairs;
  };

  // Invert the keys and values of an object. The values must be serializable.
  _.invert = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[33821,33999],"range":[33807,33999],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":105,"label":"backbone"}');
    var result = {};
    var keys = _.keys(obj);
    for (var i = 0, length = keys.length; i < length; i++) {
      result[obj[keys[i]]] = keys[i];
    }
    return result;
  };

  // Return a sorted list of the function names available on the object.
  // Aliased as `methods`
  _.functions = _.methods = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[34143,34277],"range":[34129,34277],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":106,"label":"backbone"}');
    var names = [];
    for (var key in obj) {
      if (_.isFunction(obj[key])) names.push(key);
    }
    return names.sort();
  };

  // Extend a given object with all the properties in passed-in object(s).
  _.extend = createAssigner(_.allKeys);

  // Assigns a given object with all the own properties in the passed-in object(s)
  // (https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object/assign)
  _.extendOwn = _.assign = createAssigner(_.keys);

  // Returns the first key on an object that passes a predicate test
  _.findKey = function(obj, predicate, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[34744,34963],"range":[34710,34963],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":107,"label":"backbone"}');
    predicate = cb(predicate, context);
    var keys = _.keys(obj), key;
    for (var i = 0, length = keys.length; i < length; i++) {
      key = keys[i];
      if (predicate(obj[key], key, obj)) return key;
    }
  };

  // Return a copy of the object only containing the whitelisted properties.
  _.pick = function(object, oiteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[35091,35654],"range":[35054,35654],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":109,"label":"backbone"}');
    var result = {}, obj = object, iteratee, keys;
    if (obj == null) return result;
    if (_.isFunction(oiteratee)) {
      keys = _.allKeys(obj);
      iteratee = optimizeCb(oiteratee, context);
    } else {
      keys = flatten(arguments, false, false, 1);
      iteratee = function(value, key, obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[35399,35421],"range":[35373,35421],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":108,"label":"backbone"}'); return key in obj; };
      obj = Object(obj);
    }
    for (var i = 0, length = keys.length; i < length; i++) {
      var key = keys[i];
      var value = obj[key];
      if (iteratee(value, key, obj)) result[key] = value;
    }
    return result;
  };

   // Return a copy of the object without the blacklisted properties.
  _.omit = function(obj, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[35771,36066],"range":[35738,36066],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":111,"label":"backbone"}');
    if (_.isFunction(iteratee)) {
      iteratee = _.negate(iteratee);
    } else {
      var keys = _.map(flatten(arguments, false, false, 1), String);
      iteratee = function(value, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[35964,36012],"range":[35943,36012],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":110,"label":"backbone"}');
        return !_.contains(keys, key);
      };
    }
    return _.pick(obj, iteratee, context);
  };

  // Fill in a given object with default properties.
  _.defaults = createAssigner(_.allKeys, true);

  // Creates an object that inherits from the given prototype object.
  // If additional properties are provided then they will be added to the
  // created object.
  _.create = function(prototype, props) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[36376,36483],"range":[36349,36483],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":112,"label":"backbone"}');
    var result = baseCreate(prototype);
    if (props) _.extendOwn(result, props);
    return result;
  };

  // Create a (shallow-cloned) duplicate of an object.
  _.clone = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[36567,36671],"range":[36553,36671],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":113,"label":"backbone"}');
    if (!_.isObject(obj)) return obj;
    return _.isArray(obj) ? obj.slice() : _.extend({}, obj);
  };

  // Invokes interceptor with the obj, and then returns obj.
  // The primary purpose of this method is to "tap into" a method chain, in
  // order to perform operations on intermediate results within the chain.
  _.tap = function(obj, interceptor) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[36923,36966],"range":[36896,36966],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":114,"label":"backbone"}');
    interceptor(obj);
    return obj;
  };

  // Returns whether an object has a given set of `key:value` pairs.
  _.isMatch = function(object, attrs) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[37076,37356],"range":[37052,37356],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":115,"label":"backbone"}');
    var keys = _.keys(attrs), length = keys.length;
    if (object == null) return !length;
    var obj = Object(object);
    for (var i = 0; i < length; i++) {
      var key = keys[i];
      if (attrs[key] !== obj[key] || !(key in obj)) return false;
    }
    return true;
  };


  // Internal recursive comparison function for `isEqual`.
  var eq = function(a, b, aStack, bStack) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[37461,41337],"range":[37430,41337],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":116,"label":"backbone"}');
    // Identical objects are equal. `0 === -0`, but they aren't identical.
    // See the [Harmony `egal` proposal](http://wiki.ecmascript.org/doku.php?id=harmony:egal).
    if (a === b) return a !== 0 || 1 / a === 1 / b;
    // A strict comparison is necessary because `null == undefined`.
    if (a == null || b == null) return a === b;
    // Unwrap any wrapped objects.
    if (a instanceof _) a = a._wrapped;
    if (b instanceof _) b = b._wrapped;
    // Compare `[[Class]]` names.
    var className = toString.call(a);
    if (className !== toString.call(b)) return false;
    switch (className) {
      // Strings, numbers, regular expressions, dates, and booleans are compared by value.
      case '[object RegExp]':
      // RegExps are coerced to strings for comparison (Note: '' + /a/i === '/a/i')
      case '[object String]':
        // Primitives and their corresponding object wrappers are equivalent; thus, `"5"` is
        // equivalent to `new String("5")`.
        return '' + a === '' + b;
      case '[object Number]':
        // `NaN`s are equivalent, but non-reflexive.
        // Object(NaN) is equivalent to NaN
        if (+a !== +a) return +b !== +b;
        // An `egal` comparison is performed for other numeric values.
        return +a === 0 ? 1 / +a === 1 / b : +a === +b;
      case '[object Date]':
      case '[object Boolean]':
        // Coerce dates and booleans to numeric primitive values. Dates are compared by their
        // millisecond representations. Note that invalid dates with millisecond representations
        // of `NaN` are not equivalent.
        return +a === +b;
    }

    var areArrays = className === '[object Array]';
    if (!areArrays) {
      if (typeof a != 'object' || typeof b != 'object') return false;

      // Objects with different constructors are not equivalent, but `Object`s or `Array`s
      // from different frames are.
      var aCtor = a.constructor, bCtor = b.constructor;
      if (aCtor !== bCtor && !(_.isFunction(aCtor) && aCtor instanceof aCtor &&
                               _.isFunction(bCtor) && bCtor instanceof bCtor)
                          && ('constructor' in a && 'constructor' in b)) {
        return false;
      }
    }
    // Assume equality for cyclic structures. The algorithm for detecting cyclic
    // structures is adapted from ES 5.1 section 15.12.3, abstract operation `JO`.

    // Initializing stack of traversed objects.
    // It's done here since we only need them for objects and arrays comparison.
    aStack = aStack || [];
    bStack = bStack || [];
    var length = aStack.length;
    while (length--) {
      // Linear search. Performance is inversely proportional to the number of
      // unique nested structures.
      if (aStack[length] === a) return bStack[length] === b;
    }

    // Add the first object to the stack of traversed objects.
    aStack.push(a);
    bStack.push(b);

    // Recursively compare objects and arrays.
    if (areArrays) {
      // Compare array lengths to determine if a deep comparison is necessary.
      length = a.length;
      if (length !== b.length) return false;
      // Deep compare the contents, ignoring non-numeric properties.
      while (length--) {
        if (!eq(a[length], b[length], aStack, bStack)) return false;
      }
    } else {
      // Deep compare objects.
      var keys = _.keys(a), key;
      length = keys.length;
      // Ensure that both objects contain the same number of properties before comparing deep equality.
      if (_.keys(b).length !== length) return false;
      while (length--) {
        // Deep compare each member
        key = keys[length];
        if (!(_.has(b, key) && eq(a[key], b[key], aStack, bStack))) return false;
      }
    }
    // Remove the first object from the stack of traversed objects.
    aStack.pop();
    bStack.pop();
    return true;
  };

  // Perform a deep comparison to check if two objects are equal.
  _.isEqual = function(a, b) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[41435,41461],"range":[41420,41461],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":117,"label":"backbone"}');
    return eq(a, b);
  };

  // Is a given array, string, or object empty?
  // An "empty" object has no enumerable own-properties.
  _.isEmpty = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[41597,41785],"range":[41583,41785],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":118,"label":"backbone"}');
    if (obj == null) return true;
    if (isArrayLike(obj) && (_.isArray(obj) || _.isString(obj) || _.isArguments(obj))) return obj.length === 0;
    return _.keys(obj).length === 0;
  };

  // Is a given value a DOM element?
  _.isElement = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[41855,41902],"range":[41841,41902],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":119,"label":"backbone"}');
    return !!(obj && obj.nodeType === 1);
  };

  // Is a given value an array?
  // Delegates to ECMA5's native Array.isArray
  _.isArray = nativeIsArray || function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[42029,42086],"range":[42015,42086],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":120,"label":"backbone"}');
    return toString.call(obj) === '[object Array]';
  };

  // Is a given variable an object?
  _.isObject = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[42154,42248],"range":[42140,42248],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":121,"label":"backbone"}');
    var type = typeof obj;
    return type === 'function' || type === 'object' && !!obj;
  };

  // Add some isType methods: isArguments, isFunction, isString, isNumber, isDate, isRegExp, isError.
  _.each(['Arguments', 'Function', 'String', 'Number', 'Date', 'RegExp', 'Error'], function(name) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[42451,42561],"range":[42436,42561],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":123,"label":"backbone"}');
    _['is' + name] = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[42488,42556],"range":[42474,42556],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":122,"label":"backbone"}');
      return toString.call(obj) === '[object ' + name + ']';
    };
  });

  // Define a fallback version of the method in browsers (ahem, IE < 9), where
  // there isn't any inspectable "Arguments" type.
  if (!_.isArguments(arguments)) {
    _.isArguments = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[42764,42806],"range":[42750,42806],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":124,"label":"backbone"}');
      return _.has(obj, 'callee');
    };
  }

  // Optimize `isFunction` if appropriate. Work around some typeof bugs in old v8,
  // IE 11 (#1621), and in Safari 8 (#1929).
  if (typeof /./ != 'function' && typeof Int8Array != 'object') {
    _.isFunction = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43040,43095],"range":[43026,43095],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":125,"label":"backbone"}');
      return typeof obj == 'function' || false;
    };
  }

  // Is a given object a finite number?
  _.isFinite = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43171,43229],"range":[43157,43229],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":126,"label":"backbone"}');
    return isFinite(obj) && !isNaN(parseFloat(obj));
  };

  // Is the given value `NaN`? (NaN is the only number which does not equal itself).
  _.isNaN = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43343,43392],"range":[43329,43392],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":127,"label":"backbone"}');
    return _.isNumber(obj) && obj !== +obj;
  };

  // Is a given value a boolean?
  _.isBoolean = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43458,43550],"range":[43444,43550],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":128,"label":"backbone"}');
    return obj === true || obj === false || toString.call(obj) === '[object Boolean]';
  };

  // Is a given value equal to null?
  _.isNull = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43617,43647],"range":[43603,43647],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":129,"label":"backbone"}');
    return obj === null;
  };

  // Is a given variable undefined?
  _.isUndefined = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43718,43750],"range":[43704,43750],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":130,"label":"backbone"}');
    return obj === void 0;
  };

  // Shortcut function for checking if an object has a given property directly
  // on itself (in other words, not on a prototype).
  _.has = function(obj, key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[43914,43976],"range":[43895,43976],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":131,"label":"backbone"}');
    return obj != null && hasOwnProperty.call(obj, key);
  };

  // Utility Functions
  // -----------------

  // Run Underscore.js in *noConflict* mode, returning the `_` variable to its
  // previous owner. Returns a reference to the Underscore object.
  _.noConflict = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44200,44255],"range":[44189,44255],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":132,"label":"backbone"}');
    root._ = previousUnderscore;
    return this;
  };

  // Keep the identity function around for default iteratees.
  _.identity = function(value) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44351,44374],"range":[44335,44374],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":133,"label":"backbone"}');
    return value;
  };

  // Predicate-generating functions. Often useful outside of Underscore.
  _.constant = function(value) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44481,44537],"range":[44465,44537],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":135,"label":"backbone"}');
    return function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44505,44532],"range":[44494,44532],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":134,"label":"backbone"}');
      return value;
    };
  };

  _.noop = function(){instrumentation_log('{"type":"FunctionExpression","bodyRange":[44561,44563],"range":[44551,44563],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":136,"label":"backbone"}');};

  _.property = property;

  // Generates a function for a given object that returns a given property.
  _.propertyOf = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44699,44790],"range":[44685,44790],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":139,"label":"backbone"}');
    return obj == null ? function(){instrumentation_log('{"type":"FunctionExpression","bodyRange":[44736,44738],"range":[44726,44738],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":137,"label":"backbone"}');} : function(key) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44755,44785],"range":[44741,44785],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":138,"label":"backbone"}');
      return obj[key];
    };
  };

  // Returns a predicate for checking whether an object has a given set of
  // `key:value` pairs.
  _.matcher = _.matches = function(attrs) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44934,45045],"range":[44918,45045],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":141,"label":"backbone"}');
    attrs = _.extendOwn({}, attrs);
    return function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[44997,45040],"range":[44983,45040],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":140,"label":"backbone"}');
      return _.isMatch(obj, attrs);
    };
  };

  // Run a function **n** times.
  _.times = function(n, iteratee, context) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[45124,45291],"range":[45093,45291],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":142,"label":"backbone"}');
    var accum = Array(Math.max(0, n));
    iteratee = optimizeCb(iteratee, context, 1);
    for (var i = 0; i < n; i++) accum[i] = iteratee(i);
    return accum;
  };

  // Return a random integer between min and max (inclusive).
  _.random = function(min, max) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[45388,45516],"range":[45369,45516],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":143,"label":"backbone"}');
    if (max == null) {
      max = min;
      min = 0;
    }
    return min + Math.floor(Math.random() * (max - min + 1));
  };

  // A (possibly faster) way to get the current timestamp as an integer.
  _.now = Date.now || function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[45625,45663],"range":[45614,45663],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":144,"label":"backbone"}');
    return new Date().getTime();
  };

   // List of HTML entities for escaping.
  var escapeMap = {
    '&': '&amp;',
    '<': '&lt;',
    '>': '&gt;',
    '"': '&quot;',
    "'": '&#x27;',
    '`': '&#x60;'
  };
  var unescapeMap = _.invert(escapeMap);

  // Functions for escaping and unescaping strings to/from HTML interpolation.
  var createEscaper = function(map) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[45998,46444],"range":[45984,46444],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":147,"label":"backbone"}');
    var escaper = function(match) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[46034,46066],"range":[46018,46066],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":145,"label":"backbone"}');
      return map[match];
    };
    // Regexes for identifying a key that needs to be escaped
    var source = '(?:' + _.keys(map).join('|') + ')';
    var testRegexp = RegExp(source);
    var replaceRegexp = RegExp(source, 'g');
    return function(string) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[46294,46439],"range":[46277,46439],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":146,"label":"backbone"}');
      string = string == null ? '' : '' + string;
      return testRegexp.test(string) ? string.replace(replaceRegexp, escaper) : string;
    };
  };
  _.escape = createEscaper(escapeMap);
  _.unescape = createEscaper(unescapeMap);

  // If the value of the named `property` is a function then invoke it with the
  // `object` as context; otherwise, return it.
  _.result = function(object, property, fallback) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[46707,46891],"range":[46670,46891],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":148,"label":"backbone"}');
    var value = object == null ? void 0 : object[property];
    if (value === void 0) {
      value = fallback;
    }
    return _.isFunction(value) ? value.call(object) : value;
  };

  // Generate a unique integer id (unique within the entire client session).
  // Useful for temporary DOM ids.
  var idCounter = 0;
  _.uniqueId = function(prefix) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[47059,47133],"range":[47042,47133],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":149,"label":"backbone"}');
    var id = ++idCounter + '';
    return prefix ? prefix + id : id;
  };

  // By default, Underscore uses ERB-style template delimiters, change the
  // following template settings to use alternative delimiters.
  _.templateSettings = {
    evaluate    : /<%([\s\S]+?)%>/g,
    interpolate : /<%=([\s\S]+?)%>/g,
    escape      : /<%-([\s\S]+?)%>/g
  };

  // When customizing `templateSettings`, if you don't want to define an
  // interpolation, evaluation or escaping regex, we need one that is
  // guaranteed not to match.
  var noMatch = /(.)^/;

  // Certain characters need to be escaped so that they can be put into a
  // string literal.
  var escapes = {
    "'":      "'",
    '\\':     '\\',
    '\r':     'r',
    '\n':     'n',
    '\u2028': 'u2028',
    '\u2029': 'u2029'
  };

  var escaper = /\\|'|\r|\n|\u2028|\u2029/g;

  var escapeChar = function(match) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[47938,47977],"range":[47922,47977],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":150,"label":"backbone"}');
    return '\\' + escapes[match];
  };

  // JavaScript micro-templating, similar to John Resig's implementation.
  // Underscore templating handles arbitrary delimiters, preserves whitespace,
  // and correctly escapes quotes within interpolated code.
  // NB: `oldSettings` only exists for backwards compatibility.
  _.template = function(text, settings, oldSettings) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[48310,50172],"range":[48272,50172],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":153,"label":"backbone"}');
    if (!settings && oldSettings) settings = oldSettings;
    settings = _.defaults({}, settings, _.templateSettings);

    // Combine delimiters into one regular expression via alternation.
    var matcher = RegExp([
      (settings.escape || noMatch).source,
      (settings.interpolate || noMatch).source,
      (settings.evaluate || noMatch).source
    ].join('|') + '|$', 'g');

    // Compile the template source, escaping string literals appropriately.
    var index = 0;
    var source = "__p+='";
    text.replace(matcher, function(match, escape, interpolate, evaluate, offset) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[48899,49399],"range":[48844,49399],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":151,"label":"backbone"}');
      source += text.slice(index, offset).replace(escaper, escapeChar);
      index = offset + match.length;

      if (escape) {
        source += "'+\n((__t=(" + escape + "))==null?'':_.escape(__t))+\n'";
      } else if (interpolate) {
        source += "'+\n((__t=(" + interpolate + "))==null?'':__t)+\n'";
      } else if (evaluate) {
        source += "';\n" + evaluate + "\n__p+='";
      }

      // Adobe VMs need the match returned to produce the correct offest.
      return match;
    });
    source += "';\n";

    // If a variable is not specified, place data values in local scope.
    if (!settings.variable) source = 'with(obj||{}){\n' + source + '}\n';

    source = "var __t,__p='',__j=Array.prototype.join," +
      "print=function(){__p+=__j.call(arguments,'');};\n" +
      source + 'return __p;\n';

    try {
      var render = new Function(settings.variable || 'obj', '_', source);
    } catch (e) {
      e.source = source;
      throw e;
    }

    var template = function(data) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[49907,49955],"range":[49892,49955],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":152,"label":"backbone"}');
      return render.call(this, data, _);
    };

    // Provide the compiled source as a convenience for precompilation.
    var argument = settings.variable || 'obj';
    template.source = 'function(' + argument + '){\n' + source + '}';

    return template;
  };

  // Add a "chain" function. Start chaining a wrapped Underscore object.
  _.chain = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[50274,50355],"range":[50260,50355],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":154,"label":"backbone"}');
    var instance = _(obj);
    instance._chain = true;
    return instance;
  };

  // OOP
  // ---------------
  // If Underscore is called as a function, it returns a wrapped object that
  // can be used OO-style. This wrapper holds altered versions of all the
  // underscore functions. Wrapped objects may be chained.

  // Helper function to continue chaining intermediate results.
  var result = function(instance, obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[50702,50758],"range":[50678,50758],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":155,"label":"backbone"}');
    return instance._chain ? _(obj).chain() : obj;
  };

  // Add your own custom functions to the Underscore object.
  _.mixin = function(obj) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[50848,51116],"range":[50834,51116],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":158,"label":"backbone"}');
    _.each(_.functions(obj), function(name) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[50894,51110],"range":[50879,51110],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":157,"label":"backbone"}');
      var func = _[name] = obj[name];
      _.prototype[name] = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[50971,51103],"range":[50960,51103],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":156,"label":"backbone"}');
        var args = [this._wrapped];
        push.apply(args, arguments);
        return result(this, func.apply(_, args));
      };
    });
  };

  // Add all of the Underscore functions to the wrapper object.
  _.mixin(_);

  // Add all mutator Array functions to the wrapper.
  _.each(['pop', 'push', 'reverse', 'shift', 'sort', 'splice', 'unshift'], function(name) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[51341,51610],"range":[51326,51610],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":160,"label":"backbone"}');
    var method = ArrayProto[name];
    _.prototype[name] = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[51413,51605],"range":[51402,51605],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":159,"label":"backbone"}');
      var obj = this._wrapped;
      method.apply(obj, arguments);
      if ((name === 'shift' || name === 'splice') && obj.length === 0) delete obj[0];
      return result(this, obj);
    };
  });

  // Add all accessor Array functions to the wrapper.
  _.each(['concat', 'join', 'slice'], function(name) {instrumentation_log('{"type":"FunctionExpression","bodyRange":[51721,51872],"range":[51706,51872],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":162,"label":"backbone"}');
    var method = ArrayProto[name];
    _.prototype[name] = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[51793,51867],"range":[51782,51867],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":161,"label":"backbone"}');
      return result(this, method.apply(this._wrapped, arguments));
    };
  });

  // Extracts the result from a wrapped and chained object.
  _.prototype.value = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[51969,52000],"range":[51958,52000],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":163,"label":"backbone"}');
    return this._wrapped;
  };

  // Provide unwrapping proxy for some methods used in engine operations
  // such as arithmetic and JSON stringification.
  _.prototype.valueOf = _.prototype.toJSON = _.prototype.value;

  _.prototype.toString = function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[52227,52263],"range":[52216,52263],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":164,"label":"backbone"}');
    return '' + this._wrapped;
  };

  // AMD registration happens at the end for compatibility with AMD loaders
  // that may not enforce next-turn semantics on modules. Even though general
  // practice for AMD registration is to be anonymous, underscore registers
  // as a named module because, like jQuery, it is a base library that is
  // popular enough to be bundled in a third party lib, but not be part of
  // an AMD load request. Those cases could generate an error when an
  // anonymous define() is called outside of a loader request.
  if (typeof define === 'function' && define.amd) {
    define('underscore', [], function() {instrumentation_log('{"type":"FunctionExpression","bodyRange":[52870,52893],"range":[52859,52893],"file":"todomvc/examples.lacunized.instrumented/backbone/node_modules/underscore/underscore.js","index":165,"label":"backbone"}');
      return _;
    });
  }
}.call(this));
