// LACUNA LAZY LOAD FALLBACK
function lacuna_lazy_load(id, callback){
    fetch("http://127.0.0.1:8125/lazyload/", {
        method: "POST",
        headers: { "Accept": "application/json", "Content-Type": "application/json" },
        body: JSON.stringify({id})
    }).then(response => {
        return response.text();
    }).then(callback);
}
// Copyright (c) Microsoft Open Technologies, Inc. All rights reserved. See License.txt in the project root for license information.

;(function (undefined) {

  var objectTypes = {
    'boolean': false,
    'function': true,
    'object': true,
    'number': false,
    'string': false,
    'undefined': false
  };

  var root = (objectTypes[typeof window] && window) || this,
    freeExports = objectTypes[typeof exports] && exports && !exports.nodeType && exports,
    freeModule = objectTypes[typeof module] && module && !module.nodeType && module,
    moduleExports = freeModule && freeModule.exports === freeExports && freeExports,
    freeGlobal = objectTypes[typeof global] && global;

  if (freeGlobal && (freeGlobal.global === freeGlobal || freeGlobal.window === freeGlobal)) {
    root = freeGlobal;
  }

  var Rx = {
      internals: {},
      config: {
        Promise: root.Promise
      },
      helpers: { }
  };

  // Defaults
  var noop = Rx.helpers.noop = function () { },
    notDefined = Rx.helpers.notDefined = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1046:1082]", functionData => eval(functionData))},
    identity = Rx.helpers.identity = function (x) { return x; },
    pluck = Rx.helpers.pluck = function (property) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1200:1248]", functionData => eval(functionData))},
    just = Rx.helpers.just = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1296:1337]", functionData => eval(functionData))},
    defaultNow = Rx.helpers.defaultNow = Date.now,
    defaultComparer = Rx.helpers.defaultComparer = function (x, y) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1457:1482]", functionData => eval(functionData))},
    defaultSubComparer = Rx.helpers.defaultSubComparer = function (x, y) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1557:1597]", functionData => eval(functionData))},
    defaultKeySerializer = Rx.helpers.defaultKeySerializer = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1673:1697]", functionData => eval(functionData))},
    defaultError = Rx.helpers.defaultError = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1759:1773]", functionData => eval(functionData))},
    isPromise = Rx.helpers.isPromise = function (p) { return !!p && typeof p.subscribe !== 'function' && typeof p.then === 'function'; },
    asArray = Rx.helpers.asArray = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[1960:2009]", functionData => eval(functionData))},
    not = Rx.helpers.not = function (a) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[2051:2065]", functionData => eval(functionData))},
    isFunction = Rx.helpers.isFunction = (function () {

      var isFn = function (value) {
        return typeof value == 'function' || false;
      }

      // fallback for older versions of Chrome and Safari
      if (isFn(/x/)) {
        isFn = function(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[2333:2436]", functionData => eval(functionData))};
      }

      return isFn;
    }());

  function cloneArray(arr) { for(var a = [], i = 0, len = arr.length; i < len; i++) { a.push(arr[i]); } return a;}

  Rx.config.longStackSupport = false;
  var hasStacks = false;
  try {
    throw new Error();
  } catch (e) {
    hasStacks = !!e.stack;
  }

  // All code after this point will be filtered from stack traces reported by RxJS
  var rStartingLine = captureLine(), rFileName;

  var STACK_JUMP_SEPARATOR = "From previous event:";

  function makeStackTraceLong(error, observable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[2970:3684]", functionData => eval(functionData))}

  function filterStackString(stackString) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[3728:4034]", functionData => eval(functionData))}

  function isInternalFrame(stackLine) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[4074:4400]", functionData => eval(functionData))}

  function isNodeFrame(stackLine) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[4436:4540]", functionData => eval(functionData))}

  function captureLine() {
    if (!hasStacks) { return; }

    try {
      throw new Error();
    } catch (e) {
      var lines = e.stack.split("\n");
      var firstLine = lines[0].indexOf("@") > 0 ? lines[1] : lines[2];
      var fileNameAndLineNumber = getFileNameAndLineNumber(firstLine);
      if (!fileNameAndLineNumber) { return; }

      rFileName = fileNameAndLineNumber[0];
      return fileNameAndLineNumber[1];
    }
  }

  function getFileNameAndLineNumber(stackLine) {
    // Named functions: "at functionName (filename:lineNumber:columnNumber)"
    var attempt1 = /at .+ \((.+):(\d+):(?:\d+)\)$/.exec(stackLine);
    if (attempt1) { return [attempt1[1], Number(attempt1[2])]; }

    // Anonymous functions: "at filename:lineNumber:columnNumber"
    var attempt2 = /at ([^ ]+):(\d+):(?:\d+)$/.exec(stackLine);
    if (attempt2) { return [attempt2[1], Number(attempt2[2])]; }

    // Firefox style: "function@filename:lineNumber or @filename:lineNumber"
    var attempt3 = /.*@(.+):(\d+)$/.exec(stackLine);
    if (attempt3) { return [attempt3[1], Number(attempt3[2])]; }
  }

  var EmptyError = Rx.EmptyError = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[5679:5759]", functionData => eval(functionData))};
  EmptyError.prototype = Error.prototype;

  var ObjectDisposedError = Rx.ObjectDisposedError = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[5868:5942]", functionData => eval(functionData))};
  ObjectDisposedError.prototype = Error.prototype;

  var ArgumentOutOfRangeError = Rx.ArgumentOutOfRangeError = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[6069:6140]", functionData => eval(functionData))};
  ArgumentOutOfRangeError.prototype = Error.prototype;

  var NotSupportedError = Rx.NotSupportedError = function (message) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[6266:6358]", functionData => eval(functionData))};
  NotSupportedError.prototype = Error.prototype;

  var NotImplementedError = Rx.NotImplementedError = function (message) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[6482:6576]", functionData => eval(functionData))};
  NotImplementedError.prototype = Error.prototype;

  var notImplemented = Rx.helpers.notImplemented = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[6693:6735]", functionData => eval(functionData))};

  var notSupported = Rx.helpers.notSupported = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[6797:6837]", functionData => eval(functionData))};

  // Shim in iterator support
  var $iterator$ = (typeof Symbol === 'function' && Symbol.iterator) ||
    '_es6shim_iterator_';
  // Bug for mozilla version
  if (root.Set && typeof new root.Set()['@@iterator'] === 'function') {
    $iterator$ = '@@iterator';
  }

  var doneEnumerator = Rx.doneEnumerator = { done: true, value: undefined };

  var isIterable = Rx.helpers.isIterable = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7239:7284]", functionData => eval(functionData))}

  var isArrayLike = Rx.helpers.isArrayLike = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7344:7389]", functionData => eval(functionData))}

  Rx.helpers.iterator = $iterator$;

  var bindCallback = Rx.internals.bindCallback = function (func, thisArg, argCount) {
    if (typeof thisArg === 'undefined') { return func; }
    switch(argCount) {
      case 0:
        return function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7634:7681]", functionData => eval(functionData))};
      case 1:
        return function(arg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7726:7779]", functionData => eval(functionData))}
      case 2:
        return function(value, index) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7832:7894]", functionData => eval(functionData))};
      case 3:
        return function(value, index, collection) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[7960:8034]", functionData => eval(functionData))};
    }

    return function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[8065:8117]", functionData => eval(functionData))};
  };

  /** Used to determine if values are of the language type Object */
  var dontEnums = ['toString',
    'toLocaleString',
    'valueOf',
    'hasOwnProperty',
    'isPrototypeOf',
    'propertyIsEnumerable',
    'constructor'],
  dontEnumsLength = dontEnums.length;

  /** `Object#toString` result shortcuts */
  var argsClass = '[object Arguments]',
    arrayClass = '[object Array]',
    boolClass = '[object Boolean]',
    dateClass = '[object Date]',
    errorClass = '[object Error]',
    funcClass = '[object Function]',
    numberClass = '[object Number]',
    objectClass = '[object Object]',
    regexpClass = '[object RegExp]',
    stringClass = '[object String]';

  var toString = Object.prototype.toString,
    hasOwnProperty = Object.prototype.hasOwnProperty,
    supportsArgsClass = toString.call(arguments) == argsClass, // For less <IE9 && FF<4
    supportNodeClass,
    errorProto = Error.prototype,
    objectProto = Object.prototype,
    stringProto = String.prototype,
    propertyIsEnumerable = objectProto.propertyIsEnumerable;

  try {
    supportNodeClass = !(toString.call(document) == objectClass && !({ 'toString': 0 } + ''));
  } catch (e) {
    supportNodeClass = true;
  }

  var nonEnumProps = {};
  nonEnumProps[arrayClass] = nonEnumProps[dateClass] = nonEnumProps[numberClass] = { 'constructor': true, 'toLocaleString': true, 'toString': true, 'valueOf': true };
  nonEnumProps[boolClass] = nonEnumProps[stringClass] = { 'constructor': true, 'toString': true, 'valueOf': true };
  nonEnumProps[errorClass] = nonEnumProps[funcClass] = nonEnumProps[regexpClass] = { 'constructor': true, 'toString': true };
  nonEnumProps[objectClass] = { 'constructor': true };

  var support = {};
  (function () {
    var ctor = function() { this.x = 1; },
      props = [];

    ctor.prototype = { 'valueOf': 1, 'y': 1 };
    for (var key in new ctor) { props.push(key); }
    for (key in arguments) { }

    // Detect if `name` or `message` properties of `Error.prototype` are enumerable by default.
    support.enumErrorProps = propertyIsEnumerable.call(errorProto, 'message') || propertyIsEnumerable.call(errorProto, 'name');

    // Detect if `prototype` properties are enumerable by default.
    support.enumPrototypes = propertyIsEnumerable.call(ctor, 'prototype');

    // Detect if `arguments` object indexes are non-enumerable
    support.nonEnumArgs = key != 0;

    // Detect if properties shadowing those on `Object.prototype` are non-enumerable.
    support.nonEnumShadows = !/valueOf/.test(props);
  }(1));

  var isObject = Rx.internals.isObject = function(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[10723:10828]", functionData => eval(functionData))};

  function keysIn(object) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[10857:12016]", functionData => eval(functionData))}

  function internalFor(object, callback, keysFunc) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[12069:12323]", functionData => eval(functionData))}

  function internalForIn(object, callback) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[12368:12423]", functionData => eval(functionData))}

  function isNode(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[12450:12699]", functionData => eval(functionData))}

  var isArguments = function(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[12737:12834]", functionData => eval(functionData))}

  // fallback for browsers that can't detect `arguments` objects by [[Class]]
  if (!supportsArgsClass) {
    isArguments = function(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[12976:13080]", functionData => eval(functionData))};
  }

  var isEqual = Rx.internals.isEqual = function (x, y) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[13142:13184]", functionData => eval(functionData))};

  /** @private
   * Used for deep comparison
   **/
  function deepEquals(a, b, stackA, stackB) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[13283:17614]", functionData => eval(functionData))}

  var hasProp = {}.hasOwnProperty,
      slice = Array.prototype.slice;

  var inherits = this.inherits = Rx.internals.inherits = function (child, parent) {
    function __() { this.constructor = child; }
    __.prototype = parent.prototype;
    child.prototype = new __();
  };

  var addProperties = Rx.internals.addProperties = function (obj) {
    for(var sources = [], i = 1, len = arguments.length; i < len; i++) { sources.push(arguments[i]); }
    for (var idx = 0, ln = sources.length; idx < ln; idx++) {
      var source = sources[idx];
      for (var prop in source) {
        obj[prop] = source[prop];
      }
    }
  };

  // Rx Utils
  var addRef = Rx.internals.addRef = function (xs, r) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[18317:18468]", functionData => eval(functionData))};

  function arrayInitialize(count, factory) {
    var a = new Array(count);
    for (var i = 0; i < count; i++) {
      a[i] = factory();
    }
    return a;
  }

  var errorObj = {e: {}};
  var tryCatchTarget;
  function tryCatcher() {
    try {
      return tryCatchTarget.apply(this, arguments);
    } catch (e) {
      errorObj.e = e;
      return errorObj;
    }
  }
  function tryCatch(fn) {
    if (!isFunction(fn)) { throw new TypeError('fn must be a function'); }
    tryCatchTarget = fn;
    return tryCatcher;
  }
  function thrower(e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[19017:19035]", functionData => eval(functionData))}

  // Collections
  function IndexedItem(id, value) {
    this.id = id;
    this.value = value;
  }

  IndexedItem.prototype.compareTo = function (other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[19190:19297]", functionData => eval(functionData))};

  // Priority Queue for Scheduling
  var PriorityQueue = Rx.internals.PriorityQueue = function (capacity) {
    this.items = new Array(capacity);
    this.length = 0;
  };

  var priorityProto = PriorityQueue.prototype;
  priorityProto.isHigherPriority = function (left, right) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[19578:19645]", functionData => eval(functionData))};

  priorityProto.percolate = function (index) {
    if (index >= this.length || index < 0) { return; }
    var parent = index - 1 >> 1;
    if (parent < 0 || parent === index) { return; }
    if (this.isHigherPriority(index, parent)) {
      var temp = this.items[index];
      this.items[index] = this.items[parent];
      this.items[parent] = temp;
      this.percolate(parent);
    }
  };

  priorityProto.heapify = function (index) {
    +index || (index = 0);
    if (index >= this.length || index < 0) { return; }
    var left = 2 * index + 1,
        right = 2 * index + 2,
        first = index;
    if (left < this.length && this.isHigherPriority(left, first)) {
      first = left;
    }
    if (right < this.length && this.isHigherPriority(right, first)) {
      first = right;
    }
    if (first !== index) {
      var temp = this.items[index];
      this.items[index] = this.items[first];
      this.items[first] = temp;
      this.heapify(first);
    }
  };

  priorityProto.peek = function () { return this.items[0].value; };

  priorityProto.removeAt = function (index) {
    this.items[index] = this.items[--this.length];
    this.items[this.length] = undefined;
    this.heapify();
  };

  priorityProto.dequeue = function () {
    var result = this.peek();
    this.removeAt(0);
    return result;
  };

  priorityProto.enqueue = function (item) {
    var index = this.length++;
    this.items[index] = new IndexedItem(PriorityQueue.count++, item);
    this.percolate(index);
  };

  priorityProto.remove = function (item) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[21190:21360]", functionData => eval(functionData))};
  PriorityQueue.count = 0;

  /**
   * Represents a group of disposable resources that are disposed together.
   * @constructor
   */
  var CompositeDisposable = Rx.CompositeDisposable = function () {
    var args = [], i, len;
    if (Array.isArray(arguments[0])) {
      args = arguments[0];
      len = args.length;
    } else {
      len = arguments.length;
      args = new Array(len);
      for(i = 0; i < len; i++) { args[i] = arguments[i]; }
    }
    for(i = 0; i < len; i++) {
      if (!isDisposable(args[i])) { throw new TypeError('Not a disposable'); }
    }
    this.disposables = args;
    this.isDisposed = false;
    this.length = args.length;
  };

  var CompositeDisposablePrototype = CompositeDisposable.prototype;

  /**
   * Adds a disposable to the CompositeDisposable or disposes the disposable if the CompositeDisposable is disposed.
   * @param {Mixed} item Disposable to add.
   */
  CompositeDisposablePrototype.add = function (item) {
    if (this.isDisposed) {
      item.dispose();
    } else {
      this.disposables.push(item);
      this.length++;
    }
  };

  /**
   * Removes and disposes the first occurrence of a disposable from the CompositeDisposable.
   * @param {Mixed} item Disposable to remove.
   * @returns {Boolean} true if found; false otherwise.
   */
  CompositeDisposablePrototype.remove = function (item) {
    var shouldDispose = false;
    if (!this.isDisposed) {
      var idx = this.disposables.indexOf(item);
      if (idx !== -1) {
        shouldDispose = true;
        this.disposables.splice(idx, 1);
        this.length--;
        item.dispose();
      }
    }
    return shouldDispose;
  };

  /**
   *  Disposes all disposables in the group and removes them from the group.
   */
  CompositeDisposablePrototype.dispose = function () {
    if (!this.isDisposed) {
      this.isDisposed = true;
      var len = this.disposables.length, currentDisposables = new Array(len);
      for(var i = 0; i < len; i++) { currentDisposables[i] = this.disposables[i]; }
      this.disposables = [];
      this.length = 0;

      for (i = 0; i < len; i++) {
        currentDisposables[i].dispose();
      }
    }
  };

  /**
   * Provides a set of static methods for creating Disposables.
   * @param {Function} dispose Action to run during the first call to dispose. The action is guaranteed to be run at most once.
   */
  var Disposable = Rx.Disposable = function (action) {
    this.isDisposed = false;
    this.action = action || noop;
  };

  /** Performs the task of cleaning up resources. */
  Disposable.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[23955:24045]", functionData => eval(functionData))};

  /**
   * Creates a disposable object that invokes the specified action when disposed.
   * @param {Function} dispose Action to run during the first call to dispose. The action is guaranteed to be run at most once.
   * @return {Disposable} The disposable object that runs the given action upon disposal.
   */
  var disposableCreate = Disposable.create = function (action) { return new Disposable(action); };

  /**
   * Gets the disposable that does nothing when disposed.
   */
  var disposableEmpty = Disposable.empty = { dispose: noop };

  /**
   * Validates whether the given object is a disposable
   * @param {Object} Object to test whether it has a dispose method
   * @returns {Boolean} true if a disposable object, else false.
   */
  var isDisposable = Disposable.isDisposable = function (d) {
    return d && isFunction(d.dispose);
  };

  var checkDisposed = Disposable.checkDisposed = function (disposable) {
    if (disposable.isDisposed) { throw new ObjectDisposedError(); }
  };

  // Single assignment
  var SingleAssignmentDisposable = Rx.SingleAssignmentDisposable = function () {
    this.isDisposed = false;
    this.current = null;
  };
  SingleAssignmentDisposable.prototype.getDisposable = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[25278:25308]", functionData => eval(functionData))};
  SingleAssignmentDisposable.prototype.setDisposable = function (value) {
    if (this.current) { throw new Error('Disposable has already been assigned'); }
    var shouldDispose = this.isDisposed;
    !shouldDispose && (this.current = value);
    shouldDispose && value && value.dispose();
  };
  SingleAssignmentDisposable.prototype.dispose = function () {
    if (!this.isDisposed) {
      this.isDisposed = true;
      var old = this.current;
      this.current = null;
    }
    old && old.dispose();
  };

  // Multiple assignment disposable
  var SerialDisposable = Rx.SerialDisposable = function () {
    this.isDisposed = false;
    this.current = null;
  };
  SerialDisposable.prototype.getDisposable = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[26035:26065]", functionData => eval(functionData))};
  SerialDisposable.prototype.setDisposable = function (value) {
    var shouldDispose = this.isDisposed;
    if (!shouldDispose) {
      var old = this.current;
      this.current = value;
    }
    old && old.dispose();
    shouldDispose && value && value.dispose();
  };
  SerialDisposable.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[26391:26543]", functionData => eval(functionData))};

  /**
   * Represents a disposable resource that only disposes its underlying disposable resource when all dependent disposable objects have been disposed.
   */
  var RefCountDisposable = Rx.RefCountDisposable = (function () {

    function InnerDisposable(disposable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[26816:26926]", functionData => eval(functionData))}

    InnerDisposable.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[26980:27324]", functionData => eval(functionData))};

    /**
     * Initializes a new instance of the RefCountDisposable with the specified disposable.
     * @constructor
     * @param {Disposable} disposable Underlying disposable.
      */
    function RefCountDisposable(disposable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[27560:27704]", functionData => eval(functionData))}

    /**
     * Disposes the underlying disposable only when all dependent disposables have been disposed
     */
    RefCountDisposable.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[27874:28108]", functionData => eval(functionData))};

    /**
     * Returns a dependent disposable that when disposed decreases the refcount on the underlying disposable.
     * @returns {Disposable} A dependent disposable contributing to the reference count that manages the underlying disposable's lifetime.
     */
    RefCountDisposable.prototype.getDisposable = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[28437:28520]", functionData => eval(functionData))};

    return RefCountDisposable;
  })();

  function ScheduledDisposable(scheduler, disposable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[28617:28717]", functionData => eval(functionData))}

  function scheduleItem(s, self) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[28752:28854]", functionData => eval(functionData))}

  ScheduledDisposable.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[28910:28973]", functionData => eval(functionData))};

  var ScheduledItem = Rx.internals.ScheduledItem = function (scheduler, state, action, dueTime, comparer) {
    this.scheduler = scheduler;
    this.state = state;
    this.action = action;
    this.dueTime = dueTime;
    this.comparer = comparer || defaultSubComparer;
    this.disposable = new SingleAssignmentDisposable();
  }

  ScheduledItem.prototype.invoke = function () {
    this.disposable.setDisposable(this.invokeCore());
  };

  ScheduledItem.prototype.compareTo = function (other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[29471:29531]", functionData => eval(functionData))};

  ScheduledItem.prototype.isCancelled = function () {
    return this.disposable.isDisposed;
  };

  ScheduledItem.prototype.invokeCore = function () {
    return this.action(this.scheduler, this.state);
  };

  /** Provides a set of static properties to access commonly used schedulers. */
  var Scheduler = Rx.Scheduler = (function () {

    function Scheduler(now, schedule, scheduleRelative, scheduleAbsolute) {
      this.now = now;
      this._schedule = schedule;
      this._scheduleRelative = scheduleRelative;
      this._scheduleAbsolute = scheduleAbsolute;
    }

    /** Determines whether the given object is a scheduler */
    Scheduler.isScheduler = function (s) {
      return s instanceof Scheduler;
    }

    function invokeAction(scheduler, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[30304:30357]", functionData => eval(functionData))}

    var schedulerProto = Scheduler.prototype;

    /**
     * Schedules an action to be executed.
     * @param {Function} action Action to execute.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.schedule = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[30666:30724]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed.
     * @param state State passed to the action to be executed.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleWithState = function (state, action) {
      return this._schedule(state, action);
    };

    /**
     * Schedules an action to be executed after the specified relative due time.
     * @param {Function} action Action to execute.
     * @param {Number} dueTime Relative time after which to execute the action.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleWithRelative = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[31523:31598]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed after dueTime.
     * @param state State passed to the action to be executed.
     * @param {Function} action Action to be executed.
     * @param {Number} dueTime Relative time after which to execute the action.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleWithRelativeAndState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[32058:32126]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed at the specified absolute due time.
     * @param {Function} action Action to execute.
     * @param {Number} dueTime Absolute time at which to execute the action.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
      */
    schedulerProto.scheduleWithAbsolute = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[32523:32598]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed at dueTime.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Function} action Action to be executed.
     * @param {Number}dueTime Absolute time at which to execute the action.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleWithAbsoluteAndState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[33059:33127]", functionData => eval(functionData))};

    /** Gets the current time according to the local machine's system clock. */
    Scheduler.now = defaultNow;

    /**
     * Normalizes the specified TimeSpan value to a positive value.
     * @param {Number} timeSpan The time span value to normalize.
     * @returns {Number} The specified TimeSpan value if it is zero or positive; otherwise, 0
     */
    Scheduler.normalize = function (timeSpan) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[33533:33601]", functionData => eval(functionData))};

    return Scheduler;
  }());

  var normalizeTime = Scheduler.normalize, isScheduler = Scheduler.isScheduler;

  (function (schedulerProto) {

    function invokeRecImmediate(scheduler, pair) {
      var state = pair[0], action = pair[1], group = new CompositeDisposable();

      function recursiveAction(state1) {
        action(state1, function (state2) {
          var isAdded = false, isDone = false,
          d = scheduler.scheduleWithState(state2, function (scheduler1, state3) {
            if (isAdded) {
              group.remove(d);
            } else {
              isDone = true;
            }
            recursiveAction(state3);
            return disposableEmpty;
          });
          if (!isDone) {
            group.add(d);
            isAdded = true;
          }
        });
      }
      recursiveAction(state);
      return group;
    }

    function invokeRecDate(scheduler, pair, method) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[34522:35203]", functionData => eval(functionData))}

    function scheduleInnerRecursive(action, self) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[35255:35312]", functionData => eval(functionData))}

    /**
     * Schedules an action to be executed recursively.
     * @param {Function} action Action to execute recursively. The parameter passed to the action is used to trigger recursive scheduling of the action.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursive = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[35697:35782]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed recursively.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Function} action Action to execute recursively. The last parameter passed to the action is used to trigger recursive scheduling of the action, passing in recursive invocation state.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursiveWithState = function (state, action) {
      return this.scheduleWithState([state, action], invokeRecImmediate);
    };

    /**
     * Schedules an action to be executed recursively after a specified relative due time.
     * @param {Function} action Action to execute recursively. The parameter passed to the action is used to trigger recursive scheduling of the action at the specified relative time.
     * @param {Number}dueTime Relative time after which to execute the action for the first time.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursiveWithRelative = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[36952:37057]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed recursively after a specified relative due time.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Function} action Action to execute recursively. The last parameter passed to the action is used to trigger recursive scheduling of the action, passing in the recursive due time and invocation state.
     * @param {Number}dueTime Relative time after which to execute the action for the first time.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursiveWithRelativeAndState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[37745:37910]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed recursively at a specified absolute due time.
     * @param {Function} action Action to execute recursively. The parameter passed to the action is used to trigger recursive scheduling of the action at the specified absolute time.
     * @param {Number}dueTime Absolute time at which to execute the action for the first time.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursiveWithAbsolute = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[38476:38581]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed recursively at a specified absolute due time.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Function} action Action to execute recursively. The last parameter passed to the action is used to trigger recursive scheduling of the action, passing in the recursive due time and invocation state.
     * @param {Number}dueTime Absolute time at which to execute the action for the first time.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    schedulerProto.scheduleRecursiveWithAbsoluteAndState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[39263:39428]", functionData => eval(functionData))};
  }(Scheduler.prototype));

  (function (schedulerProto) {

    /**
     * Schedules a periodic piece of work by dynamically discovering the scheduler's capabilities. The periodic task will be scheduled using window.setInterval for the base implementation.
     * @param {Number} period Period for running the work periodically.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled recurring action (best effort).
     */
    Scheduler.prototype.schedulePeriodic = function (period, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[40003:40077]", functionData => eval(functionData))};

    /**
     * Schedules a periodic piece of work by dynamically discovering the scheduler's capabilities. The periodic task will be scheduled using window.setInterval for the base implementation.
     * @param {Mixed} state Initial state passed to the action upon the first iteration.
     * @param {Number} period Period for running the work periodically.
     * @param {Function} action Action to be executed, potentially updating the state.
     * @returns {Disposable} The disposable object used to cancel the scheduled recurring action (best effort).
     */
    Scheduler.prototype.schedulePeriodicWithState = function(state, period, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[40729:41016]", functionData => eval(functionData))};

  }(Scheduler.prototype));

  (function (schedulerProto) {
    /**
     * Returns a scheduler that wraps the original scheduler, adding exception handling for scheduled actions.
     * @param {Function} handler Handler that's run if an exception is caught. The exception will be rethrown if the handler returns false.
     * @returns {Scheduler} Wrapper around the original scheduler, enforcing exception handling.
     */
    schedulerProto.catchError = schedulerProto['catch'] = function (handler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[41519:41574]", functionData => eval(functionData))};
  }(Scheduler.prototype));

  var SchedulePeriodicRecursive = Rx.internals.SchedulePeriodicRecursive = (function () {
    function tick(command, recurse) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[41730:41907]", functionData => eval(functionData))}

    function SchedulePeriodicRecursive(scheduler, state, period, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[41982:42109]", functionData => eval(functionData))}

    SchedulePeriodicRecursive.prototype.start = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[42171:42379]", functionData => eval(functionData))};

    return SchedulePeriodicRecursive;
  }());

  /** Gets a scheduler that schedules work immediately on the current thread. */
  var immediateScheduler = Scheduler.immediate = (function () {
    function scheduleNow(state, action) { return action(this, state); }
    return new Scheduler(defaultNow, scheduleNow, notSupported, notSupported);
  }());

  /**
   * Gets a scheduler that schedules work as soon as possible on the current thread.
   */
  var currentThreadScheduler = Scheduler.currentThread = (function () {
    var queue;

    function runTrampoline () {
      while (queue.length > 0) {
        var item = queue.dequeue();
        !item.isCancelled() && item.invoke();
      }
    }

    function scheduleNow(state, action) {
      var si = new ScheduledItem(this, state, action, this.now());

      if (!queue) {
        queue = new PriorityQueue(4);
        queue.enqueue(si);

        var result = tryCatch(runTrampoline)();
        queue = null;
        if (result === errorObj) { return thrower(result.e); }
      } else {
        queue.enqueue(si);
      }
      return si.disposable;
    }

    var currentScheduler = new Scheduler(defaultNow, scheduleNow, notSupported, notSupported);
    currentScheduler.scheduleRequired = function () { return !queue; };

    return currentScheduler;
  }());

  var scheduleMethod, clearMethod;

  var localTimer = (function () {
    var localSetTimeout, localClearTimeout = noop;
    if (!!root.setTimeout) {
      localSetTimeout = root.setTimeout;
      localClearTimeout = root.clearTimeout;
    } else if (!!root.WScript) {
      localSetTimeout = function (fn, time) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[44014:44071]", functionData => eval(functionData))};
    } else {
      throw new NotSupportedError();
    }

    return {
      setTimeout: localSetTimeout,
      clearTimeout: localClearTimeout
    };
  }());
  var localSetTimeout = localTimer.setTimeout,
    localClearTimeout = localTimer.clearTimeout;

  (function () {

    var nextHandle = 1, tasksByHandle = {}, currentlyRunning = false;

    clearMethod = function (handle) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[44453:44496]", functionData => eval(functionData))};

    function runTask(handle) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[44528:44929]", functionData => eval(functionData))}

    var reNative = RegExp('^' +
      String(toString)
        .replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
        .replace(/toString| for [^\]]+/g, '.*?') + '$'
    );

    var setImmediate = typeof (setImmediate = freeGlobal && moduleExports && freeGlobal.setImmediate) == 'function' &&
      !reNative.test(setImmediate) && setImmediate;

    function postMessageSupported () {
      // Ensure not in a worker
      if (!root.postMessage || root.importScripts) { return false; }
      var isAsync = false, oldHandler = root.onmessage;
      // Test for async
      root.onmessage = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[45524:45543]", functionData => eval(functionData))};
      root.postMessage('', '*');
      root.onmessage = oldHandler;

      return isAsync;
    }

    // Use in order, setImmediate, nextTick, postMessage, MessageChannel, script readystatechanged, setTimeout
    if (isFunction(setImmediate)) {
      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[45831:45979]", functionData => eval(functionData))};
    } else if (typeof process !== 'undefined' && {}.toString.call(process) === '[object process]') {
      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[46123:46275]", functionData => eval(functionData))};
    } else if (postMessageSupported()) {
      var MSG_PREFIX = 'ms.rx.schedule' + Math.random();

      function onGlobalPostMessage(event) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[46418:46670]", functionData => eval(functionData))}

      if (root.addEventListener) {
        root.addEventListener('message', onGlobalPostMessage, false);
      } else if (root.attachEvent) {
        root.attachEvent('onmessage', onGlobalPostMessage);
      } else {
        root.onmessage = onGlobalPostMessage;
      }

      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[46985:47135]", functionData => eval(functionData))};
    } else if (!!root.MessageChannel) {
      var channel = new root.MessageChannel();

      channel.port1.onmessage = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[47270:47290]", functionData => eval(functionData))};

      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[47334:47468]", functionData => eval(functionData))};
    } else if ('document' in root && 'onreadystatechange' in root.document.createElement('script')) {

      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[47614:48080]", functionData => eval(functionData))};

    } else {
      scheduleMethod = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[48137:48309]", functionData => eval(functionData))};
    }
  }());

  /**
   * Gets a scheduler that schedules work via a timed callback based upon platform.
   */
  var timeoutScheduler = Scheduler.timeout = Scheduler['default'] = (function () {

    function scheduleNow(state, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[48546:48884]", functionData => eval(functionData))}

    function scheduleRelative(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[48940:49399]", functionData => eval(functionData))}

    function scheduleAbsolute(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[49455:49547]", functionData => eval(functionData))}

    return new Scheduler(defaultNow, scheduleNow, scheduleRelative, scheduleAbsolute);
  })();

  var CatchScheduler = (function (__super__) {

    function scheduleNow(state, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[49733:49815]", functionData => eval(functionData))}

    function scheduleRelative(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[49871:49973]", functionData => eval(functionData))}

    function scheduleAbsolute(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[50029:50131]", functionData => eval(functionData))}

    inherits(CatchScheduler, __super__);

    function CatchScheduler(scheduler, handler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[50223:50491]", functionData => eval(functionData))}

    CatchScheduler.prototype._clone = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[50552:50620]", functionData => eval(functionData))};

    CatchScheduler.prototype._wrap = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[50678:50952]", functionData => eval(functionData))};

    CatchScheduler.prototype._getRecursiveWrapper = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[51028:51357]", functionData => eval(functionData))};

    CatchScheduler.prototype.schedulePeriodicWithState = function (state, period, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[51450:51895]", functionData => eval(functionData))};

    return CatchScheduler;
  }(Scheduler));

  /**
   *  Represents a notification to an observer.
   */
  var Notification = Rx.Notification = (function () {
    function Notification(kind, value, exception, accept, acceptObservable, toString) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[52143:52344]", functionData => eval(functionData))}

    /**
     * Invokes the delegate corresponding to the notification or the observer's method corresponding to the notification and returns the produced result.
     *
     * @memberOf Notification
     * @param {Any} observerOrOnNext Delegate to invoke for an OnNext notification or Observer to invoke the notification on..
     * @param {Function} onError Delegate to invoke for an OnError notification.
     * @param {Function} onCompleted Delegate to invoke for an OnCompleted notification.
     * @returns {Any} Result produced by the observation.
     */
    Notification.prototype.accept = function (observerOrOnNext, onError, onCompleted) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[52994:53186]", functionData => eval(functionData))};

    /**
     * Returns an observable sequence with a single notification.
     *
     * @memberOf Notifications
     * @param {Scheduler} [scheduler] Scheduler to send out the notification calls on.
     * @returns {Observable} The observable sequence that surfaces the behavior of the notification upon subscription.
     */
    Notification.prototype.toObservable = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[53578:53948]", functionData => eval(functionData))};

    return Notification;
  })();

  /**
   * Creates an object that represents an OnNext notification to an observer.
   * @param {Any} value The value contained in the notification.
   * @returns {Notification} The OnNext notification containing the value.
   */
  var notificationCreateOnNext = Notification.createOnNext = (function () {
      function _accept(onNext) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[54322:54352]", functionData => eval(functionData))}
      function _acceptObservable(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[54396:54435]", functionData => eval(functionData))}
      function toString() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[54462:54502]", functionData => eval(functionData))}

      return function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[54534:54632]", functionData => eval(functionData))};
  }());

  /**
   * Creates an object that represents an OnError notification to an observer.
   * @param {Any} error The exception contained in the notification.
   * @returns {Notification} The OnError notification containing the exception.
   */
  var notificationCreateOnError = Notification.createOnError = (function () {
    function _accept (onNext, onError) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55000:55035]", functionData => eval(functionData))}
    function _acceptObservable(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55077:55121]", functionData => eval(functionData))}
    function toString () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55147:55192]", functionData => eval(functionData))}

    return function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55218:55308]", functionData => eval(functionData))};
  }());

  /**
   * Creates an object that represents an OnCompleted notification to an observer.
   * @returns {Notification} The OnCompleted notification.
   */
  var notificationCreateOnCompleted = Notification.createOnCompleted = (function () {
    function _accept (onNext, onError, onCompleted) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55611:55636]", functionData => eval(functionData))}
    function _acceptObservable(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55678:55712]", functionData => eval(functionData))}
    function toString () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55738:55765]", functionData => eval(functionData))}

    return function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[55790:55883]", functionData => eval(functionData))};
  }());

  /**
   * Supports push-style iteration over an observable sequence.
   */
  var Observer = Rx.Observer = function () { };

  /**
   *  Creates a notification callback from an observer.
   * @returns The action that forwards its input notification to the underlying observer.
   */
  Observer.prototype.toNotifier = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[56223:56309]", functionData => eval(functionData))};

  /**
   *  Hides the identity of an observer.
   * @returns An observer that hides the identity of the specified observer.
   */
  Observer.prototype.asObserver = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[56488:56605]", functionData => eval(functionData))};

  /**
   *  Checks access to the observer for grammar violations. This includes checking for multiple OnError or OnCompleted calls, as well as reentrancy in any of the observer methods.
   *  If a violation is detected, an Error is thrown from the offending observer method call.
   * @returns An observer that checks callbacks invocations against the observer grammar and, if the checks pass, forwards those to the specified observer.
   */
  Observer.prototype.checked = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[57093:57130]", functionData => eval(functionData))};

  /**
   *  Creates an observer from the specified OnNext, along with optional OnError, and OnCompleted actions.
   * @param {Function} [onNext] Observer's OnNext action implementation.
   * @param {Function} [onError] Observer's OnError action implementation.
   * @param {Function} [onCompleted] Observer's OnCompleted action implementation.
   * @returns {Observer} The observer object implemented using the given actions.
   */
  var observerCreate = Observer.create = function (onNext, onError, onCompleted) {
    onNext || (onNext = noop);
    onError || (onError = defaultError);
    onCompleted || (onCompleted = noop);
    return new AnonymousObserver(onNext, onError, onCompleted);
  };

  /**
   *  Creates an observer from a notification callback.
   *
   * @static
   * @memberOf Observer
   * @param {Function} handler Action that handles a notification.
   * @returns The observer object that invokes the specified handler using a notification corresponding to each message it receives.
   */
  Observer.fromNotifier = function (handler, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[58195:58499]", functionData => eval(functionData))};

  /**
   * Schedules the invocation of observer methods on the given scheduler.
   * @param {Scheduler} scheduler Scheduler to schedule observer messages on.
   * @returns {Observer} Observer whose messages are scheduled on the given scheduler.
   */
  Observer.prototype.notifyOn = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[58806:58862]", functionData => eval(functionData))};

  Observer.prototype.makeSafe = function(disposable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[58918:59021]", functionData => eval(functionData))};

  /**
   * Abstract base class for implementations of the Observer class.
   * This base class enforces the grammar of observers where OnError and OnCompleted are terminal messages.
   */
  var AbstractObserver = Rx.internals.AbstractObserver = (function (__super__) {
    inherits(AbstractObserver, __super__);

    /**
     * Creates a new observer in a non-stopped state.
     */
    function AbstractObserver() {
      this.isStopped = false;
      __super__.call(this);
    }

    // Must be implemented by other observers
    AbstractObserver.prototype.next = notImplemented;
    AbstractObserver.prototype.error = notImplemented;
    AbstractObserver.prototype.completed = notImplemented;

    /**
     * Notifies the observer of a new element in the sequence.
     * @param {Any} value Next element in the sequence.
     */
    AbstractObserver.prototype.onNext = function (value) {
      if (!this.isStopped) { this.next(value); }
    };

    /**
     * Notifies the observer that an exception has occurred.
     * @param {Any} error The error that has occurred.
     */
    AbstractObserver.prototype.onError = function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[60162:60264]", functionData => eval(functionData))};

    /**
     * Notifies the observer of the end of the sequence.
     */
    AbstractObserver.prototype.onCompleted = function () {
      if (!this.isStopped) {
        this.isStopped = true;
        this.completed();
      }
    };

    /**
     * Disposes the observer, causing it to transition to the stopped state.
     */
    AbstractObserver.prototype.dispose = function () {
      this.isStopped = true;
    };

    AbstractObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[60737:60877]", functionData => eval(functionData))};

    return AbstractObserver;
  }(Observer));

  /**
   * Class to create an Observer instance from delegate-based implementations of the on* methods.
   */
  var AnonymousObserver = Rx.AnonymousObserver = (function (__super__) {
    inherits(AnonymousObserver, __super__);

    /**
     * Creates an observer from the specified OnNext, OnError, and OnCompleted actions.
     * @param {Any} onNext Observer's OnNext action implementation.
     * @param {Any} onError Observer's OnError action implementation.
     * @param {Any} onCompleted Observer's OnCompleted action implementation.
     */
    function AnonymousObserver(onNext, onError, onCompleted) {
      __super__.call(this);
      this._onNext = onNext;
      this._onError = onError;
      this._onCompleted = onCompleted;
    }

    /**
     * Calls the onNext action.
     * @param {Any} value Next element in the sequence.
     */
    AnonymousObserver.prototype.next = function (value) {
      this._onNext(value);
    };

    /**
     * Calls the onError action.
     * @param {Any} error The error that has occurred.
     */
    AnonymousObserver.prototype.error = function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[62029:62064]", functionData => eval(functionData))};

    /**
     *  Calls the onCompleted action.
     */
    AnonymousObserver.prototype.completed = function () {
      this._onCompleted();
    };

    return AnonymousObserver;
  }(AbstractObserver));

  var CheckedObserver = (function (__super__) {
    inherits(CheckedObserver, __super__);

    function CheckedObserver(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[62399:62522]", functionData => eval(functionData))}

    var CheckedObserverPrototype = CheckedObserver.prototype;

    CheckedObserverPrototype.onNext = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[62642:62817]", functionData => eval(functionData))};

    CheckedObserverPrototype.onError = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[62874:63048]", functionData => eval(functionData))};

    CheckedObserverPrototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[63106:63279]", functionData => eval(functionData))};

    CheckedObserverPrototype.checkAccess = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[63337:63540]", functionData => eval(functionData))};

    return CheckedObserver;
  }(Observer));

  var ScheduledObserver = Rx.internals.ScheduledObserver = (function (__super__) {
    inherits(ScheduledObserver, __super__);

    function ScheduledObserver(scheduler, observer) {
      __super__.call(this);
      this.scheduler = scheduler;
      this.observer = observer;
      this.isAcquired = false;
      this.hasFaulted = false;
      this.queue = [];
      this.disposable = new SerialDisposable();
    }

    ScheduledObserver.prototype.next = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[64060:64159]", functionData => eval(functionData))};

    ScheduledObserver.prototype.error = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[64215:64311]", functionData => eval(functionData))};

    ScheduledObserver.prototype.completed = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[64370:64469]", functionData => eval(functionData))};

    ScheduledObserver.prototype.ensureActive = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[64531:65216]", functionData => eval(functionData))};

    ScheduledObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[65273:65359]", functionData => eval(functionData))};

    return ScheduledObserver;
  }(AbstractObserver));

  var ObserveOnObserver = (function (__super__) {
    inherits(ObserveOnObserver, __super__);

    function ObserveOnObserver(scheduler, observer, cancel) {
      __super__.call(this, scheduler, observer);
      this._cancel = cancel;
    }

    ObserveOnObserver.prototype.next = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[65715:65799]", functionData => eval(functionData))};

    ObserveOnObserver.prototype.error = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[65855:65936]", functionData => eval(functionData))};

    ObserveOnObserver.prototype.completed = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[65995:66077]", functionData => eval(functionData))};

    ObserveOnObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[66134:66260]", functionData => eval(functionData))};

    return ObserveOnObserver;
  })(ScheduledObserver);

  var observableProto;

  /**
   * Represents a push-style collection.
   */
  var Observable = Rx.Observable = (function () {

    function Observable(subscribe) {
      if (Rx.config.longStackSupport && hasStacks) {
        try {
          throw new Error();
        } catch (e) {
          this.stack = e.stack.substring(e.stack.indexOf("\n") + 1);
        }

        var self = this;
        this._subscribe = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[66753:67006]", functionData => eval(functionData))};
      } else {
        this._subscribe = subscribe;
      }
    }

    observableProto = Observable.prototype;

    /**
     *  Subscribes an observer to the observable sequence.
     *  @param {Mixed} [observerOrOnNext] The object that is to receive notifications or an action to invoke for each element in the observable sequence.
     *  @param {Function} [onError] Action to invoke upon exceptional termination of the observable sequence.
     *  @param {Function} [onCompleted] Action to invoke upon graceful termination of the observable sequence.
     *  @returns {Diposable} A disposable handling the subscriptions and unsubscriptions.
     */
    observableProto.subscribe = observableProto.forEach = function (observerOrOnNext, onError, onCompleted) {
      return this._subscribe(typeof observerOrOnNext === 'object' ?
        observerOrOnNext :
        observerCreate(observerOrOnNext, onError, onCompleted));
    };

    /**
     * Subscribes to the next value in the sequence with an optional "this" argument.
     * @param {Function} onNext The function to invoke on each element in the observable sequence.
     * @param {Any} [thisArg] Object to use as this when executing callback.
     * @returns {Disposable} A disposable handling the subscriptions and unsubscriptions.
     */
    observableProto.subscribeOnNext = function (onNext, thisArg) {
      return this._subscribe(observerCreate(typeof thisArg !== 'undefined' ? function(x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[68462:68490]", functionData => eval(functionData))} : onNext));
    };

    /**
     * Subscribes to an exceptional condition in the sequence with an optional "this" argument.
     * @param {Function} onError The function to invoke upon exceptional termination of the observable sequence.
     * @param {Any} [thisArg] Object to use as this when executing callback.
     * @returns {Disposable} A disposable handling the subscriptions and unsubscriptions.
     */
    observableProto.subscribeOnError = function (onError, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[68970:69115]", functionData => eval(functionData))};

    /**
     * Subscribes to the next value in the sequence with an optional "this" argument.
     * @param {Function} onCompleted The function to invoke upon graceful termination of the observable sequence.
     * @param {Any} [thisArg] Object to use as this when executing callback.
     * @returns {Disposable} A disposable handling the subscriptions and unsubscriptions.
     */
    observableProto.subscribeOnCompleted = function (onCompleted, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[69576:69731]", functionData => eval(functionData))};

    return Observable;
  })();

  var ObservableBase = Rx.ObservableBase = (function (__super__) {
    inherits(ObservableBase, __super__);

    function fixSubscriber(subscriber) {
      return subscriber && isFunction(subscriber.dispose) ? subscriber :
        isFunction(subscriber) ? disposableCreate(subscriber) : disposableEmpty;
    }

    function setDisposable(s, state) {
      var ado = state[0], self = state[1];
      var sub = tryCatch(self.subscribeCore).call(self, ado);

      if (sub === errorObj) {
        if(!ado.fail(errorObj.e)) { return thrower(errorObj.e); }
      }
      ado.setDisposable(fixSubscriber(sub));
    }

    function subscribe(observer) {
      var ado = new AutoDetachObserver(observer), state = [ado, this];

      if (currentThreadScheduler.scheduleRequired()) {
        currentThreadScheduler.scheduleWithState(state, setDisposable);
      } else {
        setDisposable(null, state);
      }
      return ado;
    }

    function ObservableBase() {
      __super__.call(this, subscribe);
    }

    ObservableBase.prototype.subscribeCore = notImplemented;

    return ObservableBase;
  }(Observable));

  var Enumerable = Rx.internals.Enumerable = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[70939:70942]", functionData => eval(functionData))};

  var ConcatEnumerableObservable = (function(__super__) {
    inherits(ConcatEnumerableObservable, __super__);
    function ConcatEnumerableObservable(sources) {
      this.sources = sources;
      __super__.call(this);
    }
    
    ConcatEnumerableObservable.prototype.subscribeCore = function (o) {
      var isDisposed, subscription = new SerialDisposable();
      var cancelable = immediateScheduler.scheduleRecursiveWithState(this.sources[$iterator$](), function (e, self) {
        if (isDisposed) { return; }
        var currentItem = tryCatch(e.next).call(e);
        if (currentItem === errorObj) { return o.onError(currentItem.e); }

        if (currentItem.done) {
          return o.onCompleted();
        }

        // Check if promise
        var currentValue = currentItem.value;
        isPromise(currentValue) && (currentValue = observableFromPromise(currentValue));

        var d = new SingleAssignmentDisposable();
        subscription.setDisposable(d);
        d.setDisposable(currentValue.subscribe(new InnerObserver(o, self, e)));
      });

      return new CompositeDisposable(subscription, cancelable, disposableCreate(function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[72104:72140]", functionData => eval(functionData))}));
    };
    
    function InnerObserver(o, s, e) {
      this.o = o;
      this.s = s;
      this.e = e;
      this.isStopped = false;
    }
    InnerObserver.prototype.onNext = function (x) { if(!this.isStopped) { this.o.onNext(x); } };
    InnerObserver.prototype.onError = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[72434:72538]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {
      if (!this.isStopped) {
        this.isStopped = true;
        this.s(this.e);
      }
    };
    InnerObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[72745:72771]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[72823:72968]", functionData => eval(functionData))};
    
    return ConcatEnumerableObservable;
  }(ObservableBase));

  Enumerable.prototype.concat = function () {
    return new ConcatEnumerableObservable(this);
  };
  
  var CatchErrorObservable = (function(__super__) {
    inherits(CatchErrorObservable, __super__);
    function CatchErrorObservable(sources) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[73282:73347]", functionData => eval(functionData))}
    
    CatchErrorObservable.prototype.subscribeCore = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[73417:74473]", functionData => eval(functionData))};
    
    return CatchErrorObservable;
  }(ObservableBase));

  Enumerable.prototype.catchError = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[74584:74632]", functionData => eval(functionData))};

  Enumerable.prototype.catchErrorWhen = function (notificationHandler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[74706:76446]", functionData => eval(functionData))};
  
  var RepeatEnumerable = (function (__super__) {
    inherits(RepeatEnumerable, __super__);
    
    function RepeatEnumerable(v, c) {
      this.v = v;
      this.c = c == null ? -1 : c;
    }
    RepeatEnumerable.prototype[$iterator$] = function () {
      return new RepeatEnumerator(this); 
    };
    
    function RepeatEnumerator(p) {
      this.v = p.v;
      this.l = p.c;
    }
    RepeatEnumerator.prototype.next = function () {
      if (this.l === 0) { return doneEnumerator; }
      if (this.l > 0) { this.l--; }
      return { done: false, value: this.v }; 
    };
    
    return RepeatEnumerable;
  }(Enumerable));

  var enumerableRepeat = Enumerable.repeat = function (value, repeatCount) {
    return new RepeatEnumerable(value, repeatCount);
  };
  
  var OfEnumerable = (function(__super__) {
    inherits(OfEnumerable, __super__);
    function OfEnumerable(s, fn, thisArg) {
      this.s = s;
      this.fn = fn ? bindCallback(fn, thisArg, 3) : null;
    }
    OfEnumerable.prototype[$iterator$] = function () {
      return new OfEnumerator(this);
    };
    
    function OfEnumerator(p) {
      this.i = -1;
      this.s = p.s;
      this.l = this.s.length;
      this.fn = p.fn;
    }
    OfEnumerator.prototype.next = function () {
     return ++this.i < this.l ?
       { done: false, value: !this.fn ? this.s[this.i] : this.fn(this.s[this.i], this.i, this.s) } :
       doneEnumerator; 
    };
    
    return OfEnumerable;
  }(Enumerable));

  var enumerableOf = Enumerable.of = function (source, selector, thisArg) {
    return new OfEnumerable(source, selector, thisArg);
  };

   /**
   *  Wraps the source sequence in order to run its observer callbacks on the specified scheduler.
   *
   *  This only invokes observer callbacks on a scheduler. In case the subscription and/or unsubscription actions have side-effects
   *  that require to be run on a scheduler, use subscribeOn.
   *
   *  @param {Scheduler} scheduler Scheduler to notify observers on.
   *  @returns {Observable} The source sequence whose observations happen on the specified scheduler.
   */
  observableProto.observeOn = function (scheduler) {
    var source = this;
    return new AnonymousObservable(function (observer) {
      return source.subscribe(new ObserveOnObserver(scheduler, observer));
    }, source);
  };

   /**
   *  Wraps the source sequence in order to run its subscription and unsubscription logic on the specified scheduler. This operation is not commonly used;
   *  see the remarks section for more information on the distinction between subscribeOn and observeOn.

   *  This only performs the side-effects of subscription and unsubscription on the specified scheduler. In order to invoke observer
   *  callbacks on a scheduler, use observeOn.

   *  @param {Scheduler} scheduler Scheduler to perform subscription and unsubscription actions on.
   *  @returns {Observable} The source sequence whose subscriptions and unsubscriptions happen on the specified scheduler.
   */
  observableProto.subscribeOn = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[79510:79884]", functionData => eval(functionData))};

	var FromPromiseObservable = (function(__super__) {
		inherits(FromPromiseObservable, __super__);
		function FromPromiseObservable(p) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[80021:80066]", functionData => eval(functionData))}
		
		FromPromiseObservable.prototype.subscribeCore = function(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[80132:80282]", functionData => eval(functionData))};
		
		return FromPromiseObservable;
	}(ObservableBase));	 
	 
	 /**
	 * Converts a Promise to an Observable sequence
	 * @param {Promise} An ES6 Compliant promise.
	 * @returns {Observable} An Observable sequence which wraps the existing promise success and failure.
	 */
	var observableFromPromise = Observable.fromPromise = function (promise) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[80628:80677]", functionData => eval(functionData))};
  /*
   * Converts an existing observable sequence to an ES6 Compatible Promise
   * @example
   * var promise = Rx.Observable.return(42).toPromise(RSVP.Promise);
   *
   * // With config
   * Rx.config.Promise = RSVP.Promise;
   * var promise = Rx.Observable.return(42).toPromise();
   * @param {Function} [promiseCtor] The constructor of the promise. If not provided, it looks for it in Rx.config.Promise.
   * @returns {Promise} An ES6 compatible promise with the last value from the observable sequence.
   */
  observableProto.toPromise = function (promiseCtor) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[81246:81732]", functionData => eval(functionData))};

  var ToArrayObservable = (function(__super__) {
    inherits(ToArrayObservable, __super__);
    function ToArrayObservable(source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[81867:81930]", functionData => eval(functionData))}

    ToArrayObservable.prototype.subscribeCore = function(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[81992:82057]", functionData => eval(functionData))};

    function InnerObserver(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82090:82164]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82215:82258]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82311:82413]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82469:82605]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82657:82683]", functionData => eval(functionData))}
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[82732:82877]", functionData => eval(functionData))};

    return ToArrayObservable;
  }(ObservableBase));

  /**
  * Creates an array from an observable sequence.
  * @returns {Observable} An observable sequence containing a single element with a list containing all the elements of the source sequence.
  */
  observableProto.toArray = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[83175:83220]", functionData => eval(functionData))};

  /**
   *  Creates an observable sequence from a specified subscribe method implementation.
   * @example
   *  var res = Rx.Observable.create(function (observer) { return function () { } );
   *  var res = Rx.Observable.create(function (observer) { return Rx.Disposable.empty; } );
   *  var res = Rx.Observable.create(function (observer) { } );
   * @param {Function} subscribe Implementation of the resulting observable sequence's subscribe method, returning a function that will be wrapped in a Disposable.
   * @returns {Observable} The observable sequence with the specified implementation for the Subscribe method.
   */
  Observable.create = Observable.createWithDisposable = function (subscribe, parent) {
    return new AnonymousObservable(subscribe, parent);
  };

  /**
   *  Returns an observable sequence that invokes the specified factory function whenever a new observer subscribes.
   *
   * @example
   *  var res = Rx.Observable.defer(function () { return Rx.Observable.fromArray([1,2,3]); });
   * @param {Function} observableFactory Observable factory function to invoke for each observer that subscribes to the resulting sequence or Promise.
   * @returns {Observable} An observable sequence whose observers trigger an invocation of the given observable factory function.
   */
  var observableDefer = Observable.defer = function (observableFactory) {
    return new AnonymousObservable(function (observer) {
      var result;
      try {
        result = observableFactory();
      } catch (e) {
        return observableThrow(e).subscribe(observer);
      }
      isPromise(result) && (result = observableFromPromise(result));
      return result.subscribe(observer);
    });
  };

  var EmptyObservable = (function(__super__) {
    inherits(EmptyObservable, __super__);
    function EmptyObservable(scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[85059:85128]", functionData => eval(functionData))}

    EmptyObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[85196:85276]", functionData => eval(functionData))};

    function EmptySink(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[85320:85387]", functionData => eval(functionData))}

    function scheduleItem(s, state) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[85425:85459]", functionData => eval(functionData))}

    EmptySink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[85503:85593]", functionData => eval(functionData))};

    return EmptyObservable;
  }(ObservableBase));

  /**
   *  Returns an empty observable sequence, using the specified scheduler to send out the single OnCompleted message.
   *
   * @example
   *  var res = Rx.Observable.empty();
   *  var res = Rx.Observable.empty(Rx.Scheduler.timeout);
   * @param {Scheduler} [scheduler] Scheduler to send the termination call on.
   * @returns {Observable} An observable sequence with no elements.
   */
  var observableEmpty = Observable.empty = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[86105:86217]", functionData => eval(functionData))};

  var FromObservable = (function(__super__) {
    inherits(FromObservable, __super__);
    function FromObservable(iterable, mapper, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[86364:86493]", functionData => eval(functionData))}

    FromObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[86560:86639]", functionData => eval(functionData))};

    return FromObservable;
  }(ObservableBase));

  var FromSink = (function () {
    function FromSink(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[86764:86831]", functionData => eval(functionData))}

    FromSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[86874:87620]", functionData => eval(functionData))};

    return FromSink;
  }());

  var maxSafeInteger = Math.pow(2, 53) - 1;

  function StringIterable(str) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[87729:87751]", functionData => eval(functionData))}

  StringIterable.prototype[$iterator$] = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[87806:87851]", functionData => eval(functionData))};

  function StringIterator(str) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[87885:87948]", functionData => eval(functionData))}

  StringIterator.prototype[$iterator$] = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88003:88025]", functionData => eval(functionData))};

  StringIterator.prototype.next = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88074:88178]", functionData => eval(functionData))};

  function ArrayIterable(a) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88209:88231]", functionData => eval(functionData))}

  ArrayIterable.prototype[$iterator$] = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88285:88329]", functionData => eval(functionData))};

  function ArrayIterator(a) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88360:88426]", functionData => eval(functionData))}

  ArrayIterator.prototype[$iterator$] = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88480:88502]", functionData => eval(functionData))};

  ArrayIterator.prototype.next = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88550:88647]", functionData => eval(functionData))};

  function numberIsFinite(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88683:88750]", functionData => eval(functionData))}

  function isNan(n) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88772:88797]", functionData => eval(functionData))}

  function getIterable(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[88825:89172]", functionData => eval(functionData))}

  function sign(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[89197:89342]", functionData => eval(functionData))}

  function toLength(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[89367:89644]", functionData => eval(functionData))}

  /**
  * This method creates a new Observable sequence from an array-like or iterable object.
  * @param {Any} arrayLike An array-like or iterable object to convert to an Observable sequence.
  * @param {Function} [mapFn] Map function to call on every element of the array.
  * @param {Any} [thisArg] The context to use calling the mapFn if provided.
  * @param {Scheduler} [scheduler] Optional scheduler to use for scheduling.  If not provided, defaults to Scheduler.currentThread.
  */
  var observableFrom = Observable.from = function (iterable, mapFn, thisArg, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[90223:90625]", functionData => eval(functionData))}

  var FromArrayObservable = (function(__super__) {
    inherits(FromArrayObservable, __super__);
    function FromArrayObservable(args, scheduler) {
      this.args = args;
      this.scheduler = scheduler;
      __super__.call(this);
    }

    FromArrayObservable.prototype.subscribeCore = function (observer) {
      var sink = new FromArraySink(observer, this);
      return sink.run();
    };

    return FromArrayObservable;
  }(ObservableBase));

  function FromArraySink(observer, parent) {
    this.observer = observer;
    this.parent = parent;
  }

  FromArraySink.prototype.run = function () {
    var observer = this.observer, args = this.parent.args, len = args.length;
    function loopRecursive(i, recurse) {
      if (i < len) {
        observer.onNext(args[i]);
        recurse(i + 1);
      } else {
        observer.onCompleted();
      }
    }

    return this.parent.scheduler.scheduleRecursiveWithState(0, loopRecursive);
  };

  /**
  *  Converts an array to an observable sequence, using an optional scheduler to enumerate the array.
  * @deprecated use Observable.from or Observable.of
  * @param {Scheduler} [scheduler] Scheduler to run the enumeration of the input sequence on.
  * @returns {Observable} The observable sequence whose elements are pulled from the given enumerable sequence.
  */
  var observableFromArray = Observable.fromArray = function (array, scheduler) {
    isScheduler(scheduler) || (scheduler = currentThreadScheduler);
    return new FromArrayObservable(array, scheduler)
  };

  /**
   *  Generates an observable sequence by running a state-driven loop producing the sequence's elements, using the specified scheduler to send out observer messages.
   *
   * @example
   *  var res = Rx.Observable.generate(0, function (x) { return x < 10; }, function (x) { return x + 1; }, function (x) { return x; });
   *  var res = Rx.Observable.generate(0, function (x) { return x < 10; }, function (x) { return x + 1; }, function (x) { return x; }, Rx.Scheduler.timeout);
   * @param {Mixed} initialState Initial state.
   * @param {Function} condition Condition to terminate generation (upon returning false).
   * @param {Function} iterate Iteration step function.
   * @param {Function} resultSelector Selector function for results produced in the sequence.
   * @param {Scheduler} [scheduler] Scheduler on which to run the generator loop. If not provided, defaults to Scheduler.currentThread.
   * @returns {Observable} The generated sequence.
   */
  Observable.generate = function (initialState, condition, iterate, resultSelector, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[93220:93926]", functionData => eval(functionData))};

  function observableOf (scheduler, array) {
    isScheduler(scheduler) || (scheduler = currentThreadScheduler);
    return new FromArrayObservable(array, scheduler);
  }

  /**
  *  This method creates a new Observable instance with a variable number of arguments, regardless of number or type of the arguments.
  * @returns {Observable} The observable sequence whose elements are pulled from the given arguments.
  */
  Observable.of = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[94379:94566]", functionData => eval(functionData))};

  /**
  *  This method creates a new Observable instance with a variable number of arguments, regardless of number or type of the arguments.
  * @param {Scheduler} scheduler A scheduler to use for scheduling the arguments.
  * @returns {Observable} The observable sequence whose elements are pulled from the given arguments.
  */
  Observable.ofWithScheduler = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[94951:95133]", functionData => eval(functionData))};

  /**
   * Creates an Observable sequence from changes to an array using Array.observe.
   * @param {Array} array An array to observe changes.
   * @returns {Observable} An observable sequence containing changes to an array from Array.observe.
   */
  Observable.ofArrayChanges = function(array) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[95432:96039]", functionData => eval(functionData))};

  /**
   * Creates an Observable sequence from changes to an object using Object.observe.
   * @param {Object} obj An object to observe changes.
   * @returns {Observable} An observable sequence containing changes to an object from Object.observe.
   */
  Observable.ofObjectChanges = function(obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[96341:96936]", functionData => eval(functionData))};

  var NeverObservable = (function(__super__) {
    inherits(NeverObservable, __super__);
    function NeverObservable() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[97059:97094]", functionData => eval(functionData))}

    NeverObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[97162:97199]", functionData => eval(functionData))};

    return NeverObservable;
  }(ObservableBase));

  /**
   * Returns a non-terminating observable sequence, which can be used to denote an infinite duration (e.g. when using reactive joins).
   * @returns {Observable} An observable sequence whose observers will never get called.
   */
  var observableNever = Observable.never = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[97544:97583]", functionData => eval(functionData))};

  var PairsObservable = (function(__super__) {
    inherits(PairsObservable, __super__);
    function PairsObservable(obj, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[97720:97847]", functionData => eval(functionData))}

    PairsObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[97915:97995]", functionData => eval(functionData))};

    return PairsObservable;
  }(ObservableBase));

  function PairsSink(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[98088:98149]", functionData => eval(functionData))}

  PairsSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[98191:98593]", functionData => eval(functionData))};

  /**
   * Convert an object into an observable sequence of [key, value] pairs.
   * @param {Object} obj The object to inspect.
   * @param {Scheduler} [scheduler] Scheduler to run the enumeration of the input sequence on.
   * @returns {Observable} An observable sequence of [key, value] pairs from the object.
   */
  Observable.pairs = function (obj, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[98961:99069]", functionData => eval(functionData))};

    var RangeObservable = (function(__super__) {
    inherits(RangeObservable, __super__);
    function RangeObservable(start, count, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[99217:99343]", functionData => eval(functionData))}

    RangeObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[99411:99491]", functionData => eval(functionData))};

    return RangeObservable;
  }(ObservableBase));

  var RangeSink = (function () {
    function RangeSink(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[99619:99686]", functionData => eval(functionData))}

    RangeSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[99730:100115]", functionData => eval(functionData))};

    return RangeSink;
  }());

  /**
  *  Generates an observable sequence of integral numbers within a specified range, using the specified scheduler to send out observer messages.
  * @param {Number} start The value of the first integer in the sequence.
  * @param {Number} count The number of sequential integers to generate.
  * @param {Scheduler} [scheduler] Scheduler to run the generator loop on. If not specified, defaults to Scheduler.currentThread.
  * @returns {Observable} An observable sequence that contains a range of sequential integral numbers.
  */
  Observable.range = function (start, count, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[100741:100871]", functionData => eval(functionData))};

  var RepeatObservable = (function(__super__) {
    inherits(RepeatObservable, __super__);
    function RepeatObservable(value, repeatCount, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[101026:101186]", functionData => eval(functionData))}

    RepeatObservable.prototype.subscribeCore = function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[101255:101336]", functionData => eval(functionData))};

    return RepeatObservable;
  }(ObservableBase));

  function RepeatSink(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[101431:101492]", functionData => eval(functionData))}

  RepeatSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[101535:101915]", functionData => eval(functionData))};

  /**
   *  Generates an observable sequence that repeats the given element the specified number of times, using the specified scheduler to send out observer messages.
   * @param {Mixed} value Element to repeat.
   * @param {Number} repeatCount [Optiona] Number of times to repeat the element. If not specified, repeats indefinitely.
   * @param {Scheduler} scheduler Scheduler to run the producer loop on. If not specified, defaults to Scheduler.immediate.
   * @returns {Observable} An observable sequence that repeats the given element the specified number of times.
   */
  Observable.repeat = function (value, repeatCount, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[102558:102695]", functionData => eval(functionData))};

  var JustObservable = (function(__super__) {
    inherits(JustObservable, __super__);
    function JustObservable(value, scheduler) {
      this.value = value;
      this.scheduler = scheduler;
      __super__.call(this);
    }

    JustObservable.prototype.subscribeCore = function (observer) {
      var sink = new JustSink(observer, this);
      return sink.run();
    };

    function JustSink(observer, parent) {
      this.observer = observer;
      this.parent = parent;
    }

    function scheduleItem(s, state) {
      var value = state[0], observer = state[1];
      observer.onNext(value);
      observer.onCompleted();
    }

    JustSink.prototype.run = function () {
      return this.parent.scheduler.scheduleWithState([this.parent.value, this.observer], scheduleItem);
    };

    return JustObservable;
  }(ObservableBase));

  /**
   *  Returns an observable sequence that contains a single element, using the specified scheduler to send out observer messages.
   *  There is an alias called 'just' or browsers <IE9.
   * @param {Mixed} value Single element in the resulting observable sequence.
   * @param {Scheduler} scheduler Scheduler to send the single element on. If not specified, defaults to Scheduler.immediate.
   * @returns {Observable} An observable sequence containing the single specified element.
   */
  var observableReturn = Observable['return'] = Observable.just = Observable.returnValue = function (value, scheduler) {
    isScheduler(scheduler) || (scheduler = immediateScheduler);
    return new JustObservable(value, scheduler);
  };

  var ThrowObservable = (function(__super__) {
    inherits(ThrowObservable, __super__);
    function ThrowObservable(error, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[104413:104508]", functionData => eval(functionData))}

    ThrowObservable.prototype.subscribeCore = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[104569:104642]", functionData => eval(functionData))};

    function ThrowSink(o, p) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[104674:104717]", functionData => eval(functionData))}

    function scheduleItem(s, state) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[104755:104820]", functionData => eval(functionData))}

    ThrowSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[104864:104958]", functionData => eval(functionData))};

    return ThrowObservable;
  }(ObservableBase));

  /**
   *  Returns an observable sequence that terminates with an exception, using the specified scheduler to send out the single onError message.
   *  There is an alias to this method called 'throwError' for browsers <IE9.
   * @param {Mixed} error An object used for the sequence's termination.
   * @param {Scheduler} scheduler Scheduler to send the exceptional termination call on. If not specified, defaults to Scheduler.immediate.
   * @returns {Observable} The observable sequence that terminates exceptionally with the specified exception object.
   */
  var observableThrow = Observable['throw'] = Observable.throwError = Observable.throwException = function (error, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[105701:105820]", functionData => eval(functionData))};

  /**
   * Constructs an observable sequence that depends on a resource object, whose lifetime is tied to the resulting observable sequence's lifetime.
   * @param {Function} resourceFactory Factory function to obtain a resource object.
   * @param {Function} observableFactory Factory function to obtain an observable sequence that depends on the obtained resource.
   * @returns {Observable} An observable sequence whose lifetime controls the lifetime of the dependent resource object.
   */
  Observable.using = function (resourceFactory, observableFactory) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[106384:106867]", functionData => eval(functionData))};

  /**
   * Propagates the observable sequence or Promise that reacts first.
   * @param {Observable} rightSource Second observable sequence or Promise.
   * @returns {Observable} {Observable} An observable sequence that surfaces either of the given sequences, whichever reacted first.
   */
  observableProto.amb = function (rightSource) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[107208:108663]", functionData => eval(functionData))};

  /**
   * Propagates the observable sequence or Promise that reacts first.
   *
   * @example
   * var = Rx.Observable.amb(xs, ys, zs);
   * @returns {Observable} An observable sequence that surfaces any of the given sequences, whichever reacted first.
   */
  Observable.amb = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[108957:109375]", functionData => eval(functionData))};

  function observableCatchHandler(source, handler) {
    return new AnonymousObservable(function (o) {
      var d1 = new SingleAssignmentDisposable(), subscription = new SerialDisposable();
      subscription.setDisposable(d1);
      d1.setDisposable(source.subscribe(function (x) { o.onNext(x); }, function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[109691:110021]", functionData => eval(functionData))}, function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[110036:110057]", functionData => eval(functionData))}));

      return subscription;
    }, source);
  }

  /**
   * Continues an observable sequence that is terminated by an exception with the next observable sequence.
   * @example
   * 1 - xs.catchException(ys)
   * 2 - xs.catchException(function (ex) { return ys(ex); })
   * @param {Mixed} handlerOrSecond Exception handler function that returns an observable sequence given the error that occurred in the first sequence, or a second observable sequence used to produce results when an error occurred in the first sequence.
   * @returns {Observable} An observable sequence containing the first sequence's elements, followed by the elements of the handler sequence in case an exception occurred.
   */
  observableProto['catch'] = observableProto.catchError = observableProto.catchException = function (handlerOrSecond) {
    return typeof handlerOrSecond === 'function' ?
      observableCatchHandler(this, handlerOrSecond) :
      observableCatch([this, handlerOrSecond]);
  };

  /**
   * Continues an observable sequence that is terminated by an exception with the next observable sequence.
   * @param {Array | Arguments} args Arguments or an array to use as the next sequence if an error occurs.
   * @returns {Observable} An observable sequence containing elements from consecutive source sequences until a source sequence terminates successfully.
   */
  var observableCatch = Observable.catchError = Observable['catch'] = Observable.catchException = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[111531:111776]", functionData => eval(functionData))};

  /**
   * Merges the specified observable sequences into one observable sequence by using the selector function whenever any of the observable sequences or Promises produces an element.
   * This can be in the form of an argument list of observables or an array.
   *
   * @example
   * 1 - obs = observable.combineLatest(obs1, obs2, obs3, function (o1, o2, o3) { return o1 + o2 + o3; });
   * 2 - obs = observable.combineLatest([obs1, obs2, obs3], function (o1, o2, o3) { return o1 + o2 + o3; });
   * @returns {Observable} An observable sequence containing the result of combining elements of the sources using the specified result selector function.
   */
  observableProto.combineLatest = function () {
    var len = arguments.length, args = new Array(len);
    for(var i = 0; i < len; i++) { args[i] = arguments[i]; }
    if (Array.isArray(args[0])) {
      args[0].unshift(this);
    } else {
      args.unshift(this);
    }
    return combineLatest.apply(this, args);
  };

  /**
   * Merges the specified observable sequences into one observable sequence by using the selector function whenever any of the observable sequences or Promises produces an element.
   *
   * @example
   * 1 - obs = Rx.Observable.combineLatest(obs1, obs2, obs3, function (o1, o2, o3) { return o1 + o2 + o3; });
   * 2 - obs = Rx.Observable.combineLatest([obs1, obs2, obs3], function (o1, o2, o3) { return o1 + o2 + o3; });
   * @returns {Observable} An observable sequence containing the result of combining elements of the sources using the specified result selector function.
   */
  var combineLatest = Observable.combineLatest = function () {
    var len = arguments.length, args = new Array(len);
    for(var i = 0; i < len; i++) { args[i] = arguments[i]; }
    var resultSelector = args.pop();
    Array.isArray(args[0]) && (args = args[0]);

    return new AnonymousObservable(function (o) {
      var n = args.length,
        falseFactory = function () { return false; },
        hasValue = arrayInitialize(n, falseFactory),
        hasValueAll = false,
        isDone = arrayInitialize(n, falseFactory),
        values = new Array(n);

      function next(i) {
        hasValue[i] = true;
        if (hasValueAll || (hasValueAll = hasValue.every(identity))) {
          try {
            var res = resultSelector.apply(null, values);
          } catch (e) {
            return o.onError(e);
          }
          o.onNext(res);
        } else if (isDone.filter(function (x, j) { return j !== i; }).every(identity)) {
          o.onCompleted();
        }
      }

      function done (i) {
        isDone[i] = true;
        isDone.every(identity) && o.onCompleted();
      }

      var subscriptions = new Array(n);
      for (var idx = 0; idx < n; idx++) {
        (function (i) {
          var source = args[i], sad = new SingleAssignmentDisposable();
          isPromise(source) && (source = observableFromPromise(source));
          sad.setDisposable(source.subscribe(function (x) {
              values[i] = x;
              next(i);
            },
            function(e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[114852:114869]", functionData => eval(functionData))},
            function () { done(i); }
          ));
          subscriptions[i] = sad;
        }(idx));
      }

      return new CompositeDisposable(subscriptions);
    }, this);
  };

  /**
   * Concatenates all the observable sequences.  This takes in either an array or variable arguments to concatenate.
   * @returns {Observable} An observable sequence that contains the elements of each given sequence, in sequential order.
   */
  observableProto.concat = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[115345:115518]", functionData => eval(functionData))};

	var ConcatObservable = (function(__super__) {
		inherits(ConcatObservable, __super__);
		function ConcatObservable(sources) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[115646:115703]", functionData => eval(functionData))}
		
		ConcatObservable.prototype.subscribeCore = function(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[115764:115844]", functionData => eval(functionData))};
    
    function ConcatSink(sources, o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[115887:115942]", functionData => eval(functionData))}
    ConcatSink.prototype.run = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[115986:116880]", functionData => eval(functionData))};
    
		
		return ConcatObservable;
	}(ObservableBase));
  
  /**
   * Concatenates all the observable sequences.
   * @param {Array | Arguments} args Arguments or an array to concat to the observable sequence.
   * @returns {Observable} An observable sequence that contains the elements of each given sequence, in sequential order.
   */
  var observableConcat = Observable.concat = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[117277:117549]", functionData => eval(functionData))};

  /**
   * Concatenates an observable sequence of observable sequences.
   * @returns {Observable} An observable sequence that contains the elements of each observed inner sequence, in sequential order.
   */
  observableProto.concatAll = observableProto.concatObservable = function () {
    return this.merge(1);
  };

  var MergeObservable = (function (__super__) {
    inherits(MergeObservable, __super__);

    function MergeObservable(source, maxConcurrent) {
      this.source = source;
      this.maxConcurrent = maxConcurrent;
      __super__.call(this);
    }

    MergeObservable.prototype.subscribeCore = function(observer) {
      var g = new CompositeDisposable();
      g.add(this.source.subscribe(new MergeObserver(observer, this.maxConcurrent, g)));
      return g;
    };

    return MergeObservable;

  }(ObservableBase));

  var MergeObserver = (function () {
    function MergeObserver(o, max, g) {
      this.o = o;
      this.max = max;
      this.g = g;
      this.done = false;
      this.q = [];
      this.activeCount = 0;
      this.isStopped = false;
    }
    MergeObserver.prototype.handleSubscribe = function (xs) {
      var sad = new SingleAssignmentDisposable();
      this.g.add(sad);
      isPromise(xs) && (xs = observableFromPromise(xs));
      sad.setDisposable(xs.subscribe(new InnerObserver(this, sad)));
    };
    MergeObserver.prototype.onNext = function (innerSource) {
      if (this.isStopped) { return; }
        if(this.activeCount < this.max) {
          this.activeCount++;
          this.handleSubscribe(innerSource);
        } else {
          this.q.push(innerSource);
        }
      };
      MergeObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[119247:119359]", functionData => eval(functionData))};
      MergeObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[119417:119586]", functionData => eval(functionData))};
      MergeObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[119639:119665]", functionData => eval(functionData))};
      MergeObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[119717:119875]", functionData => eval(functionData))};

      function InnerObserver(parent, sad) {
        this.parent = parent;
        this.sad = sad;
        this.isStopped = false;
      }
      InnerObserver.prototype.onNext = function (x) { if(!this.isStopped) { this.parent.o.onNext(x); } };
      InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[120175:120294]", functionData => eval(functionData))};
      InnerObserver.prototype.onCompleted = function () {
        if(!this.isStopped) {
          this.isStopped = true;
          var parent = this.parent;
          parent.g.remove(this.sad);
          if (parent.q.length > 0) {
            parent.handleSubscribe(parent.q.shift());
          } else {
            parent.activeCount--;
            parent.done && parent.activeCount === 0 && parent.o.onCompleted();
          }
        }
      };
      InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[120795:120821]", functionData => eval(functionData))};
      InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[120873:121038]", functionData => eval(functionData))};

      return MergeObserver;
  }());





  /**
  * Merges an observable sequence of observable sequences into an observable sequence, limiting the number of concurrent subscriptions to inner sequences.
  * Or merges two observable sequences into a single observable sequence.
  *
  * @example
  * 1 - merged = sources.merge(1);
  * 2 - merged = source.merge(otherSource);
  * @param {Mixed} [maxConcurrentOrOther] Maximum number of inner observable sequences being subscribed to concurrently or the second observable sequence.
  * @returns {Observable} The observable sequence that merges the elements of the inner sequences.
  */
  observableProto.merge = function (maxConcurrentOrOther) {
    return typeof maxConcurrentOrOther !== 'number' ?
      observableMerge(this, maxConcurrentOrOther) :
      new MergeObservable(this, maxConcurrentOrOther);
  };

  /**
   * Merges all the observable sequences into a single observable sequence.
   * The scheduler is optional and if not specified, the immediate scheduler is used.
   * @returns {Observable} The observable sequence that merges the elements of the observable sequences.
   */
  var observableMerge = Observable.merge = function () {
    var scheduler, sources = [], i, len = arguments.length;
    if (!arguments[0]) {
      scheduler = immediateScheduler;
      for(i = 1; i < len; i++) { sources.push(arguments[i]); }
    } else if (isScheduler(arguments[0])) {
      scheduler = arguments[0];
      for(i = 1; i < len; i++) { sources.push(arguments[i]); }
    } else {
      scheduler = immediateScheduler;
      for(i = 0; i < len; i++) { sources.push(arguments[i]); }
    }
    if (Array.isArray(sources[0])) {
      sources = sources[0];
    }
    return observableOf(scheduler, sources).mergeAll();
  };

  var MergeAllObservable = (function (__super__) {
    inherits(MergeAllObservable, __super__);

    function MergeAllObservable(source) {
      this.source = source;
      __super__.call(this);
    }

    MergeAllObservable.prototype.subscribeCore = function (observer) {
      var g = new CompositeDisposable(), m = new SingleAssignmentDisposable();
      g.add(m);
      m.setDisposable(this.source.subscribe(new MergeAllObserver(observer, g)));
      return g;
    };
    
    function MergeAllObserver(o, g) {
      this.o = o;
      this.g = g;
      this.isStopped = false;
      this.done = false;
    }
    MergeAllObserver.prototype.onNext = function(innerSource) {
      if(this.isStopped) { return; }
      var sad = new SingleAssignmentDisposable();
      this.g.add(sad);

      isPromise(innerSource) && (innerSource = observableFromPromise(innerSource));

      sad.setDisposable(innerSource.subscribe(new InnerObserver(this, this.g, sad)));
    };
    MergeAllObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[123832:123933]", functionData => eval(functionData))};
    MergeAllObserver.prototype.onCompleted = function () {
      if(!this.isStopped) {
        this.isStopped = true;
        this.done = true;
        this.g.length === 1 && this.o.onCompleted();
      }
    };
    MergeAllObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[124199:124225]", functionData => eval(functionData))};
    MergeAllObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[124278:124422]", functionData => eval(functionData))};

    function InnerObserver(parent, g, sad) {
      this.parent = parent;
      this.g = g;
      this.sad = sad;
      this.isStopped = false;
    }
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[124624:124677]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[124730:124838]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[124894:125109]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[125160:125186]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[125236:125387]", functionData => eval(functionData))};

    return MergeAllObservable;
  }(ObservableBase));

  /**
  * Merges an observable sequence of observable sequences into an observable sequence.
  * @returns {Observable} The observable sequence that merges the elements of the inner sequences.
  */
  observableProto.mergeAll = observableProto.mergeObservable = function () {
    return new MergeAllObservable(this);
  };

  var CompositeError = Rx.CompositeError = function(errors) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[125825:125997]", functionData => eval(functionData))}
  CompositeError.prototype = Error.prototype;

  /**
  * Flattens an Observable that emits Observables into one Observable, in a way that allows an Observer to
  * receive all successfully emitted items from all of the source Observables without being interrupted by
  * an error notification from one of them.
  *
  * This behaves like Observable.prototype.mergeAll except that if any of the merged Observables notify of an
  * error via the Observer's onError, mergeDelayError will refrain from propagating that
  * error notification until all of the merged Observables have finished emitting items.
  * @param {Array | Arguments} args Arguments or an array to merge.
  * @returns {Observable} an Observable that emits all of the items emitted by the Observables emitted by the Observable
  */
  Observable.mergeDelayError = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[126837:128622]", functionData => eval(functionData))};

  /**
   * Continues an observable sequence that is terminated normally or by an exception with the next observable sequence.
   * @param {Observable} second Second observable sequence used to produce results after the first sequence terminates.
   * @returns {Observable} An observable sequence that concatenates the first and second sequence, even if the first sequence terminates exceptionally.
   */
  observableProto.onErrorResumeNext = function (second) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[129085:129207]", functionData => eval(functionData))};

  /**
   * Continues an observable sequence that is terminated normally or by an exception with the next observable sequence.
   *
   * @example
   * 1 - res = Rx.Observable.onErrorResumeNext(xs, ys, zs);
   * 1 - res = Rx.Observable.onErrorResumeNext([xs, ys, zs]);
   * @returns {Observable} An observable sequence that concatenates the source sequences, even if a sequence terminates exceptionally.
   */
  var onErrorResumeNext = Observable.onErrorResumeNext = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[129687:130576]", functionData => eval(functionData))};

  /**
   * Returns the values from the source observable sequence only after the other observable sequence produces a value.
   * @param {Observable | Promise} other The observable sequence or Promise that triggers propagation of elements of the source sequence.
   * @returns {Observable} An observable sequence containing the elements of the source sequence starting from the point the other sequence triggered propagation.
   */
  observableProto.skipUntil = function (other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[131058:131828]", functionData => eval(functionData))};

  var SwitchObservable = (function(__super__) {
    inherits(SwitchObservable, __super__);
    function SwitchObservable(source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[131960:132023]", functionData => eval(functionData))}

    SwitchObservable.prototype.subscribeCore = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[132085:132239]", functionData => eval(functionData))};

    function SwitchObserver(o, inner) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[132280:132442]", functionData => eval(functionData))}
    SwitchObserver.prototype.onNext = function (innerSource) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[132504:132840]", functionData => eval(functionData))};
    SwitchObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[132894:132996]", functionData => eval(functionData))};
    SwitchObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133053:133206]", functionData => eval(functionData))};
    SwitchObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133259:133285]", functionData => eval(functionData))};
    SwitchObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133336:133478]", functionData => eval(functionData))};

    function InnerObserver(parent, id) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133520:133605]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133656:133766]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[133819:133962]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[134018:134254]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[134306:134332]", functionData => eval(functionData))}
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[134381:134530]", functionData => eval(functionData))};

    return SwitchObservable;
  }(ObservableBase));

  /**
  * Transforms an observable sequence of observable sequences into an observable sequence producing values only from the most recent observable sequence.
  * @returns {Observable} The observable sequence that at any point in time produces the elements of the most recent inner observable sequence that has been received.
  */
  observableProto['switch'] = observableProto.switchLatest = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[134990:135034]", functionData => eval(functionData))};

  var TakeUntilObservable = (function(__super__) {
    inherits(TakeUntilObservable, __super__);

    function TakeUntilObservable(source, other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135183:135322]", functionData => eval(functionData))}

    TakeUntilObservable.prototype.subscribeCore = function(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135386:135525]", functionData => eval(functionData))};

    function InnerObserver(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135558:135613]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135664:135737]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135792:135896]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[135952:136009]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[136060:136086]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[136136:136279]", functionData => eval(functionData))};

    return TakeUntilObservable;
  }(ObservableBase));

  /**
   * Returns the values from the source observable sequence until the other observable sequence produces a value.
   * @param {Observable | Promise} other Observable sequence or Promise that terminates propagation of elements of the source sequence.
   * @returns {Observable} An observable sequence containing the elements of the source sequence up to the point the other sequence interrupted further propagation.
   */
  observableProto.takeUntil = function (other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[136811:136865]", functionData => eval(functionData))};

  function falseFactory() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[136894:136911]", functionData => eval(functionData))}

  /**
   * Merges the specified observable sequences into one observable sequence by using the selector function only when the (first) source observable sequence produces an element.
   * @returns {Observable} An observable sequence containing the result of combining elements of the sources using the specified result selector function.
   */
  observableProto.withLatestFrom = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[137304:138818]", functionData => eval(functionData))};

  function zipArray(second, resultSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[138865:139373]", functionData => eval(functionData))}

  function falseFactory() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[139401:139418]", functionData => eval(functionData))}
  function emptyArrayFactory() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[139450:139464]", functionData => eval(functionData))}

  /**
   * Merges the specified observable sequences into one observable sequence by using the selector function whenever all of the observable sequences or an array have produced an element at a corresponding index.
   * The last element in the arguments must be a function to invoke for each series of elements at corresponding indexes in the args.
   * @returns {Observable} An observable sequence containing the result of combining elements of the args using the specified result selector function.
   */
  observableProto.zip = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[140011:141569]", functionData => eval(functionData))};

  /**
   * Merges the specified observable sequences into one observable sequence by using the selector function whenever all of the observable sequences have produced an element at a corresponding index.
   * @param arguments Observable sources.
   * @param {Function} resultSelector Function to invoke for each series of elements at corresponding indexes in the sources.
   * @returns {Observable} An observable sequence containing the result of combining elements of the sources using the specified result selector function.
   */
  Observable.zip = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[142137:142329]", functionData => eval(functionData))};

  function falseFactory() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[142358:142375]", functionData => eval(functionData))}
  function arrayFactory() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[142402:142416]", functionData => eval(functionData))}

  /**
   * Merges the specified observable sequences into one observable sequence by emitting a list with the elements of the observable sequences at corresponding indexes.
   * @param arguments Observable sources.
   * @returns {Observable} An observable sequence containing lists of elements at corresponding indexes.
   */
  Observable.zipArray = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[142780:144045]", functionData => eval(functionData))};

  /**
   *  Hides the identity of an observable sequence.
   * @returns {Observable} An observable sequence that hides the identity of the source sequence.
   */
  observableProto.asObservable = function () {
    var source = this;
    return new AnonymousObservable(function (o) { return source.subscribe(o); }, source);
  };

  /**
   *  Projects each element of an observable sequence into zero or more buffers which are produced based on element count information.
   *
   * @example
   *  var res = xs.bufferWithCount(10);
   *  var res = xs.bufferWithCount(10, 1);
   * @param {Number} count Length of each buffer.
   * @param {Number} [skip] Number of elements to skip between creation of consecutive buffers. If not provided, defaults to the count.
   * @returns {Observable} An observable sequence of buffers.
   */
  observableProto.bufferWithCount = function (count, skip) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[144932:145159]", functionData => eval(functionData))};

  /**
   * Dematerializes the explicit notification values of an observable sequence as implicit notifications.
   * @returns {Observable} An observable sequence exhibiting the behavior corresponding to the source sequence's notification values.
   */
  observableProto.dematerialize = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[145460:145686]", functionData => eval(functionData))};

  /**
   *  Returns an observable sequence that contains only distinct contiguous elements according to the keySelector and the comparer.
   *
   *  var obs = observable.distinctUntilChanged();
   *  var obs = observable.distinctUntilChanged(function (x) { return x.id; });
   *  var obs = observable.distinctUntilChanged(function (x) { return x.id; }, function (x, y) { return x === y; });
   *
   * @param {Function} [keySelector] A function to compute the comparison key for each element. If not provided, it projects the value.
   * @param {Function} [comparer] Equality comparer for computed key values. If not provided, defaults to an equality comparer function.
   * @returns {Observable} An observable sequence only containing the distinct contiguous elements, based on a computed key value, from the source sequence.
   */
  observableProto.distinctUntilChanged = function (keySelector, comparer) {
    var source = this;
    comparer || (comparer = defaultComparer);
    return new AnonymousObservable(function (o) {
      var hasCurrentKey = false, currentKey;
      return source.subscribe(function (value) {
        var key = value;
        if (keySelector) {
          key = tryCatch(keySelector)(value);
          if (key === errorObj) { return o.onError(key.e); }
        }
        if (hasCurrentKey) {
          var comparerEquals = tryCatch(comparer)(currentKey, key);
          if (comparerEquals === errorObj) { return o.onError(comparerEquals.e); }
        }
        if (!hasCurrentKey || !comparerEquals) {
          hasCurrentKey = true;
          currentKey = key;
          o.onNext(value);
        }
      }, function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[147337:147354]", functionData => eval(functionData))}, function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[147368:147388]", functionData => eval(functionData))});
    }, this);
  };

  var TapObservable = (function(__super__) {
    inherits(TapObservable,__super__);
    function TapObservable(source, observerOrOnNext, onError, onCompleted) {
      this.source = source;
      this.t = !observerOrOnNext || isFunction(observerOrOnNext) ?
        observerCreate(observerOrOnNext || noop, onError || noop, onCompleted || noop) :
        observerOrOnNext;
      __super__.call(this);
    }

    TapObservable.prototype.subscribeCore = function(o) {
      return this.source.subscribe(new InnerObserver(o, this.t));
    };

    function InnerObserver(o, t) {
      this.o = o;
      this.t = t;
      this.isStopped = false;
    }
    InnerObserver.prototype.onNext = function(x) {
      if (this.isStopped) { return; }
      var res = tryCatch(this.t.onNext).call(this.t, x);
      if (res === errorObj) { this.o.onError(res.e); }
      this.o.onNext(x);
    };
    InnerObserver.prototype.onError = function(err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[148340:148570]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[148625:148855]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[148906:148932]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[148982:149125]", functionData => eval(functionData))};

    return TapObservable;
  }(ObservableBase));

  /**
  *  Invokes an action for each element in the observable sequence and invokes an action upon graceful or exceptional termination of the observable sequence.
  *  This method can be used for debugging, logging, etc. of query behavior by intercepting the message stream to run arbitrary actions for messages on the pipeline.
  * @param {Function | Observer} observerOrOnNext Action to invoke for each element in the observable sequence or an o.
  * @param {Function} [onError]  Action to invoke upon exceptional termination of the observable sequence. Used if only the observerOrOnNext parameter is also a function.
  * @param {Function} [onCompleted]  Action to invoke upon graceful termination of the observable sequence. Used if only the observerOrOnNext parameter is also a function.
  * @returns {Observable} The source sequence with the side-effecting behavior applied.
  */
  observableProto['do'] = observableProto.tap = observableProto.doAction = function (observerOrOnNext, onError, onCompleted) {
    return new TapObservable(this, observerOrOnNext, onError, onCompleted);
  };

  /**
  *  Invokes an action for each element in the observable sequence.
  *  This method can be used for debugging, logging, etc. of query behavior by intercepting the message stream to run arbitrary actions for messages on the pipeline.
  * @param {Function} onNext Action to invoke for each element in the observable sequence.
  * @param {Any} [thisArg] Object to use as this when executing callback.
  * @returns {Observable} The source sequence with the side-effecting behavior applied.
  */
  observableProto.doOnNext = observableProto.tapOnNext = function (onNext, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[150854:150965]", functionData => eval(functionData))};

  /**
  *  Invokes an action upon exceptional termination of the observable sequence.
  *  This method can be used for debugging, logging, etc. of query behavior by intercepting the message stream to run arbitrary actions for messages on the pipeline.
  * @param {Function} onError Action to invoke upon exceptional termination of the observable sequence.
  * @param {Any} [thisArg] Object to use as this when executing callback.
  * @returns {Observable} The source sequence with the side-effecting behavior applied.
  */
  observableProto.doOnError = observableProto.tapOnError = function (onError, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[151578:151697]", functionData => eval(functionData))};

  /**
  *  Invokes an action upon graceful termination of the observable sequence.
  *  This method can be used for debugging, logging, etc. of query behavior by intercepting the message stream to run arbitrary actions for messages on the pipeline.
  * @param {Function} onCompleted Action to invoke upon graceful termination of the observable sequence.
  * @param {Any} [thisArg] Object to use as this when executing callback.
  * @returns {Observable} The source sequence with the side-effecting behavior applied.
  */
  observableProto.doOnCompleted = observableProto.tapOnCompleted = function (onCompleted, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[152320:152449]", functionData => eval(functionData))};

  /**
   *  Invokes a specified action after the source observable sequence terminates gracefully or exceptionally.
   * @param {Function} finallyAction Action to invoke after the source observable sequence terminates.
   * @returns {Observable} Source sequence with the action-invoking termination behavior applied.
   */
  observableProto['finally'] = observableProto.ensure = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[152849:153291]", functionData => eval(functionData))};

  /**
   * @deprecated use #finally or #ensure instead.
   */
  observableProto.finallyAction = function (action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[153408:153500]", functionData => eval(functionData))};

  var IgnoreElementsObservable = (function(__super__) {
    inherits(IgnoreElementsObservable, __super__);

    function IgnoreElementsObservable(source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[153657:153720]", functionData => eval(functionData))}

    IgnoreElementsObservable.prototype.subscribeCore = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[153790:153855]", functionData => eval(functionData))};

    function InnerObserver(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[153888:153943]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = noop;
    InnerObserver.prototype.onError = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[154040:154143]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[154199:154303]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[154354:154380]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[154430:154581]", functionData => eval(functionData))};

    return IgnoreElementsObservable;
  }(ObservableBase));

  /**
   *  Ignores all elements in an observable sequence leaving only the termination messages.
   * @returns {Observable} An empty observable sequence that signals termination, successful or exceptional, of the source sequence.
   */
  observableProto.ignoreElements = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[154928:154980]", functionData => eval(functionData))};

  /**
   *  Materializes the implicit notifications of an observable sequence as explicit notification values.
   * @returns {Observable} An observable sequence containing the materialized notification values from the source sequence.
   */
  observableProto.materialize = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[155268:155710]", functionData => eval(functionData))};

  /**
   *  Repeats the observable sequence a specified number of times. If the repeat count is not specified, the sequence repeats indefinitely.
   * @param {Number} [repeatCount]  Number of times to repeat the sequence. If not provided, repeats the sequence indefinitely.
   * @returns {Observable} The observable sequence producing the elements of the given sequence repeatedly.
   */
  observableProto.repeat = function (repeatCount) {
    return enumerableRepeat(this, repeatCount).concat();
  };

  /**
   *  Repeats the source observable sequence the specified number of times or until it successfully terminates. If the retry count is not specified, it retries indefinitely.
   *  Note if you encounter an error and want it to retry once, then you must use .retry(2);
   *
   * @example
   *  var res = retried = retry.repeat();
   *  var res = retried = retry.repeat(2);
   * @param {Number} [retryCount]  Number of times to retry the sequence. If not provided, retry the sequence indefinitely.
   * @returns {Observable} An observable sequence producing the elements of the given sequence repeatedly until it terminates successfully.
   */
  observableProto.retry = function (retryCount) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[156911:156976]", functionData => eval(functionData))};

  /**
   *  Repeats the source observable sequence upon error each time the notifier emits or until it successfully terminates. 
   *  if the notifier completes, the observable sequence completes.
   *
   * @example
   *  var timer = Observable.timer(500);
   *  var source = observable.retryWhen(timer);
   * @param {Observable} [notifier] An observable that triggers the retries or completes the observable with onNext or onCompleted respectively.
   * @returns {Observable} An observable sequence producing the elements of the given sequence repeatedly until it terminates successfully.
   */
  observableProto.retryWhen = function (notifier) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[157625:157690]", functionData => eval(functionData))};
  var ScanObservable = (function(__super__) {
    inherits(ScanObservable, __super__);
    function ScanObservable(source, accumulator, hasSeed, seed) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[157843:157998]", functionData => eval(functionData))}

    ScanObservable.prototype.subscribeCore = function(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[158064:158140]", functionData => eval(functionData))};

    return ScanObservable;
  }(ObservableBase));

  function ScanObserver(observer, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[158235:158496]", functionData => eval(functionData))}
  ScanObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[158544:158990]", functionData => eval(functionData))};
  ScanObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[159040:159140]", functionData => eval(functionData))};
  ScanObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[159193:159368]", functionData => eval(functionData))};
  ScanObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[159416:159442]", functionData => eval(functionData))};
  ScanObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[159489:159625]", functionData => eval(functionData))};

  /**
  *  Applies an accumulator function over an observable sequence and returns each intermediate result. The optional seed value is used as the initial accumulator value.
  *  For aggregation behavior with no intermediate results, see Observable.aggregate.
  * @param {Mixed} [seed] The initial accumulator value.
  * @param {Function} accumulator An accumulator function to be invoked on each element.
  * @returns {Observable} An observable sequence containing the accumulated values.
  */
  observableProto.scan = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[160161:160460]", functionData => eval(functionData))};

  /**
   *  Bypasses a specified number of elements at the end of an observable sequence.
   * @description
   *  This operator accumulates a queue with a length enough to store the first `count` elements. As more elements are
   *  received, elements are taken from the front of the queue and produced on the result sequence. This causes elements to be delayed.
   * @param count Number of elements to bypass at the end of the source sequence.
   * @returns {Observable} An observable sequence containing the source sequence elements except for the bypassed ones at the end.
   */
  observableProto.skipLast = function (count) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[161091:161452]", functionData => eval(functionData))};

  /**
   *  Prepends a sequence of values to an observable sequence with an optional scheduler and an argument list of values to prepend.
   *  @example
   *  var res = source.startWith(1, 2, 3);
   *  var res = source.startWith(Rx.Scheduler.timeout, 1, 2, 3);
   * @param {Arguments} args The specified values to prepend to the observable sequence
   * @returns {Observable} The source sequence prepended with the specified values.
   */
  observableProto.startWith = function () {
    var values, scheduler, start = 0;
    if (!!arguments.length && isScheduler(arguments[0])) {
      scheduler = arguments[0];
      start = 1;
    } else {
      scheduler = immediateScheduler;
    }
    for(var args = [], i = start, len = arguments.length; i < len; i++) { args.push(arguments[i]); }
    return enumerableOf([observableFromArray(args, scheduler), this]).concat();
  };

  /**
   *  Returns a specified number of contiguous elements from the end of an observable sequence.
   * @description
   *  This operator accumulates a buffer with a length enough to store elements count elements. Upon completion of
   *  the source sequence, this buffer is drained on the result sequence. This causes the elements to be delayed.
   * @param {Number} count Number of elements to take from the end of the source sequence.
   * @returns {Observable} An observable sequence containing the specified number of elements from the end of the source sequence.
   */
  observableProto.takeLast = function (count) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[162951:163370]", functionData => eval(functionData))};

  /**
   *  Returns an array with the specified number of contiguous elements from the end of an observable sequence.
   *
   * @description
   *  This operator accumulates a buffer with a length enough to store count elements. Upon completion of the
   *  source sequence, this buffer is produced on the result sequence.
   * @param {Number} count Number of elements to take from the end of the source sequence.
   * @returns {Observable} An observable sequence containing a single array with the specified number of elements from the end of the source sequence.
   */
  observableProto.takeLastBuffer = function (count) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[163995:164321]", functionData => eval(functionData))};

  /**
   *  Projects each element of an observable sequence into zero or more windows which are produced based on element count information.
   *
   *  var res = xs.windowWithCount(10);
   *  var res = xs.windowWithCount(10, 1);
   * @param {Number} count Length of each window.
   * @param {Number} [skip] Number of elements to skip between creation of consecutive windows. If not specified, defaults to the count.
   * @returns {Observable} An observable sequence of windows.
   */
  observableProto.windowWithCount = function (count, skip) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[164867:166164]", functionData => eval(functionData))};

  function concatMap(source, selector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[166215:166564]", functionData => eval(functionData))}

  /**
   *  One of the Following:
   *  Projects each element of an observable sequence to an observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   * @example
   *  var res = source.concatMap(function (x) { return Rx.Observable.range(0, x); });
   *  Or:
   *  Projects each element of an observable sequence to an observable sequence, invokes the result selector for the source element and each of the corresponding inner sequence's elements, and merges the results into one observable sequence.
   *
   *  var res = source.concatMap(function (x) { return Rx.Observable.range(0, x); }, function (x, y) { return x + y; });
   *  Or:
   *  Projects each element of the source observable sequence to the other observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   *  var res = source.concatMap(Rx.Observable.fromArray([1,2,3]));
   * @param {Function} selector A transform function to apply to each element or an observable sequence to project each element from the
   * source sequence onto which could be either an observable or Promise.
   * @param {Function} [resultSelector]  A transform function to apply to each element of the intermediate sequence.
   * @returns {Observable} An observable sequence whose elements are the result of invoking the one-to-many transform function collectionSelector on each element of the input sequence and then mapping each of those sequence elements and their corresponding source element to a result element.
   */
  observableProto.selectConcat = observableProto.concatMap = function (selector, resultSelector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[168226:168863]", functionData => eval(functionData))};

  /**
   * Projects each notification of an observable sequence to an observable sequence and concats the resulting observable sequences into one observable sequence.
   * @param {Function} onNext A transform function to apply to each element; the second parameter of the function represents the index of the source element.
   * @param {Function} onError A transform function to apply when an error occurs in the source sequence.
   * @param {Function} onCompleted A transform function to apply when the end of the source sequence is reached.
   * @param {Any} [thisArg] An optional "this" to use to invoke each transform.
   * @returns {Observable} An observable sequence whose elements are the result of invoking the one-to-many transform function corresponding to each notification in the input sequence.
   */
  observableProto.concatMapObserver = observableProto.selectConcatObserver = function(onNext, onError, onCompleted, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[169806:171147]", functionData => eval(functionData))};

    /**
     *  Returns the elements of the specified sequence or the specified value in a singleton sequence if the sequence is empty.
     *
     *  var res = obs = xs.defaultIfEmpty();
     *  2 - obs = xs.defaultIfEmpty(false);
     *
     * @memberOf Observable#
     * @param defaultValue The value to return if the sequence is empty. If not provided, this defaults to null.
     * @returns {Observable} An observable sequence that contains the specified default value if the source is empty; otherwise, the elements of the source itself.
     */
    observableProto.defaultIfEmpty = function (defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[171764:172238]", functionData => eval(functionData))};

  // Swap out for Array.findIndex
  function arrayIndexOfComparer(array, item, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[172330:172462]", functionData => eval(functionData))}

  function HashSet(comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[172493:172547]", functionData => eval(functionData))}
  HashSet.prototype.push = function(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[172591:172735]", functionData => eval(functionData))};

  /**
   *  Returns an observable sequence that contains only distinct elements according to the keySelector and the comparer.
   *  Usage of this operator should be considered carefully due to the maintenance of an internal lookup structure which can grow large.
   *
   * @example
   *  var res = obs = xs.distinct();
   *  2 - obs = xs.distinct(function (x) { return x.id; });
   *  2 - obs = xs.distinct(function (x) { return x.id; }, function (a,b) { return a === b; });
   * @param {Function} [keySelector]  A function to compute the comparison key for each element.
   * @param {Function} [comparer]  Used to compare items in the collection.
   * @returns {Observable} An observable sequence only containing the distinct elements, based on a computed key value, from the source sequence.
   */
  observableProto.distinct = function (keySelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[173601:174142]", functionData => eval(functionData))};

  /**
   *  Groups the elements of an observable sequence according to a specified key selector function and comparer and selects the resulting elements by using a specified function.
   *
   * @example
   *  var res = observable.groupBy(function (x) { return x.id; });
   *  2 - observable.groupBy(function (x) { return x.id; }), function (x) { return x.name; });
   *  3 - observable.groupBy(function (x) { return x.id; }), function (x) { return x.name; }, function (x) { return x.toString(); });
   * @param {Function} keySelector A function to extract the key for each element.
   * @param {Function} [elementSelector]  A function to map each source element to an element in an observable group.
   * @param {Function} [comparer] Used to determine whether the objects are equal.
   * @returns {Observable} A sequence of observable groups, each of which corresponds to a unique key value, containing all elements that share that same key value.
   */
  observableProto.groupBy = function (keySelector, elementSelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[175177:175269]", functionData => eval(functionData))};

    /**
     *  Groups the elements of an observable sequence according to a specified key selector function.
     *  A duration selector function is used to control the lifetime of groups. When a group expires, it receives an OnCompleted notification. When a new element with the same
     *  key value as a reclaimed group occurs, the group will be reborn with a new lifetime request.
     *
     * @example
     *  var res = observable.groupByUntil(function (x) { return x.id; }, null,  function () { return Rx.Observable.never(); });
     *  2 - observable.groupBy(function (x) { return x.id; }), function (x) { return x.name; },  function () { return Rx.Observable.never(); });
     *  3 - observable.groupBy(function (x) { return x.id; }), function (x) { return x.name; },  function () { return Rx.Observable.never(); }, function (x) { return x.toString(); });
     * @param {Function} keySelector A function to extract the key for each element.
     * @param {Function} durationSelector A function to signal the expiration of a group.
     * @param {Function} [comparer] Used to compare objects. When not specified, the default comparer is used.
     * @returns {Observable}
     *  A sequence of observable groups, each of which corresponds to a unique key value, containing all elements that share that same key value.
     *  If a group's lifetime expires, a new group with the same key value can be created once an element with such a key value is encoutered.
     *
     */
    observableProto.groupByUntil = function (keySelector, elementSelector, durationSelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[176861:179323]", functionData => eval(functionData))};

  var MapObservable = (function (__super__) {
    inherits(MapObservable, __super__);

    function MapObservable(source, selector, thisArg) {
      this.source = source;
      this.selector = bindCallback(selector, thisArg, 3);
      __super__.call(this);
    }
    
    function innerMap(selector, self) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[179632:179733]", functionData => eval(functionData))}

    MapObservable.prototype.internalMap = function (selector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[179806:179893]", functionData => eval(functionData))};

    MapObservable.prototype.subscribeCore = function (o) {
      return this.source.subscribe(new InnerObserver(o, this.selector, this));
    };
    
    function InnerObserver(o, selector, source) {
      this.o = o;
      this.selector = selector;
      this.source = source;
      this.i = 0;
      this.isStopped = false;
    }
  
    InnerObserver.prototype.onNext = function(x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[180280:180506]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[180559:180638]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[180694:180776]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[180827:180853]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[180903:181049]", functionData => eval(functionData))};

    return MapObservable;

  }(ObservableBase));

  /**
  * Projects each element of an observable sequence into a new form by incorporating the element's index.
  * @param {Function} selector A transform function to apply to each source element; the second parameter of the function represents the index of the source element.
  * @param {Any} [thisArg] Object to use as this when executing callback.
  * @returns {Observable} An observable sequence whose elements are the result of invoking the transform function on each element of source.
  */
  observableProto.map = observableProto.select = function (selector, thisArg) {
    var selectorFn = typeof selector === 'function' ? selector : function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[181757:181777]", functionData => eval(functionData))};
    return this instanceof MapObservable ?
      this.internalMap(selectorFn, thisArg) :
      new MapObservable(this, selectorFn, thisArg);
  };

  /**
   * Retrieves the value of a specified nested property from all elements in
   * the Observable sequence.
   * @param {Arguments} arguments The nested properties to pluck.
   * @returns {Observable} Returns a new Observable sequence of property values.
   */
  observableProto.pluck = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[182230:182666]", functionData => eval(functionData))};

  function flatMap(source, selector, thisArg) {
    var selectorFunc = bindCallback(selector, thisArg, 3);
    return source.map(function (x, i) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[182814:183046]", functionData => eval(functionData))}).mergeAll();
  }

  /**
   *  One of the Following:
   *  Projects each element of an observable sequence to an observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   * @example
   *  var res = source.selectMany(function (x) { return Rx.Observable.range(0, x); });
   *  Or:
   *  Projects each element of an observable sequence to an observable sequence, invokes the result selector for the source element and each of the corresponding inner sequence's elements, and merges the results into one observable sequence.
   *
   *  var res = source.selectMany(function (x) { return Rx.Observable.range(0, x); }, function (x, y) { return x + y; });
   *  Or:
   *  Projects each element of the source observable sequence to the other observable sequence and merges the resulting observable sequences into one observable sequence.
   *
   *  var res = source.selectMany(Rx.Observable.fromArray([1,2,3]));
   * @param {Function} selector A transform function to apply to each element or an observable sequence to project each element from the source sequence onto which could be either an observable or Promise.
   * @param {Function} [resultSelector]  A transform function to apply to each element of the intermediate sequence.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence whose elements are the result of invoking the one-to-many transform function collectionSelector on each element of the input sequence and then mapping each of those sequence elements and their corresponding source element to a result element.
   */
  observableProto.selectMany = observableProto.flatMap = function (selector, resultSelector, thisArg) {
    if (isFunction(selector) && isFunction(resultSelector)) {
      return this.flatMap(function (x, i) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[184900:185283]", functionData => eval(functionData))}, thisArg);
    }
    return isFunction(selector) ?
      flatMap(this, selector, thisArg) :
      flatMap(this, function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[185408:185428]", functionData => eval(functionData))});
  };

  /**
   * Projects each notification of an observable sequence to an observable sequence and merges the resulting observable sequences into one observable sequence.
   * @param {Function} onNext A transform function to apply to each element; the second parameter of the function represents the index of the source element.
   * @param {Function} onError A transform function to apply when an error occurs in the source sequence.
   * @param {Function} onCompleted A transform function to apply when the end of the source sequence is reached.
   * @param {Any} [thisArg] An optional "this" to use to invoke each transform.
   * @returns {Observable} An observable sequence whose elements are the result of invoking the one-to-many transform function corresponding to each notification in the input sequence.
   */
  observableProto.flatMapObserver = observableProto.selectManyObserver = function (onNext, onError, onCompleted, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[186373:187567]", functionData => eval(functionData))};

  /**
   *  Projects each element of an observable sequence into a new sequence of observable sequences by incorporating the element's index and then
   *  transforms an observable sequence of observable sequences into an observable sequence producing values only from the most recent observable sequence.
   * @param {Function} selector A transform function to apply to each source element; the second parameter of the function represents the index of the source element.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence whose elements are the result of invoking the transform function on each element of source producing an Observable of Observable sequences
   *  and that at any point in time produces the elements of the most recent inner observable sequence that has been received.
   */
  observableProto.selectSwitch = observableProto.flatMapLatest = observableProto.switchMap = function (selector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[188562:188625]", functionData => eval(functionData))};

  var SkipObservable = (function(__super__) {
    inherits(SkipObservable, __super__);
    function SkipObservable(source, count) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[188758:188851]", functionData => eval(functionData))}
    
    SkipObservable.prototype.subscribeCore = function (o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[188915:188996]", functionData => eval(functionData))};
    
    function InnerObserver(o, c) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189036:189127]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189178:189316]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function(e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189368:189448]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189503:189586]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189637:189663]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function(e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[189712:189855]", functionData => eval(functionData))};
    
    return SkipObservable;
  }(ObservableBase));  
  
  /**
   * Bypasses a specified number of elements in an observable sequence and then returns the remaining elements.
   * @param {Number} count The number of elements to skip before returning the remaining elements.
   * @returns {Observable} An observable sequence that contains the elements that occur after the specified index in the input sequence.
   */
  observableProto.skip = function (count) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[190318:190427]", functionData => eval(functionData))};
  /**
   *  Bypasses elements in an observable sequence as long as a specified condition is true and then returns the remaining elements.
   *  The element's index is used in the logic of the predicate function.
   *
   *  var res = source.skipWhile(function (value) { return value < 10; });
   *  var res = source.skipWhile(function (value, index) { return value < 10 || index < 10; });
   * @param {Function} predicate A function to test each element for a condition; the second parameter of the function represents the index of the source element.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence that contains the elements from the input sequence starting at the first element in the linear series that does not pass the test specified by predicate.
   */
  observableProto.skipWhile = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[191325:191843]", functionData => eval(functionData))};

  /**
   *  Returns a specified number of contiguous elements from the start of an observable sequence, using the specified scheduler for the edge case of take(0).
   *
   *  var res = source.take(5);
   *  var res = source.take(0, Rx.Scheduler.timeout);
   * @param {Number} count The number of elements to return.
   * @param {Scheduler} [scheduler] Scheduler used to produce an OnCompleted message in case <paramref name="count count</paramref> is set to 0.
   * @returns {Observable} An observable sequence that contains the specified number of elements from the start of the input sequence.
   */
  observableProto.take = function (count, scheduler) {
    if (count < 0) { throw new ArgumentOutOfRangeError(); }
    if (count === 0) { return observableEmpty(scheduler); }
    var source = this;
    return new AnonymousObservable(function (o) {
      var remaining = count;
      return source.subscribe(function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[192768:192886]", functionData => eval(functionData))}, function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[192901:192918]", functionData => eval(functionData))}, function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[192932:192952]", functionData => eval(functionData))});
    }, source);
  };

  /**
   *  Returns elements from an observable sequence as long as a specified condition is true.
   *  The element's index is used in the logic of the predicate function.
   * @param {Function} predicate A function to test each element for a condition; the second parameter of the function represents the index of the source element.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence that contains the elements from the input sequence that occur before the element at which the test no longer passes.
   */
  observableProto.takeWhile = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[193621:194214]", functionData => eval(functionData))};

  var FilterObservable = (function (__super__) {
    inherits(FilterObservable, __super__);

    function FilterObservable(source, predicate, thisArg) {
      this.source = source;
      this.predicate = bindCallback(predicate, thisArg, 3);
      __super__.call(this);
    }

    FilterObservable.prototype.subscribeCore = function (o) {
      return this.source.subscribe(new InnerObserver(o, this.predicate, this));
    };
    
    function innerPredicate(predicate, self) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[194692:194799]", functionData => eval(functionData))}

    FilterObservable.prototype.internalFilter = function(predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[194878:194975]", functionData => eval(functionData))};
    
    function InnerObserver(o, predicate, source) {
      this.o = o;
      this.predicate = predicate;
      this.source = source;
      this.i = 0;
      this.isStopped = false;
    }
  
    InnerObserver.prototype.onNext = function(x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[195219:195471]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[195524:195603]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[195659:195741]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[195792:195818]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[195868:196011]", functionData => eval(functionData))};

    return FilterObservable;

  }(ObservableBase));

  /**
  *  Filters the elements of an observable sequence based on a predicate by incorporating the element's index.
  * @param {Function} predicate A function to test each source element for a condition; the second parameter of the function represents the index of the source element.
  * @param {Any} [thisArg] Object to use as this when executing callback.
  * @returns {Observable} An observable sequence that contains elements from the input sequence that satisfy the condition.
  */
  observableProto.filter = observableProto.where = function (predicate, thisArg) {
    return this instanceof FilterObservable ? this.internalFilter(predicate, thisArg) :
      new FilterObservable(this, predicate, thisArg);
  };

  function extremaBy(source, keySelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[196839:197687]", functionData => eval(functionData))}

  function firstOnly(x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[197713:197787]", functionData => eval(functionData))}

  /**
   * Applies an accumulator function over an observable sequence, returning the result of the aggregation as a single element in the result sequence. The specified seed value is used as the initial accumulator value.
   * For aggregation behavior with incremental intermediate results, see Observable.scan.
   * @deprecated Use #reduce instead
   * @param {Mixed} [seed] The initial accumulator value.
   * @param {Function} accumulator An accumulator function to be invoked on each element.
   * @returns {Observable} An observable sequence containing a single element with the final accumulator value.
   */
  observableProto.aggregate = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[198447:199491]", functionData => eval(functionData))};

  var ReduceObservable = (function(__super__) {
    inherits(ReduceObservable, __super__);
    function ReduceObservable(source, acc, hasSeed, seed) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[199643:199782]", functionData => eval(functionData))}

    ReduceObservable.prototype.subscribeCore = function(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[199850:199927]", functionData => eval(functionData))};

    function InnerObserver(o, parent) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[199968:200211]", functionData => eval(functionData))}
    InnerObserver.prototype.onNext = function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[200262:200653]", functionData => eval(functionData))};
    InnerObserver.prototype.onError = function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[200706:200788]", functionData => eval(functionData))};
    InnerObserver.prototype.onCompleted = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[200844:201147]", functionData => eval(functionData))};
    InnerObserver.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[201199:201225]", functionData => eval(functionData))};
    InnerObserver.prototype.fail = function(e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[201274:201417]", functionData => eval(functionData))};

    return ReduceObservable;
  }(ObservableBase));

  /**
  * Applies an accumulator function over an observable sequence, returning the result of the aggregation as a single element in the result sequence. The specified seed value is used as the initial accumulator value.
  * For aggregation behavior with incremental intermediate results, see Observable.scan.
  * @param {Function} accumulator An accumulator function to be invoked on each element.
  * @param {Any} [seed] The initial accumulator value.
  * @returns {Observable} An observable sequence containing a single element with the final accumulator value.
  */
  observableProto.reduce = function (accumulator) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[202093:202283]", functionData => eval(functionData))};

  /**
   * Determines whether any element of an observable sequence satisfies a condition if present, else if any items are in the sequence.
   * @param {Function} [predicate] A function to test each element for a condition.
   * @returns {Observable} An observable sequence containing a single element determining whether any elements in the source sequence pass the test in the specified predicate if given, else if any items are in the sequence.
   */
  observableProto.some = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[202796:203223]", functionData => eval(functionData))};

  /** @deprecated use #some instead */
  observableProto.any = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[203301:203383]", functionData => eval(functionData))};

  /**
   * Determines whether an observable sequence is empty.
   * @returns {Observable} An observable sequence containing a single element determining whether the source sequence is empty.
   */
  observableProto.isEmpty = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[203623:203660]", functionData => eval(functionData))};

  /**
   * Determines whether all elements of an observable sequence satisfy a condition.
   * @param {Function} [predicate] A function to test each element for a condition.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence containing a single element determining whether all elements in the source sequence pass the test in the specified predicate.
   */
  observableProto.every = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[204150:204244]", functionData => eval(functionData))};

  /** @deprecated use #every instead */
  observableProto.all = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[204323:204407]", functionData => eval(functionData))};

  /**
   * Determines whether an observable sequence includes a specified element with an optional equality comparer.
   * @param searchElement The value to locate in the source sequence.
   * @param {Number} [fromIndex] An equality comparer to compare elements.
   * @returns {Observable} An observable sequence containing a single element determining whether the source sequence includes an element that has the specified value from the given index.
   */
  observableProto.includes = function (searchElement, fromIndex) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[204933:205643]", functionData => eval(functionData))};

  /**
   * @deprecated use #includes instead.
   */
  observableProto.contains = function (searchElement, fromIndex) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[205763:205865]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence containing a value that represents how many elements in the specified observable sequence satisfy a condition if provided, else the count of items.
   * @example
   * res = source.count();
   * res = source.count(function (x) { return x > 3; });
   * @param {Function} [predicate]A function to test each element for a condition.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence containing a single element with a number that represents how many elements in the input sequence satisfy the condition in the predicate function if provided, else the count of items in the sequence.
   */
  observableProto.count = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[206626:206764]", functionData => eval(functionData))};

  /**
   * Returns the first index at which a given element can be found in the observable sequence, or -1 if it is not present.
   * @param {Any} searchElement Element to locate in the array.
   * @param {Number} [fromIndex] The index to start the search.  If not specified, defaults to 0.
   * @returns {Observable} And observable sequence containing the first index at which a given element can be found in the observable sequence, or -1 if it is not present.
   */
  observableProto.indexOf = function(searchElement, fromIndex) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[207299:207898]", functionData => eval(functionData))};

  /**
   * Computes the sum of a sequence of values that are obtained by invoking an optional transform function on each element of the input sequence, else if not specified computes the sum on each item in the sequence.
   * @param {Function} [selector] A transform function to apply to each element.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence containing a single element with the sum of the values in the source sequence.
   */
  observableProto.sum = function (keySelector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[208469:208640]", functionData => eval(functionData))};

  /**
   * Returns the elements in an observable sequence with the minimum key value according to the specified comparer.
   * @example
   * var res = source.minBy(function (x) { return x.value; });
   * var res = source.minBy(function (x) { return x.value; }, function (x, y) { return x - y; });
   * @param {Function} keySelector Key selector function.
   * @param {Function} [comparer] Comparer used to compare key values.
   * @returns {Observable} An observable sequence containing a list of zero or more elements that have a minimum key value.
   */
  observableProto.minBy = function (keySelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[209258:209402]", functionData => eval(functionData))};

  /**
   * Returns the minimum element in an observable sequence according to the optional comparer else a default greater than less than check.
   * @example
   * var res = source.min();
   * var res = source.min(function (x, y) { return x.value - y.value; });
   * @param {Function} [comparer] Comparer used to compare elements.
   * @returns {Observable} An observable sequence containing a single element with the minimum element in the source sequence.
   */
  observableProto.min = function (comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[209913:210004]", functionData => eval(functionData))};

  /**
   * Returns the elements in an observable sequence with the maximum  key value according to the specified comparer.
   * @example
   * var res = source.maxBy(function (x) { return x.value; });
   * var res = source.maxBy(function (x) { return x.value; }, function (x, y) { return x - y;; });
   * @param {Function} keySelector Key selector function.
   * @param {Function} [comparer]  Comparer used to compare key values.
   * @returns {Observable} An observable sequence containing a list of zero or more elements that have a maximum key value.
   */
  observableProto.maxBy = function (keySelector, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[210625:210730]", functionData => eval(functionData))};

  /**
   * Returns the maximum value in an observable sequence according to the specified comparer.
   * @example
   * var res = source.max();
   * var res = source.max(function (x, y) { return x.value - y.value; });
   * @param {Function} [comparer] Comparer used to compare elements.
   * @returns {Observable} An observable sequence containing a single element with the maximum element in the source sequence.
   */
  observableProto.max = function (comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[211196:211287]", functionData => eval(functionData))};

  /**
   * Computes the average of an observable sequence of values that are in the sequence or obtained by invoking a transform function on each element of the input sequence if present.
   * @param {Function} [selector] A transform function to apply to each element.
   * @param {Any} [thisArg] Object to use as this when executing callback.
   * @returns {Observable} An observable sequence containing a single element with the average of the sequence of values.
   */
  observableProto.average = function (keySelector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[211822:212206]", functionData => eval(functionData))};

  /**
   *  Determines whether two sequences are equal by comparing the elements pairwise using a specified equality comparer.
   *
   * @example
   * var res = res = source.sequenceEqual([1,2,3]);
   * var res = res = source.sequenceEqual([{ value: 42 }], function (x, y) { return x.value === y.value; });
   * 3 - res = source.sequenceEqual(Rx.Observable.returnValue(42));
   * 4 - res = source.sequenceEqual(Rx.Observable.returnValue({ value: 42 }), function (x, y) { return x.value === y.value; });
   * @param {Observable} second Second observable sequence or array to compare.
   * @param {Function} [comparer] Comparer used to compare elements of both sequences.
   * @returns {Observable} An observable sequence that contains a single element which indicates whether both sequences are of equal length and their corresponding elements are equal according to the specified equality comparer.
   */
  observableProto.sequenceEqual = function (second, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[213176:215232]", functionData => eval(functionData))};

  function elementAtOrDefault(source, index, hasDefault, defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[215306:215820]", functionData => eval(functionData))}

  /**
   * Returns the element at a specified index in a sequence.
   * @example
   * var res = source.elementAt(5);
   * @param {Number} index The zero-based index of the element to retrieve.
   * @returns {Observable} An observable sequence that produces the element at the specified position in the source sequence.
   */
  observableProto.elementAt =  function (index) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[216195:216251]", functionData => eval(functionData))};

  /**
   * Returns the element at a specified index in a sequence or a default value if the index is out of range.
   * @example
   * var res = source.elementAtOrDefault(5);
   * var res = source.elementAtOrDefault(5, 0);
   * @param {Number} index The zero-based index of the element to retrieve.
   * @param [defaultValue] The default value if the index is outside the bounds of the source sequence.
   * @returns {Observable} An observable sequence that produces the element at the specified position in the source sequence, or a default value if the index is outside the bounds of the source sequence.
   */
  observableProto.elementAtOrDefault = function (index, defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[216936:217005]", functionData => eval(functionData))};

  function singleOrDefaultAsync(source, hasDefault, defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[217074:217643]", functionData => eval(functionData))}

  /**
   * Returns the only element of an observable sequence that satisfies the condition in the optional predicate, and reports an exception if there is not exactly one element in the observable sequence.
   * @param {Function} [predicate] A predicate function to evaluate for elements in the source sequence.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the single element in the observable sequence that satisfies the condition in the predicate.
   */
  observableProto.single = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[218242:218384]", functionData => eval(functionData))};

  /**
   * Returns the only element of an observable sequence that matches the predicate, or a default value if no such element exists; this method reports an exception if there is more than one element in the observable sequence.
   * @example
   * var res = res = source.singleOrDefault();
   * var res = res = source.singleOrDefault(function (x) { return x === 42; });
   * res = source.singleOrDefault(function (x) { return x === 42; }, 0);
   * res = source.singleOrDefault(null, 0);
   * @memberOf Observable#
   * @param {Function} predicate A predicate function to evaluate for elements in the source sequence.
   * @param [defaultValue] The default value if the index is outside the bounds of the source sequence.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the single element in the observable sequence that satisfies the condition in the predicate, or a default value if no such element exists.
   */
  observableProto.singleOrDefault = function (predicate, defaultValue, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[219464:219647]", functionData => eval(functionData))};

  function firstOrDefaultAsync(source, hasDefault, defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[219715:220096]", functionData => eval(functionData))}

  /**
   * Returns the first element of an observable sequence that satisfies the condition in the predicate if present else the first item in the sequence.
   * @example
   * var res = res = source.first();
   * var res = res = source.first(function (x) { return x > 3; });
   * @param {Function} [predicate] A predicate function to evaluate for elements in the source sequence.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the first element in the observable sequence that satisfies the condition in the predicate if provided, else the first item in the sequence.
   */
  observableProto.first = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[220810:220925]", functionData => eval(functionData))};

  /**
   * Returns the first element of an observable sequence that satisfies the condition in the predicate, or a default value if no such element exists.
   * @param {Function} [predicate] A predicate function to evaluate for elements in the source sequence.
   * @param {Any} [defaultValue] The default value if no such element exists.  If not specified, defaults to null.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the first element in the observable sequence that satisfies the condition in the predicate, or a default value if no such element exists.
   */
  observableProto.firstOrDefault = function (predicate, defaultValue, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[221656:221802]", functionData => eval(functionData))};

  function lastOrDefaultAsync(source, hasDefault, defaultValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[221869:222307]", functionData => eval(functionData))}

  /**
   * Returns the last element of an observable sequence that satisfies the condition in the predicate if specified, else the last element.
   * @param {Function} [predicate] A predicate function to evaluate for elements in the source sequence.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the last element in the observable sequence that satisfies the condition in the predicate.
   */
  observableProto.last = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[222840:222953]", functionData => eval(functionData))};

  /**
   * Returns the last element of an observable sequence that satisfies the condition in the predicate, or a default value if no such element exists.
   * @param {Function} [predicate] A predicate function to evaluate for elements in the source sequence.
   * @param [defaultValue] The default value if no such element exists.  If not specified, defaults to null.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} Sequence containing the last element in the observable sequence that satisfies the condition in the predicate, or a default value if no such element exists.
   */
  observableProto.lastOrDefault = function (predicate, defaultValue, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[223675:223828]", functionData => eval(functionData))};

  function findValue (source, predicate, thisArg, yieldIndex) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[223893:224510]", functionData => eval(functionData))}

  /**
   * Searches for an element that matches the conditions defined by the specified predicate, and returns the first occurrence within the entire Observable sequence.
   * @param {Function} predicate The predicate that defines the conditions of the element to search for.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} An Observable sequence with the first element that matches the conditions defined by the specified predicate, if found; otherwise, undefined.
   */
  observableProto.find = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[225100:225160]", functionData => eval(functionData))};

  /**
   * Searches for an element that matches the conditions defined by the specified predicate, and returns
   * an Observable sequence with the zero-based index of the first occurrence within the entire Observable sequence.
   * @param {Function} predicate The predicate that defines the conditions of the element to search for.
   * @param {Any} [thisArg] Object to use as `this` when executing the predicate.
   * @returns {Observable} An Observable sequence with the zero-based index of the first occurrence of an element that matches the conditions defined by match, if found; otherwise, –1.
  */
  observableProto.findIndex = function (predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[225828:225887]", functionData => eval(functionData))};

  /**
   * Converts the observable sequence to a Set if it exists.
   * @returns {Observable} An observable sequence with a single value of a Set containing the values from the observable sequence.
   */
  observableProto.toSet = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[226132:226515]", functionData => eval(functionData))};

  /**
  * Converts the observable sequence to a Map if it exists.
  * @param {Function} keySelector A function which produces the key for the Map.
  * @param {Function} [elementSelector] An optional function which produces the element for the Map. If not present, defaults to the value from the observable sequence.
  * @returns {Observable} An observable sequence with a single value of a Map containing the values from the observable sequence.
  */
  observableProto.toMap = function (keySelector, elementSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[227035:227824]", functionData => eval(functionData))};

  var fnString = 'function',
      throwString = 'throw',
      isObject = Rx.internals.isObject;

  function toThunk(obj, ctx) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[227955:228437]", functionData => eval(functionData))}

  function objectToThunk(obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[228469:229495]", functionData => eval(functionData))}

  function observableToThunk(observable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[229538:229806]", functionData => eval(functionData))}

  function promiseToThunk(promise) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[229843:229952]", functionData => eval(functionData))}

  function isObservable(obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[229983:230041]", functionData => eval(functionData))}

  function isGeneratorFunction(obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[230079:230167]", functionData => eval(functionData))}

  function isGenerator(obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[230197:230290]", functionData => eval(functionData))}

  /*
   * Spawns a generator function which allows for Promises, Observable sequences, Arrays, Objects, Generators and functions.
   * @param {Function} The spawning function.
   * @returns {Function} a function which has a done continuation.
   */
  var observableSpawn = Rx.spawn = function (fn) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[230590:232539]", functionData => eval(functionData))};

  function handleError(err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[232570:232669]", functionData => eval(functionData))}

  /**
   * Invokes the specified function asynchronously on the specified scheduler, surfacing the result through an observable sequence.
   *
   * @example
   * var res = Rx.Observable.start(function () { console.log('hello'); });
   * var res = Rx.Observable.start(function () { console.log('hello'); }, Rx.Scheduler.timeout);
   * var res = Rx.Observable.start(function () { this.log('hello'); }, Rx.Scheduler.timeout, console);
   *
   * @param {Function} func Function to run asynchronously.
   * @param {Scheduler} [scheduler]  Scheduler to run the function on. If not specified, defaults to Scheduler.timeout.
   * @param [context]  The context for the func parameter to be executed.  If not specified, defaults to undefined.
   * @returns {Observable} An observable sequence exposing the function's result value, or an exception.
   *
   * Remarks
   * * The function is called immediately, not during the subscription of the resulting sequence.
   * * Multiple subscriptions to the resulting sequence can observe the function's result.
   */
  Observable.start = function (func, context, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[233779:233842]", functionData => eval(functionData))};

  /**
   * Converts the function into an asynchronous function. Each invocation of the resulting asynchronous function causes an invocation of the original synchronous function on the specified scheduler.
   * @param {Function} function Function to convert to an asynchronous function.
   * @param {Scheduler} [scheduler] Scheduler to run the function on. If not specified, defaults to Scheduler.timeout.
   * @param {Mixed} [context] The context for the func parameter to be executed.  If not specified, defaults to undefined.
   * @returns {Function} Asynchronous function.
   */
  var observableToAsync = Observable.toAsync = function (func, context, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[234510:234985]", functionData => eval(functionData))};

  /**
   * Converts a callback function to an observable sequence.
   *
   * @param {Function} function Function with a callback as the last parameter to convert to an Observable sequence.
   * @param {Mixed} [context] The context for the func parameter to be executed.  If not specified, defaults to undefined.
   * @param {Function} [selector] A selector which takes the arguments from the callback to produce a single item to yield on next.
   * @returns {Function} A function, when executed with the required parameters minus the callback, produces an Observable sequence with a single value of the arguments to the callback as an array.
   */
  Observable.fromCallback = function (func, context, selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[235699:236647]", functionData => eval(functionData))};

  /**
   * Converts a Node.js callback style function to an observable sequence.  This must be in function (err, ...) format.
   * @param {Function} func The function to call
   * @param {Mixed} [context] The context for the func parameter to be executed.  If not specified, defaults to undefined.
   * @param {Function} [selector] A selector which takes the arguments from the callback minus the error to produce a single item to yield on next.
   * @returns {Function} An async function which when applied, returns an observable sequence with the callback arguments as an array.
   */
  Observable.fromNodeCallback = function (func, context, selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[237304:238336]", functionData => eval(functionData))};

  function createListener (element, name, handler) {
    if (element.addEventListener) {
      element.addEventListener(name, handler, false);
      return disposableCreate(function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[238524:238592]", functionData => eval(functionData))});
    }
    throw new Error('No listener found');
  }

  function createEventListener (el, eventName, handler) {
    var disposables = new CompositeDisposable();

    // Asume NodeList or HTMLCollection
    var toStr = Object.prototype.toString;
    if (toStr.call(el) === '[object NodeList]' || toStr.call(el) === '[object HTMLCollection]') {
      for (var i = 0, len = el.length; i < len; i++) {
        disposables.add(createEventListener(el.item(i), eventName, handler));
      }
    } else if (el) {
      disposables.add(createListener(el, eventName, handler));
    }

    return disposables;
  }

  /**
   * Configuration option to determine whether to use native events only
   */
  Rx.config.useNativeEvents = false;

  /**
   * Creates an observable sequence by adding an event listener to the matching DOMElement or each item in the NodeList.
   *
   * @example
   *   var source = Rx.Observable.fromEvent(element, 'mouseup');
   *
   * @param {Object} element The DOMElement or NodeList to attach a listener.
   * @param {String} eventName The event name to attach the observable sequence.
   * @param {Function} [selector] A selector which takes the arguments from the event handler to produce a single item to yield on next.
   * @returns {Observable} An observable sequence of events from the specified element and the specified event.
   */
  Observable.fromEvent = function (element, eventName, selector) {
    // Node.js specific
    if (element.addListener) {
      return fromEventPattern(
        function (h) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[240125:240163]", functionData => eval(functionData))},
        function (h) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[240186:240227]", functionData => eval(functionData))},
        selector);
    }

    // Use only if non-native events are allowed
    if (!Rx.config.useNativeEvents) {
      // Handles jq, Angular.js, Zepto, Marionette, Ember.js
      if (typeof element.on === 'function' && typeof element.off === 'function') {
        return fromEventPattern(
          function (h) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[240542:240571]", functionData => eval(functionData))},
          function (h) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[240596:240626]", functionData => eval(functionData))},
          selector);
      }
    }
    return new AnonymousObservable(function (observer) {
      return createEventListener(
        element,
        eventName,
        function handler (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[240819:241082]", functionData => eval(functionData))});
    }).publish().refCount();
  };

  /**
   * Creates an observable sequence from an event emitter via an addHandler/removeHandler pair.
   * @param {Function} addHandler The function to add a handler to the emitter.
   * @param {Function} [removeHandler] The optional function to remove a handler from an emitter.
   * @param {Function} [selector] A selector which takes the arguments from the event handler to produce a single item to yield on next.
   * @returns {Observable} An observable sequence which wraps an event from an event emitter
   */
  var fromEventPattern = Observable.fromEventPattern = function (addHandler, removeHandler, selector) {
    return new AnonymousObservable(function (observer) {
      function innerHandler (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[241829:242067]", functionData => eval(functionData))}

      var returnValue = addHandler(innerHandler);
      return disposableCreate(function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[242161:242261]", functionData => eval(functionData))});
    }).publish().refCount();
  };

  /**
   * Invokes the asynchronous function, surfacing the result through an observable sequence.
   * @param {Function} functionAsync Asynchronous function which returns a Promise to run.
   * @returns {Observable} An observable sequence exposing the function's result value, or an exception.
   */
  Observable.startAsync = function (functionAsync) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[242651:242816]", functionData => eval(functionData))}

  var PausableObservable = (function (__super__) {

    inherits(PausableObservable, __super__);

    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[242949:243405]", functionData => eval(functionData))}

    function PausableObservable(source, pauser) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[243455:243733]", functionData => eval(functionData))}

    PausableObservable.prototype.pause = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[243788:243832]", functionData => eval(functionData))};

    PausableObservable.prototype.resume = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[243889:243932]", functionData => eval(functionData))};

    return PausableObservable;

  }(Observable));

  /**
   * Pauses the underlying observable sequence based upon the observable sequence which yields true/false.
   * @example
   * var pauser = new Rx.Subject();
   * var source = Rx.Observable.interval(100).pausable(pauser);
   * @param {Observable} pauser The observable sequence used to pause the underlying sequence.
   * @returns {Observable} The observable sequence which is paused based upon the pauser.
   */
  observableProto.pausable = function (pauser) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[244451:244505]", functionData => eval(functionData))};

  function combineLatestSource(source, subject, resultSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[244572:245811]", functionData => eval(functionData))}

  var PausableBufferedObservable = (function (__super__) {

    inherits(PausableBufferedObservable, __super__);

    function subscribe(o) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[245953:247185]", functionData => eval(functionData))}

    function PausableBufferedObservable(source, pauser) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[247243:247521]", functionData => eval(functionData))}

    PausableBufferedObservable.prototype.pause = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[247584:247628]", functionData => eval(functionData))};

    PausableBufferedObservable.prototype.resume = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[247693:247736]", functionData => eval(functionData))};

    return PausableBufferedObservable;

  }(Observable));

  /**
   * Pauses the underlying observable sequence based upon the observable sequence which yields true/false,
   * and yields the values that were buffered while paused.
   * @example
   * var pauser = new Rx.Subject();
   * var source = Rx.Observable.interval(100).pausableBuffered(pauser);
   * @param {Observable} pauser The observable sequence used to pause the underlying sequence.
   * @returns {Observable} The observable sequence which is paused based upon the pauser.
   */
  observableProto.pausableBuffered = function (subject) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[248340:248403]", functionData => eval(functionData))};

  var ControlledObservable = (function (__super__) {

    inherits(ControlledObservable, __super__);

    function subscribe (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[248542:248595]", functionData => eval(functionData))}

    function ControlledObservable (source, enableQueue, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[248664:248849]", functionData => eval(functionData))}

    ControlledObservable.prototype.request = function (numberOfItems) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[248921:249007]", functionData => eval(functionData))};

    return ControlledObservable;

  }(Observable));

  var ControlledSubject = (function (__super__) {

    function subscribe (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[249148:249202]", functionData => eval(functionData))}

    inherits(ControlledSubject, __super__);

    function ControlledSubject(enableQueue, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[249304:249749]", functionData => eval(functionData))}

    addProperties(ControlledSubject.prototype, Observer, {
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[249841:250070]", functionData => eval(functionData))},
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[250104:250360]", functionData => eval(functionData))},
      onNext: function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[250393:250772]", functionData => eval(functionData))},
      _processRequest: function (numberOfItems) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[250822:251453]", functionData => eval(functionData))},
      request: function (number) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[251488:252002]", functionData => eval(functionData))},
      disposeCurrentRequest: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[252045:252150]", functionData => eval(functionData))}
    });

    return ControlledSubject;
  }(Observable));

  /**
   * Attaches a controller to the observable sequence with the ability to queue.
   * @example
   * var source = Rx.Observable.interval(100).controlled();
   * source.request(3); // Reads 3 values
   * @param {bool} enableQueue truthy value to determine if values should be queued pending the next request
   * @param {Scheduler} scheduler determines how the requests will be scheduled
   * @returns {Observable} The observable sequence which only propagates values on request.
   */
  observableProto.controlled = function (enableQueue, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[252764:253010]", functionData => eval(functionData))};

  var StopAndWaitObservable = (function (__super__) {

    function subscribe (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[253102:253349]", functionData => eval(functionData))}

    inherits(StopAndWaitObservable, __super__);

    function StopAndWaitObservable (source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[253444:253526]", functionData => eval(functionData))}

    var StopAndWaitObserver = (function (__sub__) {

      inherits(StopAndWaitObserver, __sub__);

      function StopAndWaitObserver (observer, observable, cancel) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[253694:253833]", functionData => eval(functionData))}

      var stopAndWaitObserverProto = StopAndWaitObserver.prototype;

      stopAndWaitObserverProto.completed = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[253959:254029]", functionData => eval(functionData))};

      stopAndWaitObserverProto.error = function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[254088:254159]", functionData => eval(functionData))}

      stopAndWaitObserverProto.next = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[254216:254393]", functionData => eval(functionData))};

      stopAndWaitObserverProto.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[254449:254634]", functionData => eval(functionData))};

      return StopAndWaitObserver;
    }(AbstractObserver));

    return StopAndWaitObservable;
  }(Observable));


  /**
   * Attaches a stop and wait observable to the current observable.
   * @returns {Observable} A stop and wait observable.
   */
  ControlledObservable.prototype.stopAndWait = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[254946:254995]", functionData => eval(functionData))};

  var WindowedObservable = (function (__super__) {

    function subscribe (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[255084:255356]", functionData => eval(functionData))}

    inherits(WindowedObservable, __super__);

    function WindowedObservable(source, windowSize) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[255456:255574]", functionData => eval(functionData))}

    var WindowedObserver = (function (__sub__) {

      inherits(WindowedObserver, __sub__);

      function WindowedObserver(observer, observable, cancel) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[255732:255870]", functionData => eval(functionData))}

      var windowedObserverPrototype = WindowedObserver.prototype;

      windowedObserverPrototype.completed = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[255995:256065]", functionData => eval(functionData))};

      windowedObserverPrototype.error = function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[256125:256196]", functionData => eval(functionData))};

      windowedObserverPrototype.next = function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[256255:256580]", functionData => eval(functionData))};

      windowedObserverPrototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[256637:256822]", functionData => eval(functionData))};

      return WindowedObserver;
    }(AbstractObserver));

    return WindowedObservable;
  }(Observable));

  /**
   * Creates a sliding windowed observable based upon the window size.
   * @param {Number} windowSize The number of items in the window
   * @returns {Observable} A windowed observable based upon the window size.
   */
  ControlledObservable.prototype.windowed = function (windowSize) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[257225:257283]", functionData => eval(functionData))};

  /**
   * Pipes the existing Observable sequence into a Node.js Stream.
   * @param {Stream} dest The destination Node.js stream.
   * @returns {Stream} The destination stream.
   */
  observableProto.pipe = function (dest) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[257511:258029]", functionData => eval(functionData))};

  /**
   * Multicasts the source sequence notifications through an instantiated subject into all uses of the sequence within a selector function. Each
   * subscription to the resulting sequence causes a separate multicast invocation, exposing the sequence resulting from the selector function's
   * invocation. For specializations with fixed subject types, see Publish, PublishLast, and Replay.
   *
   * @example
   * 1 - res = source.multicast(observable);
   * 2 - res = source.multicast(function () { return new Subject(); }, function (x) { return x; });
   *
   * @param {Function|Subject} subjectOrSubjectSelector
   * Factory function to create an intermediate subject through which the source sequence's elements will be multicast to the selector function.
   * Or:
   * Subject to push source elements into.
   *
   * @param {Function} [selector] Optional selector function which can use the multicasted source sequence subject to the policies enforced by the created subject. Specified only if <paramref name="subjectOrSubjectSelector" is a factory function.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.multicast = function (subjectOrSubjectSelector, selector) {
    var source = this;
    return typeof subjectOrSubjectSelector === 'function' ?
      new AnonymousObservable(function (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[259484:259671]", functionData => eval(functionData))}, source) :
      new ConnectableObservable(source, subjectOrSubjectSelector);
  };

  /**
   * Returns an observable sequence that is the result of invoking the selector on a connectable observable sequence that shares a single subscription to the underlying sequence.
   * This operator is a specialization of Multicast using a regular Subject.
   *
   * @example
   * var resres = source.publish();
   * var res = source.publish(function (x) { return x; });
   *
   * @param {Function} [selector] Selector function which can use the multicasted source sequence as many times as needed, without causing multiple subscriptions to the source sequence. Subscribers to the given source will receive all notifications of the source from the time of the subscription on.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.publish = function (selector) {
    return selector && isFunction(selector) ?
      this.multicast(function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[260737:260762]", functionData => eval(functionData))}, selector) :
      this.multicast(new Subject());
  };

  /**
   * Returns an observable sequence that shares a single subscription to the underlying sequence.
   * This operator is a specialization of publish which creates a subscription when the number of observers goes from zero to one, then shares that subscription with all subsequent observers until the number of observers returns to zero, at which point the subscription is disposed.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence.
   */
  observableProto.share = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[261387:261430]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence that is the result of invoking the selector on a connectable observable sequence that shares a single subscription to the underlying sequence containing only the last notification.
   * This operator is a specialization of Multicast using a AsyncSubject.
   *
   * @example
   * var res = source.publishLast();
   * var res = source.publishLast(function (x) { return x; });
   *
   * @param selector [Optional] Selector function which can use the multicasted source sequence as many times as needed, without causing multiple subscriptions to the source sequence. Subscribers to the given source will only receive the last notification of the source.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.publishLast = function (selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[262347:262517]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence that is the result of invoking the selector on a connectable observable sequence that shares a single subscription to the underlying sequence and starts with initialValue.
   * This operator is a specialization of Multicast using a BehaviorSubject.
   *
   * @example
   * var res = source.publishValue(42);
   * var res = source.publishValue(function (x) { return x.select(function (y) { return y * y; }) }, 42);
   *
   * @param {Function} [selector] Optional selector function which can use the multicasted source sequence as many times as needed, without causing multiple subscriptions to the source sequence. Subscribers to the given source will receive immediately receive the initial value, followed by all notifications of the source from the time of the subscription on.
   * @param {Mixed} initialValue Initial value received by observers upon subscription.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.publishValue = function (initialValueOrSelector, initialValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[263681:263909]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence that shares a single subscription to the underlying sequence and starts with an initialValue.
   * This operator is a specialization of publishValue which creates a subscription when the number of observers goes from zero to one, then shares that subscription with all subsequent observers until the number of observers returns to zero, at which point the subscription is disposed.
   * @param {Mixed} initialValue Initial value received by observers upon subscription.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence.
   */
  observableProto.shareValue = function (initialValue) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[264622:264682]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence that is the result of invoking the selector on a connectable observable sequence that shares a single subscription to the underlying sequence replaying notifications subject to a maximum time length for the replay buffer.
   * This operator is a specialization of Multicast using a ReplaySubject.
   *
   * @example
   * var res = source.replay(null, 3);
   * var res = source.replay(null, 3, 500);
   * var res = source.replay(null, 3, 500, scheduler);
   * var res = source.replay(function (x) { return x.take(6).repeat(); }, 3, 500, scheduler);
   *
   * @param selector [Optional] Selector function which can use the multicasted source sequence as many times as needed, without causing multiple subscriptions to the source sequence. Subscribers to the given source will receive all the notifications of the source subject to the specified replay buffer trimming policy.
   * @param bufferSize [Optional] Maximum element count of the replay buffer.
   * @param windowSize [Optional] Maximum time length of the replay buffer.
   * @param scheduler [Optional] Scheduler where connected observers within the selector function will be invoked on.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.replay = function (selector, bufferSize, windowSize, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[266125:266363]", functionData => eval(functionData))};

  /**
   * Returns an observable sequence that shares a single subscription to the underlying sequence replaying notifications subject to a maximum time length for the replay buffer.
   * This operator is a specialization of replay which creates a subscription when the number of observers goes from zero to one, then shares that subscription with all subsequent observers until the number of observers returns to zero, at which point the subscription is disposed.
   *
   * @example
   * var res = source.shareReplay(3);
   * var res = source.shareReplay(3, 500);
   * var res = source.shareReplay(3, 500, scheduler);
   *

   * @param bufferSize [Optional] Maximum element count of the replay buffer.
   * @param window [Optional] Maximum time length of the replay buffer.
   * @param scheduler [Optional] Scheduler where connected observers within the selector function will be invoked on.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence.
   */
  observableProto.shareReplay = function (bufferSize, windowSize, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[267479:267560]", functionData => eval(functionData))};

  var InnerSubscription = function (subject, observer) {
    this.subject = subject;
    this.observer = observer;
  };

  InnerSubscription.prototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[267736:267945]", functionData => eval(functionData))};

  /**
   *  Represents a value that changes over time.
   *  Observers can subscribe to the subject to receive the last (or initial) value and all subsequent notifications.
   */
  var BehaviorSubject = Rx.BehaviorSubject = (function (__super__) {
    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[268229:268580]", functionData => eval(functionData))}

    inherits(BehaviorSubject, __super__);

    /**
     *  Initializes a new instance of the BehaviorSubject class which creates a subject that caches its last value and starts with the specified value.
     *  @param {Mixed} value Initial value sent to observers when no other value has been received by the subject yet.
     */
    function BehaviorSubject(value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[268948:269137]", functionData => eval(functionData))}

    addProperties(BehaviorSubject.prototype, Observer, {
      /**
       * Gets the current value or throws an exception.
       * Value is frozen after onCompleted is called.
       * After onError is called always throws the specified exception.
       * An exception is always thrown after dispose is called.
       * @returns {Mixed} The initial value passed to the constructor until onNext is called; after which, the last value passed to onNext.
       */
      getValue: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[269630:269774]", functionData => eval(functionData))},
      /**
       * Indicates whether the subject has observers subscribed to it.
       * @returns {Boolean} Indicates whether the subject has observers subscribed to it.
       */
      hasObservers: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[269989:270026]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the end of the sequence.
       */
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[270153:270429]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the exception.
       * @param {Mixed} error The exception to send to all observers.
       */
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[270617:270953]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the arrival of the specified element in the sequence.
       * @param {Mixed} value The value to send to all observers.
       */
      onNext: function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[271175:271412]", functionData => eval(functionData))},
      /**
       * Unsubscribe all observers and release resources.
       */
      dispose: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[271519:271649]", functionData => eval(functionData))}
    });

    return BehaviorSubject;
  }(Observable));

  /**
   * Represents an object that is both an observable sequence as well as an observer.
   * Each notification is broadcasted to all subscribed and future observers, subject to buffer trimming policies.
   */
  var ReplaySubject = Rx.ReplaySubject = (function (__super__) {

    var maxSafeInteger = Math.pow(2, 53) - 1;

    function createRemovableDisposable(subject, observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[272090:272276]", functionData => eval(functionData))}

    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[272311:272826]", functionData => eval(functionData))}

    inherits(ReplaySubject, __super__);

    /**
     *  Initializes a new instance of the ReplaySubject class with the specified buffer size, window size and scheduler.
     *  @param {Number} [bufferSize] Maximum element count of the replay buffer.
     *  @param {Number} [windowSize] Maximum time length of the replay buffer.
     *  @param {Scheduler} [scheduler] Scheduler the observers are invoked on.
     */
    function ReplaySubject(bufferSize, windowSize, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[273307:273722]", functionData => eval(functionData))}

    addProperties(ReplaySubject.prototype, Observer.prototype, {
      /**
       * Indicates whether the subject has observers subscribed to it.
       * @returns {Boolean} Indicates whether the subject has observers subscribed to it.
       */
      hasObservers: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[274002:274053]", functionData => eval(functionData))},
      _trim: function (now) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[274083:274298]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the arrival of the specified element in the sequence.
       * @param {Mixed} value The value to send to all observers.
       */
      onNext: function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[274520:274919]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the exception.
       * @param {Mixed} error The exception to send to all observers.
       */
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[275107:275576]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the end of the sequence.
       */
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[275703:276113]", functionData => eval(functionData))},
      /**
       * Unsubscribe all observers and release resources.
       */
      dispose: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[276220:276292]", functionData => eval(functionData))}
    });

    return ReplaySubject;
  }(Observable));

  var ConnectableObservable = Rx.ConnectableObservable = (function (__super__) {
    inherits(ConnectableObservable, __super__);

    function ConnectableObservable(source, subject) {
      var hasSubscription = false,
        subscription,
        sourceObservable = source.asObservable();

      this.connect = function () {
        if (!hasSubscription) {
          hasSubscription = true;
          subscription = new CompositeDisposable(sourceObservable.subscribe(subject), disposableCreate(function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[276855:276905]", functionData => eval(functionData))}));
        }
        return subscription;
      };

      __super__.call(this, function (o) { return subject.subscribe(o); });
    }

    ConnectableObservable.prototype.refCount = function () {
      var connectableSubscription, count = 0, source = this;
      return new AnonymousObservable(function (observer) {
          var shouldConnect = ++count === 1,
            subscription = source.subscribe(observer);
          shouldConnect && (connectableSubscription = source.connect());
          return function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[277423:277536]", functionData => eval(functionData))};
      });
    };

    return ConnectableObservable;
  }(Observable));

  /**
   * Returns an observable sequence that shares a single subscription to the underlying sequence. This observable sequence
   * can be resubscribed to, even if all prior subscriptions have ended. (unlike `.publish().refCount()`)
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source.
   */
  observableProto.singleInstance = function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[278024:278415]", functionData => eval(functionData))};

  var Dictionary = (function () {

    var primes = [1, 3, 7, 13, 31, 61, 127, 251, 509, 1021, 2039, 4093, 8191, 16381, 32749, 65521, 131071, 262139, 524287, 1048573, 2097143, 4194301, 8388593, 16777213, 33554393, 67108859, 134217689, 268435399, 536870909, 1073741789, 2147483647],
      noSuchkey = "no such key",
      duplicatekey = "duplicate key";

    function isPrime(candidate) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[278804:279058]", functionData => eval(functionData))}

    function getPrime(min) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[279087:279445]", functionData => eval(functionData))}

    function stringHashFn(str) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[279478:279756]", functionData => eval(functionData))}

    function numberHashFn(key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[279789:279995]", functionData => eval(functionData))}

    var getHashCode = (function () {
      var uniqueIdCounter = 0;

      return function (obj) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[280094:281040]", functionData => eval(functionData))};
    }());

    function newEntry() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[281077:281147]", functionData => eval(functionData))}

    function Dictionary(capacity, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[281193:281446]", functionData => eval(functionData))}

    var dictionaryProto = Dictionary.prototype;

    dictionaryProto._initialize = function (capacity) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[281551:281815]", functionData => eval(functionData))};

    dictionaryProto.add = function (key, value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[281866:281911]", functionData => eval(functionData))};

    dictionaryProto._insert = function (key, value, add) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[281971:283029]", functionData => eval(functionData))};

    dictionaryProto._resize = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[283074:283750]", functionData => eval(functionData))};

    dictionaryProto.remove = function (key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[283797:284732]", functionData => eval(functionData))};

    dictionaryProto.clear = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[284775:285106]", functionData => eval(functionData))};

    dictionaryProto._findEntry = function (key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[285157:285527]", functionData => eval(functionData))};

    dictionaryProto.count = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[285570:285618]", functionData => eval(functionData))};

    dictionaryProto.tryGetValue = function (key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[285670:285798]", functionData => eval(functionData))};

    dictionaryProto.getValues = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[285845:286137]", functionData => eval(functionData))};

    dictionaryProto.get = function (key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[286181:286322]", functionData => eval(functionData))};

    dictionaryProto.set = function (key, value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[286373:286419]", functionData => eval(functionData))};

    dictionaryProto.containskey = function (key) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[286471:286518]", functionData => eval(functionData))};

    return Dictionary;
  }());

  /**
   *  Correlates the elements of two sequences based on overlapping durations.
   *
   *  @param {Observable} right The right observable sequence to join elements for.
   *  @param {Function} leftDurationSelector A function to select the duration (expressed as an observable sequence) of each element of the left observable sequence, used to determine overlap.
   *  @param {Function} rightDurationSelector A function to select the duration (expressed as an observable sequence) of each element of the right observable sequence, used to determine overlap.
   *  @param {Function} resultSelector A function invoked to compute a result element for any two overlapping elements of the left and right observable sequences. The parameters passed to the function correspond with the elements from the left and right source sequences for which overlap occurs.
   *  @returns {Observable} An observable sequence that contains result elements computed from source elements that have an overlapping duration.
   */
  observableProto.join = function (right, leftDurationSelector, rightDurationSelector, resultSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[287667:290301]", functionData => eval(functionData))};

  /**
   *  Correlates the elements of two sequences based on overlapping durations, and groups the results.
   *
   *  @param {Observable} right The right observable sequence to join elements for.
   *  @param {Function} leftDurationSelector A function to select the duration (expressed as an observable sequence) of each element of the left observable sequence, used to determine overlap.
   *  @param {Function} rightDurationSelector A function to select the duration (expressed as an observable sequence) of each element of the right observable sequence, used to determine overlap.
   *  @param {Function} resultSelector A function invoked to compute a result element for any element of the left sequence with overlapping elements from the right observable sequence. The first parameter passed to the function is an element of the left sequence. The second parameter passed to the function is an observable sequence with elements from the right sequence that overlap with the left sequence's element.
   *  @returns {Observable} An observable sequence that contains result elements computed from source elements that have an overlapping duration.
   */
  observableProto.groupJoin = function (right, leftDurationSelector, rightDurationSelector, resultSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[291569:294346]", functionData => eval(functionData))};

    /**
     *  Projects each element of an observable sequence into zero or more buffers.
     *
     *  @param {Mixed} bufferOpeningsOrClosingSelector Observable sequence whose elements denote the creation of new windows, or, a function invoked to define the boundaries of the produced windows (a new window is started when the previous one is closed, resulting in non-overlapping windows).
     *  @param {Function} [bufferClosingSelector] A function invoked to define the closing of each produced window. If a closing selector function is specified for the first parameter, this parameter is ignored.
     *  @returns {Observable} An observable sequence of windows.
     */
    observableProto.buffer = function (bufferOpeningsOrClosingSelector, bufferClosingSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[295122:295229]", functionData => eval(functionData))};

  /**
   *  Projects each element of an observable sequence into zero or more windows.
   *
   *  @param {Mixed} windowOpeningsOrClosingSelector Observable sequence whose elements denote the creation of new windows, or, a function invoked to define the boundaries of the produced windows (a new window is started when the previous one is closed, resulting in non-overlapping windows).
   *  @param {Function} [windowClosingSelector] A function invoked to define the closing of each produced window. If a closing selector function is specified for the first parameter, this parameter is ignored.
   *  @returns {Observable} An observable sequence of windows.
   */
  observableProto.window = function (windowOpeningsOrClosingSelector, windowClosingSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[295989:296419]", functionData => eval(functionData))};

  function observableWindowWithOpenings(windowOpenings, windowClosingSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[296501:296634]", functionData => eval(functionData))}

  function observableWindowWithBoundaries(windowBoundaries) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[296696:297635]", functionData => eval(functionData))}

  function observableWindowWithClosingSelector(windowClosingSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[297707:298985]", functionData => eval(functionData))}

  /**
   * Returns a new observable that triggers on the second and subsequent triggerings of the input observable.
   * The Nth triggering of the input observable passes the arguments from the N-1th and Nth triggering as a pair.
   * The argument passed to the N-1th triggering is held in hidden internal state until the Nth triggering occurs.
   * @returns {Observable} An observable that triggers on successive pairs of observations from the input observable as an array.
   */
  observableProto.pairwise = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[299509:299963]", functionData => eval(functionData))};

  /**
   * Returns two observables which partition the observations of the source by the given function.
   * The first will trigger observations for those values for which the predicate returns true.
   * The second will trigger observations for those values where the predicate returns false.
   * The predicate is executed once for each subscribed observer.
   * Both also propagate all error observations arising from the source and each completes
   * when the source completes.
   * @param {Function} predicate
   *    The function to determine which output Observable will trigger a particular observation.
   * @returns {Array}
   *    An array of observables. The first triggers when the predicate returns true,
   *    and the second triggers when the predicate returns false.
  */
  observableProto.partition = function(predicate, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[300817:300965]", functionData => eval(functionData))};

  var WhileEnumerable = (function(__super__) {
    inherits(WhileEnumerable, __super__);
    function WhileEnumerable(c, s) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[301092:301135]", functionData => eval(functionData))}
    WhileEnumerable.prototype[$iterator$] = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[301192:301398]", functionData => eval(functionData))};
    return WhileEnumerable;
  }(Enumerable));
  
  function enumerableWhile(condition, source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[301495:301551]", functionData => eval(functionData))}  

   /**
   *  Returns an observable sequence that is the result of invoking the selector on the source sequence, without sharing subscriptions.
   *  This operator allows for a fluent style of writing queries that use the same sequence multiple times.
   *
   * @param {Function} selector Selector function which can use the source sequence as many times as needed, without sharing subscriptions to the source sequence.
   * @returns {Observable} An observable sequence that contains the elements of a sequence produced by multicasting the source sequence within a selector function.
   */
  observableProto.letBind = observableProto['let'] = function (func) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[302213:302241]", functionData => eval(functionData))};

   /**
   *  Determines whether an observable collection contains values. There is an alias for this method called 'ifThen' for browsers <IE9
   *
   * @example
   *  1 - res = Rx.Observable.if(condition, obs1);
   *  2 - res = Rx.Observable.if(condition, obs1, obs2);
   *  3 - res = Rx.Observable.if(condition, obs1, scheduler);
   * @param {Function} condition The condition which determines if the thenSource or elseSource will be run.
   * @param {Observable} thenSource The observable sequence or Promise that will be run if the condition function returns true.
   * @param {Observable} [elseSource] The observable sequence or Promise that will be run if the condition function returns false. If this is not provided, it defaults to Rx.Observabe.Empty with the specified scheduler.
   * @returns {Observable} An observable sequence which is either the thenSource or elseSource.
   */
  Observable['if'] = Observable.ifThen = function (condition, thenSource, elseSourceOrScheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[303231:303787]", functionData => eval(functionData))};

   /**
   *  Concatenates the observable sequences obtained by running the specified result selector for each element in source.
   * There is an alias for this method called 'forIn' for browsers <IE9
   * @param {Array} sources An array of values to turn into an observable sequence.
   * @param {Function} resultSelector A function to apply to each item in the sources array to turn it into an observable sequence.
   * @returns {Observable} An observable sequence from the concatenated observable sequences.
   */
  Observable['for'] = Observable.forIn = function (sources, resultSelector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[304392:304465]", functionData => eval(functionData))};

   /**
   *  Repeats source as long as condition holds emulating a while loop.
   * There is an alias for this method called 'whileDo' for browsers <IE9
   *
   * @param {Function} condition The condition which determines if the source will be repeated.
   * @param {Observable} source The observable sequence that will be run if the condition function returns true.
   * @returns {Observable} An observable sequence which is repeated as long as the condition holds.
   */
  var observableWhileDo = Observable['while'] = Observable.whileDo = function (condition, source) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[305039:305167]", functionData => eval(functionData))};

   /**
   *  Repeats source as long as condition holds emulating a do while loop.
   *
   * @param {Function} condition The condition which determines if the source will be repeated.
   * @param {Observable} source The observable sequence that will be run if the condition function returns true.
   * @returns {Observable} An observable sequence which is repeated as long as the condition holds.
   */
  observableProto.doWhile = function (condition) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[305621:305699]", functionData => eval(functionData))};

   /**
   *  Uses selector to determine which source in sources to use.
   *  There is an alias 'switchCase' for browsers <IE9.
   *
   * @example
   *  1 - res = Rx.Observable.case(selector, { '1': obs1, '2': obs2 });
   *  1 - res = Rx.Observable.case(selector, { '1': obs1, '2': obs2 }, obs0);
   *  1 - res = Rx.Observable.case(selector, { '1': obs1, '2': obs2 }, scheduler);
   *
   * @param {Function} selector The function which extracts the value for to test in a case statement.
   * @param {Array} sources A object which has keys which correspond to the case statement labels.
   * @param {Observable} [elseSource] The observable sequence or Promise that will be run if the sources are not matched. If this is not provided, it defaults to Rx.Observabe.empty with the specified scheduler.
   *
   * @returns {Observable} An observable sequence which is determined by a case statement.
   */
  Observable['case'] = Observable.switchCase = function (selector, sources, defaultSourceOrScheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[306704:307256]", functionData => eval(functionData))};

   /**
   *  Expands an observable sequence by recursively invoking selector.
   *
   * @param {Function} selector Selector function to invoke for each produced element, resulting in another sequence to which the selector will be invoked recursively again.
   * @param {Scheduler} [scheduler] Scheduler on which to perform the expansion. If not provided, this defaults to the current thread scheduler.
   * @returns {Observable} An observable sequence containing all the elements produced by the recursive expansion.
   */
  observableProto.expand = function (selector, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[307840:309390]", functionData => eval(functionData))};

   /**
   *  Runs all observable sequences in parallel and collect their last elements.
   *
   * @example
   *  1 - res = Rx.Observable.forkJoin([obs1, obs2]);
   *  1 - res = Rx.Observable.forkJoin(obs1, obs2, ...);
   * @returns {Observable} An observable sequence with an array collecting the last elements of all the input sequences.
   */
  Observable.forkJoin = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[309774:311471]", functionData => eval(functionData))};

   /**
   *  Runs two observable sequences in parallel and combines their last elemenets.
   *
   * @param {Observable} second Second observable sequence.
   * @param {Function} resultSelector Result selector function to invoke with the last elements of both sequences.
   * @returns {Observable} An observable sequence with the result of calling the selector function with the last elements of both input sequences.
   */
  observableProto.forkJoin = function (second, resultSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[311960:314138]", functionData => eval(functionData))};

  /**
   * Comonadic bind operator.
   * @param {Function} selector A transform function to apply to each element.
   * @param {Object} scheduler Scheduler used to execute the operation. If not specified, defaults to the ImmediateScheduler.
   * @returns {Observable} An observable sequence which results from the comonadic bind operation.
   */
  observableProto.manySelect = observableProto.extend = function (selector, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[314574:315135]", functionData => eval(functionData))};

  var ChainObservable = (function (__super__) {

    function subscribe (observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[315221:315461]", functionData => eval(functionData))}

    inherits(ChainObservable, __super__);

    function ChainObservable(head) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[315541:315649]", functionData => eval(functionData))}

    addProperties(ChainObservable.prototype, Observer, {
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[315739:315789]", functionData => eval(functionData))},
      onError: function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[315819:315875]", functionData => eval(functionData))},
      onNext: function (v) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[315904:315975]", functionData => eval(functionData))}
    });

    return ChainObservable;

  }(Observable));

  /** @private */
  var Map = root.Map || (function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[316088:316723]", functionData => eval(functionData))}());

  /**
   * @constructor
   * Represents a join pattern over observable sequences.
   */
  function Pattern(patterns) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[316846:316881]", functionData => eval(functionData))}

  /**
   *  Creates a pattern that matches the current plan matches and when the specified observable sequences has an available value.
   *  @param other Observable sequence to match in addition to the current pattern.
   *  @return {Pattern} Pattern object that matches when all observable sequences in the pattern have an available value.
   */
  Pattern.prototype.and = function (other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[317274:317332]", functionData => eval(functionData))};

  /**
   *  Matches when all observable sequences in the pattern (specified using a chain of and operators) have an available value and projects the values.
   *  @param {Function} selector Selector that will be invoked with available values from the source sequences, in the same order of the sequences in the pattern.
   *  @return {Plan} Plan that produces the projected values, to be fed (with other plans) to the when operator.
   */
  Pattern.prototype.thenDo = function (selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[317823:317865]", functionData => eval(functionData))};

  function Plan(expression, selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[317906:317979]", functionData => eval(functionData))}

  Plan.prototype.activate = function (externalSubscriptions, observer, deactivate) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[318064:318913]", functionData => eval(functionData))};

  function planCreateObserver(externalSubscriptions, observable, onError) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[318990:319230]", functionData => eval(functionData))}

  function ActivePlan(joinObserverArray, onNext, onCompleted) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[319294:319634]", functionData => eval(functionData))}

  ActivePlan.prototype.dequeue = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[319681:319753]", functionData => eval(functionData))};

  ActivePlan.prototype.match = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[319799:320627]", functionData => eval(functionData))};

  var JoinObserver = (function (__super__) {
    inherits(JoinObserver, __super__);

    function JoinObserver(source, onError) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[320758:320994]", functionData => eval(functionData))}

    var JoinObserverPrototype = JoinObserver.prototype;

    JoinObserverPrototype.next = function (notification) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[321110:321463]", functionData => eval(functionData))};

    JoinObserverPrototype.error = noop;
    JoinObserverPrototype.completed = noop;

    JoinObserverPrototype.addActivePlan = function (activePlan) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[321615:321663]", functionData => eval(functionData))};

    JoinObserverPrototype.subscribe = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[321716:321805]", functionData => eval(functionData))};

    JoinObserverPrototype.removeActivePlan = function (activePlan) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[321875:322009]", functionData => eval(functionData))};

    JoinObserverPrototype.dispose = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[322060:322220]", functionData => eval(functionData))};

    return JoinObserver;
  } (AbstractObserver));

  /**
   *  Creates a pattern that matches when both observable sequences have an available value.
   *
   *  @param right Observable sequence to match with the current sequence.
   *  @return {Pattern} Pattern object that matches when both observable sequences have an available value.
   */
  observableProto.and = function (right) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[322608:322652]", functionData => eval(functionData))};

  /**
   *  Matches when the observable sequence has an available value and projects the value.
   *
   *  @param {Function} selector Selector that will be invoked for values in the source sequence.
   *  @returns {Plan} Plan that produces the projected values, to be fed (with other plans) to the when operator.
   */
  observableProto.thenDo = function (selector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[323021:323075]", functionData => eval(functionData))};

  /**
   *  Joins together the results from several patterns.
   *
   *  @param plans A series of plans (specified as an Array of as a series of arguments) created by use of the Then operator on patterns.
   *  @returns {Observable} Observable sequence with the results form matching several patterns.
   */
  Observable.when = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[323418:324668]", functionData => eval(functionData))};

  function observableTimerDate(dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[324722:324929]", functionData => eval(functionData))}

  function observableTimerDateAndPeriod(dueTime, period, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[324999:325407]", functionData => eval(functionData))}

  function observableTimerTimeSpan(dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[325464:325686]", functionData => eval(functionData))}

  function observableTimerTimeSpanAndPeriod(dueTime, period, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[325760:326152]", functionData => eval(functionData))}

  /**
   *  Returns an observable sequence that produces a value after each period.
   *
   * @example
   *  1 - res = Rx.Observable.interval(1000);
   *  2 - res = Rx.Observable.interval(1000, Rx.Scheduler.timeout);
   *
   * @param {Number} period Period for producing the values in the resulting sequence (specified as an integer denoting milliseconds).
   * @param {Scheduler} [scheduler] Scheduler to run the timer on. If not specified, Rx.Scheduler.timeout is used.
   * @returns {Observable} An observable sequence that produces a value after each period.
   */
  var observableinterval = Observable.interval = function (period, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[326801:326922]", functionData => eval(functionData))};

  /**
   *  Returns an observable sequence that produces a value after dueTime has elapsed and then after each period.
   * @param {Number} dueTime Absolute (specified as a Date object) or relative time (specified as an integer denoting milliseconds) at which to produce the first value.
   * @param {Mixed} [periodOrScheduler]  Period to produce subsequent values (specified as an integer denoting milliseconds), or the scheduler to run the timer on. If not specified, the resulting timer is not recurring.
   * @param {Scheduler} [scheduler]  Scheduler to run the timer on. If not specified, the timeout scheduler is used.
   * @returns {Observable} An observable sequence that produces a value after due time has elapsed and then each period.
   */
  var observableTimer = Observable.timer = function (dueTime, periodOrScheduler, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[327769:328525]", functionData => eval(functionData))};

  function observableDelayTimeSpan(source, dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[328591:330751]", functionData => eval(functionData))}

  function observableDelayDate(source, dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[330812:330950]", functionData => eval(functionData))}

  /**
   *  Time shifts the observable sequence by dueTime. The relative time intervals between the values are preserved.
   *
   * @example
   *  1 - res = Rx.Observable.delay(new Date());
   *  2 - res = Rx.Observable.delay(new Date(), Rx.Scheduler.timeout);
   *
   *  3 - res = Rx.Observable.delay(5000);
   *  4 - res = Rx.Observable.delay(5000, 1000, Rx.Scheduler.timeout);
   * @memberOf Observable#
   * @param {Number} dueTime Absolute (specified as a Date object) or relative time (specified as an integer denoting milliseconds) by which to shift the observable sequence.
   * @param {Scheduler} [scheduler] Scheduler to run the delay timers on. If not specified, the timeout scheduler is used.
   * @returns {Observable} Time-shifted sequence.
   */
  observableProto.delay = function (dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[331769:331994]", functionData => eval(functionData))};

  /**
   *  Ignores values from an observable sequence which are followed by another value before dueTime.
   * @param {Number} dueTime Duration of the debounce period for each value (specified as an integer denoting milliseconds).
   * @param {Scheduler} [scheduler]  Scheduler to run the debounce timers on. If not specified, the timeout scheduler is used.
   * @returns {Observable} The debounced sequence.
   */
  observableProto.debounce = observableProto.throttleWithTimeout = function (dueTime, scheduler) {
    isScheduler(scheduler) || (scheduler = timeoutScheduler);
    var source = this;
    return new AnonymousObservable(function (observer) {
      var cancelable = new SerialDisposable(), hasvalue = false, value, id = 0;
      var subscription = source.subscribe(
        function (x) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[332798:333185]", functionData => eval(functionData))},
        function (e) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[333208:333326]", functionData => eval(functionData))},
        function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[333348:333515]", functionData => eval(functionData))});
      return new CompositeDisposable(subscription, cancelable);
    }, this);
  };

  /**
   * @deprecated use #debounce or #throttleWithTimeout instead.
   */
  observableProto.throttle = function(dueTime, scheduler) {
    //deprecate('throttle', 'debounce or throttleWithTimeout');
    return this.debounce(dueTime, scheduler);
  };

  /**
   *  Projects each element of an observable sequence into zero or more windows which are produced based on timing information.
   * @param {Number} timeSpan Length of each window (specified as an integer denoting milliseconds).
   * @param {Mixed} [timeShiftOrScheduler]  Interval between creation of consecutive windows (specified as an integer denoting milliseconds), or an optional scheduler parameter. If not specified, the time shift corresponds to the timeSpan parameter, resulting in non-overlapping adjacent windows.
   * @param {Scheduler} [scheduler]  Scheduler to run windowing timers on. If not specified, the timeout scheduler is used.
   * @returns {Observable} An observable sequence of windows.
   */
  observableProto.windowWithTime = function (timeSpan, timeShiftOrScheduler, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[334666:337001]", functionData => eval(functionData))};

  /**
   *  Projects each element of an observable sequence into a window that is completed when either it's full or a given amount of time has elapsed.
   * @param {Number} timeSpan Maximum time length of a window.
   * @param {Number} count Maximum element count of a window.
   * @param {Scheduler} [scheduler]  Scheduler to run windowing timers on. If not specified, the timeout scheduler is used.
   * @returns {Observable} An observable sequence of windows.
   */
  observableProto.windowWithTimeOrCount = function (timeSpan, count, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[337554:339145]", functionData => eval(functionData))};

    /**
     *  Projects each element of an observable sequence into zero or more buffers which are produced based on timing information.
     *
     * @example
     *  1 - res = xs.bufferWithTime(1000, scheduler); // non-overlapping segments of 1 second
     *  2 - res = xs.bufferWithTime(1000, 500, scheduler; // segments of 1 second with time shift 0.5 seconds
     *
     * @param {Number} timeSpan Length of each buffer (specified as an integer denoting milliseconds).
     * @param {Mixed} [timeShiftOrScheduler]  Interval between creation of consecutive buffers (specified as an integer denoting milliseconds), or an optional scheduler parameter. If not specified, the time shift corresponds to the timeSpan parameter, resulting in non-overlapping adjacent buffers.
     * @param {Scheduler} [scheduler]  Scheduler to run buffer timers on. If not specified, the timeout scheduler is used.
     * @returns {Observable} An observable sequence of buffers.
     */
    observableProto.bufferWithTime = function (timeSpan, timeShiftOrScheduler, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[340207:340322]", functionData => eval(functionData))};

    /**
     *  Projects each element of an observable sequence into a buffer that is completed when either it's full or a given amount of time has elapsed.
     *
     * @example
     *  1 - res = source.bufferWithTimeOrCount(5000, 50); // 5s or 50 items in an array
     *  2 - res = source.bufferWithTimeOrCount(5000, 50, scheduler); // 5s or 50 items in an array
     *
     * @param {Number} timeSpan Maximum time length of a buffer.
     * @param {Number} count Maximum element count of a buffer.
     * @param {Scheduler} [scheduler]  Scheduler to run bufferin timers on. If not specified, the timeout scheduler is used.
     * @returns {Observable} An observable sequence of buffers.
     */
    observableProto.bufferWithTimeOrCount = function (timeSpan, count, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[341107:341254]", functionData => eval(functionData))};

  /**
   *  Records the time interval between consecutive values in an observable sequence.
   *
   * @example
   *  1 - res = source.timeInterval();
   *  2 - res = source.timeInterval(Rx.Scheduler.timeout);
   *
   * @param [scheduler]  Scheduler used to compute time intervals. If not specified, the timeout scheduler is used.
   * @returns {Observable} An observable sequence with time interval information on values.
   */
  observableProto.timeInterval = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[341739:342080]", functionData => eval(functionData))};

  /**
   *  Records the timestamp for each value in an observable sequence.
   *
   * @example
   *  1 - res = source.timestamp(); // produces { value: x, timestamp: ts }
   *  2 - res = source.timestamp(Rx.Scheduler.default);
   *
   * @param {Scheduler} [scheduler]  Scheduler used to compute timestamps. If not specified, the default scheduler is used.
   * @returns {Observable} An observable sequence with timestamp information on values.
   */
  observableProto.timestamp = function (scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[342584:342749]", functionData => eval(functionData))};

  function sampleObservable(source, sampler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[342797:343619]", functionData => eval(functionData))}

  /**
   *  Samples the observable sequence at each interval.
   *
   * @example
   *  1 - res = source.sample(sampleObservable); // Sampler tick sequence
   *  2 - res = source.sample(5000); // 5 seconds
   *  2 - res = source.sample(5000, Rx.Scheduler.timeout); // 5 seconds
   *
   * @param {Mixed} intervalOrSampler Interval at which to sample (specified as an integer denoting milliseconds) or Sampler Observable.
   * @param {Scheduler} [scheduler]  Scheduler to run the sampling timer on. If not specified, the timeout scheduler is used.
   * @returns {Observable} Sampled observable sequence.
   */
  observableProto.sample = observableProto.throttleLatest = function (intervalOrSampler, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[344328:344576]", functionData => eval(functionData))};

  /**
   *  Returns the source observable sequence or the other observable sequence if dueTime elapses.
   * @param {Number} dueTime Absolute (specified as a Date object) or relative time (specified as an integer denoting milliseconds) when a timeout occurs.
   * @param {Observable} [other]  Sequence to return in case of a timeout. If not specified, a timeout error throwing sequence will be used.
   * @param {Scheduler} [scheduler]  Scheduler to run the timeout timers on. If not specified, the timeout scheduler is used.
   * @returns {Observable} The source sequence switching to the other sequence in case of a timeout.
   */
  observableProto.timeout = function (dueTime, other, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[345277:346694]", functionData => eval(functionData))};

  /**
   *  Generates an observable sequence by iterating a state from an initial state until the condition fails.
   *
   * @example
   *  res = source.generateWithAbsoluteTime(0,
   *      function (x) { return return true; },
   *      function (x) { return x + 1; },
   *      function (x) { return x; },
   *      function (x) { return new Date(); }
   *  });
   *
   * @param {Mixed} initialState Initial state.
   * @param {Function} condition Condition to terminate generation (upon returning false).
   * @param {Function} iterate Iteration step function.
   * @param {Function} resultSelector Selector function for results produced in the sequence.
   * @param {Function} timeSelector Time selector function to control the speed of values being produced each iteration, returning Date values.
   * @param {Scheduler} [scheduler]  Scheduler on which to run the generator loop. If not specified, the timeout scheduler is used.
   * @returns {Observable} The generated sequence.
   */
  Observable.generateWithAbsoluteTime = function (initialState, condition, iterate, resultSelector, timeSelector, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[347814:348669]", functionData => eval(functionData))};

  /**
   *  Generates an observable sequence by iterating a state from an initial state until the condition fails.
   *
   * @example
   *  res = source.generateWithRelativeTime(0,
   *      function (x) { return return true; },
   *      function (x) { return x + 1; },
   *      function (x) { return x; },
   *      function (x) { return 500; }
   *  );
   *
   * @param {Mixed} initialState Initial state.
   * @param {Function} condition Condition to terminate generation (upon returning false).
   * @param {Function} iterate Iteration step function.
   * @param {Function} resultSelector Selector function for results produced in the sequence.
   * @param {Function} timeSelector Time selector function to control the speed of values being produced each iteration, returning integer values denoting milliseconds.
   * @param {Scheduler} [scheduler]  Scheduler on which to run the generator loop. If not specified, the timeout scheduler is used.
   * @returns {Observable} The generated sequence.
   */
  Observable.generateWithRelativeTime = function (initialState, condition, iterate, resultSelector, timeSelector, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[349806:350647]", functionData => eval(functionData))};

  /**
   *  Time shifts the observable sequence by delaying the subscription with the specified relative time duration, using the specified scheduler to run timers.
   *
   * @example
   *  1 - res = source.delaySubscription(5000); // 5s
   *  2 - res = source.delaySubscription(5000, Rx.Scheduler.default); // 5 seconds
   *
   * @param {Number} dueTime Relative or absolute time shift of the subscription.
   * @param {Scheduler} [scheduler]  Scheduler to run the subscription delay timer on. If not specified, the timeout scheduler is used.
   * @returns {Observable} Time-shifted sequence.
   */
  observableProto.delaySubscription = function (dueTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[351318:351755]", functionData => eval(functionData))};

  /**
   *  Time shifts the observable sequence based on a subscription delay and a delay selector function for each element.
   *
   * @example
   *  1 - res = source.delayWithSelector(function (x) { return Rx.Scheduler.timer(5000); }); // with selector only
   *  1 - res = source.delayWithSelector(Rx.Observable.timer(2000), function (x) { return Rx.Observable.timer(x); }); // with delay and selector
   *
   * @param {Observable} [subscriptionDelay]  Sequence indicating the delay for the subscription to the source.
   * @param {Function} delayDurationSelector Selector function to retrieve a sequence indicating the delay for each given element.
   * @returns {Observable} Time-shifted sequence.
   */
  observableProto.delayWithSelector = function (subscriptionDelay, delayDurationSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[352557:354175]", functionData => eval(functionData))};

    /**
     *  Returns the source observable sequence, switching to the other observable sequence if a timeout is signaled.
     * @param {Observable} [firstTimeout]  Observable sequence that represents the timeout for the first element. If not provided, this defaults to Observable.never().
     * @param {Function} timeoutDurationSelector Selector to retrieve an observable sequence that represents the timeout between the current element and the next element.
     * @param {Observable} [other]  Sequence to return in case of a timeout. If not provided, this is set to Observable.throwException().
     * @returns {Observable} The source sequence switching to the other sequence in case of a timeout.
     */
    observableProto.timeoutWithSelector = function (firstTimeout, timeoutdurationSelector, other) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[354989:356922]", functionData => eval(functionData))};

  /**
   * Ignores values from an observable sequence which are followed by another value within a computed throttle duration.
   * @param {Function} durationSelector Selector function to retrieve a sequence indicating the throttle duration for each given element.
   * @returns {Observable} The debounced sequence.
   */
  observableProto.debounceWithSelector = function (durationSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[357316:358681]", functionData => eval(functionData))};

  /**
   * @deprecated use #debounceWithSelector instead.
   */
  observableProto.throttleWithSelector = function (durationSelector) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[358817:358943]", functionData => eval(functionData))};

  /**
   *  Skips elements for the specified duration from the end of the observable source sequence, using the specified scheduler to run timers.
   *
   *  1 - res = source.skipLastWithTime(5000);
   *  2 - res = source.skipLastWithTime(5000, scheduler);
   *
   * @description
   *  This operator accumulates a queue with a length enough to store elements received during the initial duration window.
   *  As more elements are received, elements older than the specified duration are taken from the queue and produced on the
   *  result sequence. This causes elements to be delayed with duration.
   * @param {Number} duration Duration for skipping elements from the end of the sequence.
   * @param {Scheduler} [scheduler]  Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout
   * @returns {Observable} An observable sequence with the elements skipped during the specified duration from the end of the source sequence.
   */
  observableProto.skipLastWithTime = function (duration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[359975:360625]", functionData => eval(functionData))};

  /**
   *  Returns elements within the specified duration from the end of the observable source sequence, using the specified schedulers to run timers and to drain the collected elements.
   * @description
   *  This operator accumulates a queue with a length enough to store elements received during the initial duration window.
   *  As more elements are received, elements older than the specified duration are taken from the queue and produced on the
   *  result sequence. This causes elements to be delayed with duration.
   * @param {Number} duration Duration for taking elements from the end of the sequence.
   * @param {Scheduler} [scheduler]  Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout.
   * @returns {Observable} An observable sequence with the elements taken during the specified duration from the end of the source sequence.
   */
  observableProto.takeLastWithTime = function (duration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[361581:362248]", functionData => eval(functionData))};

  /**
   *  Returns an array with the elements within the specified duration from the end of the observable source sequence, using the specified scheduler to run timers.
   * @description
   *  This operator accumulates a queue with a length enough to store elements received during the initial duration window.
   *  As more elements are received, elements older than the specified duration are taken from the queue and produced on the
   *  result sequence. This causes elements to be delayed with duration.
   * @param {Number} duration Duration for taking elements from the end of the sequence.
   * @param {Scheduler} scheduler Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout.
   * @returns {Observable} An observable sequence containing a single array with the elements taken during the specified duration from the end of the source sequence.
   */
  observableProto.takeLastBufferWithTime = function (duration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[363214:363908]", functionData => eval(functionData))};

  /**
   *  Takes elements for the specified duration from the start of the observable source sequence, using the specified scheduler to run timers.
   *
   * @example
   *  1 - res = source.takeWithTime(5000,  [optional scheduler]);
   * @description
   *  This operator accumulates a queue with a length enough to store elements received during the initial duration window.
   *  As more elements are received, elements older than the specified duration are taken from the queue and produced on the
   *  result sequence. This causes elements to be delayed with duration.
   * @param {Number} duration Duration for taking elements from the start of the sequence.
   * @param {Scheduler} scheduler Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout.
   * @returns {Observable} An observable sequence with the elements taken during the specified duration from the start of the source sequence.
   */
  observableProto.takeWithTime = function (duration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[364906:365197]", functionData => eval(functionData))};

  /**
   *  Skips elements for the specified duration from the start of the observable source sequence, using the specified scheduler to run timers.
   *
   * @example
   *  1 - res = source.skipWithTime(5000, [optional scheduler]);
   *
   * @description
   *  Specifying a zero value for duration doesn't guarantee no elements will be dropped from the start of the source sequence.
   *  This is a side-effect of the asynchrony introduced by the scheduler, where the action that causes callbacks from the source sequence to be forwarded
   *  may not execute immediately, despite the zero due time.
   *
   *  Errors produced by the source sequence are always forwarded to the result sequence, even if the error occurs before the duration.
   * @param {Number} duration Duration for skipping elements from the start of the sequence.
   * @param {Scheduler} scheduler Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout.
   * @returns {Observable} An observable sequence with the elements skipped during the specified duration from the start of the source sequence.
   */
  observableProto.skipWithTime = function (duration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[366367:366815]", functionData => eval(functionData))};

  /**
   *  Skips elements from the observable source sequence until the specified start time, using the specified scheduler to run timers.
   *  Errors produced by the source sequence are always forwarded to the result sequence, even if the error occurs before the start time.
   *
   * @examples
   *  1 - res = source.skipUntilWithTime(new Date(), [scheduler]);
   *  2 - res = source.skipUntilWithTime(5000, [scheduler]);
   * @param {Date|Number} startTime Time to start taking elements from the source sequence. If this value is less than or equal to Date(), no elements will be skipped.
   * @param {Scheduler} [scheduler] Scheduler to run the timer on. If not specified, defaults to Rx.Scheduler.timeout.
   * @returns {Observable} An observable sequence with the elements skipped until the specified start time.
   */
  observableProto.skipUntilWithTime = function (startTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[367715:368271]", functionData => eval(functionData))};

  /**
   *  Takes elements for the specified duration until the specified end time, using the specified scheduler to run timers.
   * @param {Number | Date} endTime Time to stop taking elements from the source sequence. If this value is less than or equal to new Date(), the result stream will complete immediately.
   * @param {Scheduler} [scheduler] Scheduler to run the timer on.
   * @returns {Observable} An observable sequence with the elements taken until the specified end time.
   */
  observableProto.takeUntilWithTime = function (endTime, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[368835:369243]", functionData => eval(functionData))};

  /**
   * Returns an Observable that emits only the first item emitted by the source Observable during sequential time windows of a specified duration.
   * @param {Number} windowDuration time to wait before emitting another item after emitting the last item
   * @param {Scheduler} [scheduler] the Scheduler to use internally to manage the timers that handle timeout for each item. If not provided, defaults to Scheduler.timeout.
   * @returns {Observable} An Observable that performs the throttle operation.
   */
  observableProto.throttleFirst = function (windowDuration, scheduler) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[369834:370462]", functionData => eval(functionData))};

  /**
   * Executes a transducer to transform the observable sequence
   * @param {Transducer} transducer A transducer to execute
   * @returns {Observable} An Observable sequence containing the results from the transducer.
   */
  observableProto.transduce = function(transducer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[370746:371523]", functionData => eval(functionData))};

  /*
   * Performs a exclusive waiting for the first to finish before subscribing to another observable.
   * Observables that come in between subscriptions will be dropped on the floor.
   * @returns {Observable} A exclusive observable with only the results that happen when subscribed.
   */
  observableProto.exclusive = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[371862:373093]", functionData => eval(functionData))};

  /*
   * Performs a exclusive map waiting for the first to finish before subscribing to another observable.
   * Observables that come in between subscriptions will be dropped on the floor.
   * @param {Function} selector Selector to invoke for every item in the current subscription.
   * @param {Any} [thisArg] An optional context to invoke with the selector parameter.
   * @returns {Observable} An exclusive observable with only the results that happen when subscribed.
   */
  observableProto.exclusiveMap = function (selector, thisArg) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[373639:375229]", functionData => eval(functionData))};

  /** Provides a set of extension methods for virtual time scheduling. */
  Rx.VirtualTimeScheduler = (function (__super__) {

    function localNow() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[375383:375438]", functionData => eval(functionData))}

    function scheduleNow(state, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[375480:375559]", functionData => eval(functionData))}

    function scheduleRelative(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[375615:375708]", functionData => eval(functionData))}

    function scheduleAbsolute(state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[375764:375870]", functionData => eval(functionData))}

    function invokeAction(scheduler, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[375917:375970]", functionData => eval(functionData))}

    inherits(VirtualTimeScheduler, __super__);

    /**
     * Creates a new virtual time scheduler with the specified initial clock value and absolute time comparer.
     *
     * @constructor
     * @param {Number} initialClock Initial value for the clock.
     * @param {Function} comparer Comparer to determine causality of events based on absolute time.
     */
    function VirtualTimeScheduler(initialClock, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[376397:376630]", functionData => eval(functionData))}

    var VirtualTimeSchedulerPrototype = VirtualTimeScheduler.prototype;

    /**
     * Adds a relative time value to an absolute time value.
     * @param {Number} absolute Absolute virtual time value.
     * @param {Number} relative Relative virtual time value to add.
     * @return {Number} Resulting absolute virtual time sum value.
     */
    VirtualTimeSchedulerPrototype.add = notImplemented;

    /**
     * Converts an absolute time to a number
     * @param {Any} The absolute time.
     * @returns {Number} The absolute time in ms
     */
    VirtualTimeSchedulerPrototype.toDateTimeOffset = notImplemented;

    /**
     * Converts the TimeSpan value to a relative virtual time value.
     * @param {Number} timeSpan TimeSpan value to convert.
     * @return {Number} Corresponding relative virtual time value.
     */
    VirtualTimeSchedulerPrototype.toRelative = notImplemented;

    /**
     * Schedules a periodic piece of work by dynamically discovering the scheduler's capabilities. The periodic task will be emulated using recursive scheduling.
     * @param {Mixed} state Initial state passed to the action upon the first iteration.
     * @param {Number} period Period for running the work periodically.
     * @param {Function} action Action to be executed, potentially updating the state.
     * @returns {Disposable} The disposable object used to cancel the scheduled recurring action (best effort).
     */
    VirtualTimeSchedulerPrototype.schedulePeriodicWithState = function (state, period, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[378162:378267]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed after dueTime.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Number} dueTime Relative time after which to execute the action.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    VirtualTimeSchedulerPrototype.scheduleRelativeWithState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[378747:378870]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed at dueTime.
     * @param {Number} dueTime Relative time after which to execute the action.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    VirtualTimeSchedulerPrototype.scheduleRelative = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[379260:379343]", functionData => eval(functionData))};

    /**
     * Starts the virtual time scheduler.
     */
    VirtualTimeSchedulerPrototype.start = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[379458:379830]", functionData => eval(functionData))};

    /**
     * Stops the virtual time scheduler.
     */
    VirtualTimeSchedulerPrototype.stop = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[379943:379980]", functionData => eval(functionData))};

    /**
     * Advances the scheduler's clock to the specified time, running all work till that point.
     * @param {Number} time Absolute time to advance the scheduler's clock to.
     */
    VirtualTimeSchedulerPrototype.advanceTo = function (time) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[380235:380860]", functionData => eval(functionData))};

    /**
     * Advances the scheduler's clock by the specified relative time, running all work scheduled for that timespan.
     * @param {Number} time Relative time to advance the scheduler's clock by.
     */
    VirtualTimeSchedulerPrototype.advanceBy = function (time) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[381136:381375]", functionData => eval(functionData))};

    /**
     * Advances the scheduler's clock by the specified relative time.
     * @param {Number} time Relative time to advance the scheduler's clock by.
     */
    VirtualTimeSchedulerPrototype.sleep = function (time) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[381601:381762]", functionData => eval(functionData))};

    /**
     * Gets the next scheduled item to be executed.
     * @returns {ScheduledItem} The next scheduled item.
     */
    VirtualTimeSchedulerPrototype.getNext = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[381946:382172]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed at dueTime.
     * @param {Scheduler} scheduler Scheduler to execute the action on.
     * @param {Number} dueTime Absolute time at which to execute the action.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    VirtualTimeSchedulerPrototype.scheduleAbsolute = function (dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[382631:382714]", functionData => eval(functionData))};

    /**
     * Schedules an action to be executed at dueTime.
     * @param {Mixed} state State passed to the action to be executed.
     * @param {Number} dueTime Absolute time at which to execute the action.
     * @param {Function} action Action to be executed.
     * @returns {Disposable} The disposable object used to cancel the scheduled action (best effort).
     */
    VirtualTimeSchedulerPrototype.scheduleAbsoluteWithState = function (state, dueTime, action) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[383188:383476]", functionData => eval(functionData))};

    return VirtualTimeScheduler;
  }(Scheduler));

  /** Provides a virtual time scheduler that uses Date for absolute time and number for relative time. */
  Rx.HistoricalScheduler = (function (__super__) {
    inherits(HistoricalScheduler, __super__);

    /**
     * Creates a new historical scheduler with the specified initial clock value.
     * @constructor
     * @param {Number} initialClock Initial value for the clock.
     * @param {Function} comparer Comparer to determine causality of events based on absolute time.
     */
    function HistoricalScheduler(initialClock, comparer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[384074:384228]", functionData => eval(functionData))}

    var HistoricalSchedulerProto = HistoricalScheduler.prototype;

    /**
     * Adds a relative time value to an absolute time value.
     * @param {Number} absolute Absolute virtual time value.
     * @param {Number} relative Relative virtual time value to add.
     * @return {Number} Resulting absolute virtual time sum value.
     */
    HistoricalSchedulerProto.add = function (absolute, relative) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[384635:384676]", functionData => eval(functionData))};

    HistoricalSchedulerProto.toDateTimeOffset = function (absolute) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[384747:384797]", functionData => eval(functionData))};

    /**
     * Converts the TimeSpan value to a relative virtual time value.
     * @memberOf HistoricalScheduler
     * @param {Number} timeSpan TimeSpan value to convert.
     * @return {Number} Corresponding relative virtual time value.
     */
    HistoricalSchedulerProto.toRelative = function (timeSpan) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[385110:385140]", functionData => eval(functionData))};

    return HistoricalScheduler;
  }(Rx.VirtualTimeScheduler));

  var AnonymousObservable = Rx.AnonymousObservable = (function (__super__) {
    inherits(AnonymousObservable, __super__);

    // Fix subscriber to check for undefined or function returned to decorate as Disposable
    function fixSubscriber(subscriber) {
      return subscriber && isFunction(subscriber.dispose) ? subscriber :
        isFunction(subscriber) ? disposableCreate(subscriber) : disposableEmpty;
    }

    function setDisposable(s, state) {
      var ado = state[0], subscribe = state[1];
      var sub = tryCatch(subscribe)(ado);

      if (sub === errorObj) {
        if(!ado.fail(errorObj.e)) { return thrower(errorObj.e); }
      }
      ado.setDisposable(fixSubscriber(sub));
    }

    function AnonymousObservable(subscribe, parent) {
      this.source = parent;

      function s(observer) {
        var ado = new AutoDetachObserver(observer), state = [ado, subscribe];

        if (currentThreadScheduler.scheduleRequired()) {
          currentThreadScheduler.scheduleWithState(state, setDisposable);
        } else {
          setDisposable(null, state);
        }
        return ado;
      }

      __super__.call(this, s);
    }

    return AnonymousObservable;

  }(Observable));

  var AutoDetachObserver = (function (__super__) {
    inherits(AutoDetachObserver, __super__);

    function AutoDetachObserver(observer) {
      __super__.call(this);
      this.observer = observer;
      this.m = new SingleAssignmentDisposable();
    }

    var AutoDetachObserverPrototype = AutoDetachObserver.prototype;

    AutoDetachObserverPrototype.next = function (value) {
      var result = tryCatch(this.observer.onNext).call(this.observer, value);
      if (result === errorObj) {
        this.dispose();
        thrower(result.e);
      }
    };

    AutoDetachObserverPrototype.error = function (err) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[387034:387188]", functionData => eval(functionData))};

    AutoDetachObserverPrototype.completed = function () {
      var result = tryCatch(this.observer.onCompleted).call(this.observer);
      this.dispose();
      result === errorObj && thrower(result.e);
    };

    AutoDetachObserverPrototype.setDisposable = function (value) { this.m.setDisposable(value); };
    AutoDetachObserverPrototype.getDisposable = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[387562:387596]", functionData => eval(functionData))};

    AutoDetachObserverPrototype.dispose = function () {
      __super__.prototype.dispose.call(this);
      this.m.dispose();
    };

    return AutoDetachObserver;
  }(AbstractObserver));

  var GroupedObservable = (function (__super__) {
    inherits(GroupedObservable, __super__);

    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[387917:387984]", functionData => eval(functionData))}

    function GroupedObservable(key, underlyingObservable, mergedDisposable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[388062:388399]", functionData => eval(functionData))}

    return GroupedObservable;
  }(Observable));

  /**
   *  Represents an object that is both an observable sequence as well as an observer.
   *  Each notification is broadcasted to all subscribed observers.
   */
  var Subject = Rx.Subject = (function (__super__) {
    function subscribe(observer) {
      checkDisposed(this);
      if (!this.isStopped) {
        this.observers.push(observer);
        return new InnerSubscription(this, observer);
      }
      if (this.hasError) {
        observer.onError(this.error);
        return disposableEmpty;
      }
      observer.onCompleted();
      return disposableEmpty;
    }

    inherits(Subject, __super__);

    /**
     * Creates a subject.
     */
    function Subject() {
      __super__.call(this, subscribe);
      this.isDisposed = false,
      this.isStopped = false,
      this.observers = [];
      this.hasError = false;
    }

    addProperties(Subject.prototype, Observer.prototype, {
      /**
       * Indicates whether the subject has observers subscribed to it.
       * @returns {Boolean} Indicates whether the subject has observers subscribed to it.
       */
      hasObservers: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[389571:389608]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the end of the sequence.
       */
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[389735:390022]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the exception.
       * @param {Mixed} error The exception to send to all observers.
       */
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[390210:390560]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the arrival of the specified element in the sequence.
       * @param {Mixed} value The value to send to all observers.
       */
      onNext: function (value) {
        checkDisposed(this);
        if (!this.isStopped) {
          for (var i = 0, os = cloneArray(this.observers), len = os.length; i < len; i++) {
            os[i].onNext(value);
          }
        }
      },
      /**
       * Unsubscribe all observers and release resources.
       */
      dispose: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[391105:391177]", functionData => eval(functionData))}
    });

    /**
     * Creates a subject from the specified observer and observable.
     * @param {Observer} observer The observer used to send messages to the subject.
     * @param {Observable} observable The observable used to subscribe to messages sent from the subject.
     * @returns {Subject} Subject implemented using the given observer and observable.
     */
    Subject.create = function (observer, observable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[391603:391667]", functionData => eval(functionData))};

    return Subject;
  }(Observable));

  /**
   *  Represents the result of an asynchronous operation.
   *  The last value before the OnCompleted notification, or the error received through OnError, is sent to all subscribed observers.
   */
  var AsyncSubject = Rx.AsyncSubject = (function (__super__) {

    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[392010:392430]", functionData => eval(functionData))}

    inherits(AsyncSubject, __super__);

    /**
     * Creates a subject that can only receive one value and that value is cached for all future observations.
     * @constructor
     */
    function AsyncSubject() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[392647:392840]", functionData => eval(functionData))}

    addProperties(AsyncSubject.prototype, Observer, {
      /**
       * Indicates whether the subject has observers subscribed to it.
       * @returns {Boolean} Indicates whether the subject has observers subscribed to it.
       */
      hasObservers: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[393109:393189]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the end of the sequence, also causing the last received value to be sent out (if any).
       */
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[393378:393914]", functionData => eval(functionData))},
      /**
       * Notifies all subscribed observers about the error.
       * @param {Mixed} error The Error to send to all observers.
       */
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[394094:394445]", functionData => eval(functionData))},
      /**
       * Sends a value to the subject. The last value received before successful termination will be sent to all subscribed and future observers.
       * @param {Mixed} value The value to store in the subject.
       */
      onNext: function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[394709:394845]", functionData => eval(functionData))},
      /**
       * Unsubscribe all observers and release resources.
       */
      dispose: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[394952:395082]", functionData => eval(functionData))}
    });

    return AsyncSubject;
  }(Observable));

  var AnonymousSubject = Rx.AnonymousSubject = (function (__super__) {
    inherits(AnonymousSubject, __super__);

    function subscribe(observer) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[395284:395341]", functionData => eval(functionData))}

    function AnonymousSubject(observer, observable) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[395395:395509]", functionData => eval(functionData))}

    addProperties(AnonymousSubject.prototype, Observer.prototype, {
      onCompleted: function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[395610:395656]", functionData => eval(functionData))},
      onError: function (error) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[395690:395737]", functionData => eval(functionData))},
      onNext: function (value) {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[395770:395816]", functionData => eval(functionData))}
    });

    return AnonymousSubject;
  }(Observable));

  /**
  * Used to pause and resume streams.
  */
  Rx.Pauser = (function (__super__) {
    inherits(Pauser, __super__);

    function Pauser() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[396017:396052]", functionData => eval(functionData))}

    /**
     * Pauses the underlying sequence.
     */
    Pauser.prototype.pause = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[396150:396173]", functionData => eval(functionData))};

    /**
    * Resumes the underlying sequence.
    */
    Pauser.prototype.resume = function () {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[396272:396294]", functionData => eval(functionData))};

    return Pauser;
  }(Subject));

  if (typeof define == 'function' && typeof define.amd == 'object' && define.amd) {
    root.Rx = Rx;

    define(function() {lacuna_lazy_load("node_modules/rx/dist/rx.all.js[396457:396481]", functionData => eval(functionData))});
  } else if (freeExports && freeModule) {
    // in Node.js or RingoJS
    if (moduleExports) {
      (freeModule.exports = Rx).Rx = Rx;
    } else {
      freeExports.Rx = Rx;
    }
  } else {
    // in a browser or Rhino
    root.Rx = Rx;
  }

  // All code before this point will be filtered from stack traces.
  var rEndingLine = captureLine();

}.call(this));
